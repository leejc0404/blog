# 🩺 GSC-Daily-Report-Routine (일간 고장 점검) — v3.2

> **이 문서는 Cowork 예약 작업 `daily-blog-health-check`의 본문이다.**
> 이름은 주간 루틴(`GSC-Weekly-Report-Routine.md`)과 짝을 맞춘 것이지만, **성과 리포트가 아니라 고장 점검**이다. 성과 지표는 주간 루틴이 본다.
> ⚠️ 이 파일은 **기록용 사본**이다. 실제 실행되는 것은 Cowork 예약 작업에 저장된 본문이므로, 이 문서를 고치면 **예약 작업 본문도 같이 교체**해야 반영된다.
> GSC 계정: leejc0404@gmail.com
> **v3 변경점(2026-08-17)**: ① 항목 12 이미지 파이프라인 신설 ② 글 식별자를 Notion `Blog #N`으로 고정 ③ 항목 7·8·10·12를 REST 우선으로 전환(Chrome 미연결에도 확인 가능) ④ 항목 11에 Notion 장애 시 원고 직접 실측 폴백 추가 ⑤ `pw.txt` 파싱 시 공백 포함 값 주의 명시
> **v3.2 변경점(2026-08-26)**: **「오늘 발행 마감」 판정을 리포트 첫 줄로 고정.** 이 루틴은 지금까지 12개 항목을 다 통과시키고도 사용자가 매일 실제로 궁금해하는 것 — *오늘 만든 초안 중 오늘 안 내보내면 안 되는 게 있는가* — 에 답하지 않았다. 항목 10이 발행 순서를 보긴 하지만 **큐 전체의 마감을 보는 항목**이라, 오늘자 초안 2편에 대한 즉답이 리포트 어디에도 없었다(2026-08-26 사용자 지적). → **출력 규칙 최상단에 `0행` 신설**. 이상 유무와 무관하게 **모든 리포트의 첫 줄**에 오늘 마감 글의 유무를 쓴다. 정상인 날의 한 줄 출력도 이 판정을 포함하는 형태로 바꾼다.
>
> **v3.1 변경점(2026-08-20)**: **항목 12의 이미지 규격 기준을 실제 사양으로 갱신.** 舊 기준(984×409 / 984×469)은 Flow 전환 이전 값이라, 정상 산출물에 매일 🟡 오탐을 냈다(2026-08-20 실측 사고). 현행 기준은 **KoreaPlug 1226×768 / 0and1Life 1100×689**이며, 판정 규칙도 "고정값 일치"가 아니라 **"이미지 루틴 문서의 현재 표기와 일치"** 로 바꿨다 — 이미지 루틴은 앞으로도 바뀌므로 점검 루틴에 숫자를 박아두면 같은 사고가 반복된다.

koreaplug.com과 0and1life.com의 **고장 여부만** 매일 점검하고, 이상이 있으면 **바로 실행 가능한 해결책까지** 제시한다.

⚠️ **이 루틴의 목적은 "좋아졌나 나빠졌나"가 아니라 "켜졌나 꺼졌나"다.** 클릭·노출·CTR·순위·세션 같은 성과 지표는 **절대 보지 않는다.** 그건 토요일 주간 루틴(weekly-koreaplug-seo-check)의 일이다.

이유: 주간 루틴은 28일 롤링 윈도를 쓴다. 어제와 오늘은 27/28(=96%)이 같은 데이터라 일간으로 성과를 재면 전부 노이즈다(KoreaPlug 하루 평균 1.4클릭, 0and1Life 네이버 하루 3세션). 반면 아래 항목들은 이진 사건이라 하루 늦게 아는 것과 일주일 늦게 아는 것이 다르다.

---

## 출력 규칙

### 0행 — 「오늘 발행 마감」 판정 (v3.2 신설 · 리포트 맨 첫 줄, 예외 없음)

> **왜 첫 줄인가**: 사용자가 이 리포트를 열어서 제일 먼저 확인하는 것은 "지금 당장 내가 눌러야 할 게 있나"다. 12개 항목은 **고장 여부**를 보지만, 오늘 만든 초안 중 **오늘을 넘기면 가치가 사라지는 글**이 있는지는 그 12개 어디에도 안 나온다. 항목 10은 큐 전체의 선점 창을 보는 항목이라 판정 단위가 다르고, 리포트 중간에 있어서 눈에 늦게 띈다.

**대상**: 두 사이트의 초안 중 **오늘 생성된 것만** (`status=draft` 이면서 `date`가 오늘 00:00 이후). 어제 이전 초안과 예약글은 이 행에서 보지 않는다 — 그건 항목 10 소관이다.

**판정** — 각 초안에 대해 아래를 순서대로 본다. 하나라도 걸리면 「오늘 마감」이다.

1. **날짜가 박힌 이벤트인가** — 포커스 키워드·제목에 특정 날짜·회차·마감일이 들어 있고, 그 날짜가 **오늘 또는 내일**인가 (예: 청약 접수 마감, 발표일, 개막일, 요금 개정 시행일)
2. **오늘이 검색 피크인가** — 이벤트 당일에만 검색이 몰리는 유형인가 (경기 결과, 당첨 번호, 실시간 장애, 당일 교통 통제)
3. **다음 빈 슬롯이 마감을 넘기는가** — 이 글이 큐 순서대로 가면 배정될 슬롯이 그 이벤트의 선점 창 마감(KoreaPlug D-21 / 0and1Life D-14)을 넘는가
4. **Notion `Track`이 `S`이고 `배포 우선`이 `즉시`인가** — Writer가 스스로 긴급으로 표시했는가

⚠️ **Track `L`(에버그린 롱테일)은 이 판정에서 자동 탈락이다.** 마감이 없는 글이므로 1~4를 볼 필요가 없다. 오늘자 초안이 전부 `L`이면 그 자체로 「없음」이다.

**출력 형식** — 이상 유무와 무관하게 **항상 첫 줄에 쓴다.**

- 없을 때 (대부분의 날):
  ```
  📌 오늘(YYYY-MM-DD) 반드시 발행할 글: 없음 — 오늘자 초안 {N}편 전부 Track L(마감 없음)
  ```
- 있을 때:
  ```
  📌 오늘(YYYY-MM-DD) 반드시 발행할 글: {N}편
  🔴 Blog #{N} — {제목} ({사이트}, WP {id})
     마감 근거: {날짜가 박힌 이벤트 / 오늘이 검색 피크 / 슬롯이 창을 넘김 / Writer 즉시 지정} — {한 줄}
     발행 가능 상태: 대표이미지 {O/X} · 본문 이미지 {N}장 {정상/결손} · 플레이스홀더 {0건/잔존}
     실행: wp-admin > 글 > {글 제목} > 공개 상태를 '비공개'로 전환 후 즉시 발행
  ```

⚠️ **「오늘 마감」 글이 있는데 이미지가 결손이면(항목 12), 그 사실을 이 0행에 반드시 같이 적는다.** 마감을 알리면서 발행 불가 상태를 안 알리면 사용자가 wp-admin에 들어가서야 알게 된다.
⚠️ **이 루틴은 발행하지 않는다.** 사용자 지침상 모든 게시는 사용자 확인 후 비공개 우선이므로, 0행은 **판정과 절차만** 제시한다. → 언제나 `사용자필요`

### 1행 이하 — 13개 항목 점검 결과

- **모든 항목 정상이면 0행 다음에 딱 한 줄**: `✅ YYYY-MM-DD 이상 없음 (13개 항목 통과)` — 설명·요약·표 금지
- **이상이 있으면** 해당 항목만 아래 5줄 형식으로 쓴다. 진단만 하고 끝내지 말 것 — **해결책이 없는 알람은 무효**다:
  1. `[등급] 무엇이` — 🔴/🟡/⚪ + 증상 한 줄
  2. `언제부터` — 추정 시점과 근거
  3. `영향` — 방치하면 무슨 일이 생기는가
  4. `해결책` — 아래 항목별 플레이북에서 가져온 **구체적 절차**. "점검 필요" 같은 말 금지, 어느 화면의 어느 버튼인지 쓸 것
  5. `실행 주체` — `루틴가능`(다음 실행에서 자동 처리 가능) / `사용자필요`(사람이 눌러야 함) 중 하나
- **이상 항목이 있을 때만** KoreaPlug 리포트 페이지(398bfe4a-2ae1-8150-93c4-fb98977c64bf)의 `## 리포트 (최신순)` 바로 아래에 `> 🚨 YYYY-MM-DD 일간 알람: (증상) → (해결책 요약)` 인용문 한 줄을 추가한다. **정상인 날은 Notion에 아무것도 쓰지 않는다** — 정상 로그로 도배되면 주간 루틴이 기준치를 못 찾는다
- 브라우저·로그인 문제로 확인 못 한 항목은 정상 처리하지 말고 `⚠️ 미확인`으로 보고한다

### 글 식별 표기 규칙 (v3 신설 — 반드시 지킬 것)

> **왜 필요한가**: WordPress post ID(3434, 1138 …)는 사용자가 평소에 보지 않는 내부 숫자다. 사용자가 실제로 추적하는 번호는 Notion 서브페이지 제목의 `Blog #N`이다. 내부 ID만 적으면 사용자가 어느 글인지 알아내는 데 시간이 든다.

- 글을 언급할 때는 항상 **`Blog #N`을 앞에 쓰고, WordPress ID는 괄호 보조로만** 적는다
  - ✅ `Blog #136 — Incheon Airport Chuseok 2026 (WP 3434, 초안)`
  - ❌ `post 3434` / ❌ `ID 3434 Incheon Airport…`
- `Blog #N`은 Notion 검색으로 찾는다: `notion-search`에 그 글의 **제목 핵심 단어**를 넣으면 `Blog #N — 제목` 형태의 서브페이지가 나온다. 제목 전체를 넣지 말고 고유 단어 2~4개만 쓸 것(예: `Incheon Airport Chuseok 2026`)
- Notion 조회가 실패해 번호를 못 찾으면 `Blog #미확인(WP {id}) — {제목}` 으로 적고, 그 사실을 해당 항목 끝에 한 줄로 남긴다. 번호가 없다고 항목 자체를 건너뛰지는 않는다
- **번호 대조표를 매 실행마다 새로 만들지 말 것.** 알람에 등장한 글만 조회한다(정상 항목의 글은 조회하지 않는다 — Notion 호출 낭비다)

### 데이터 취득 우선순위 (v3 신설)

같은 정보를 얻는 경로가 여러 개면 **아래 순서대로** 시도한다. 아래로 갈수록 느리고 실패 확률이 높다.

1. **WordPress REST + 앱 비밀번호** — 항목 7·8·10·12는 전부 이 경로로 끝난다. Chrome 없이도 된다
2. **단순 fetch(curl)** — 항목 1·2·5의 XML/HTML 확인
3. **Chrome 확장** — 항목 3·4·5(GSC 상태)·6. GSC는 REST가 없으므로 대체 불가
4. **Notion MCP** — 항목 11의 리포트 조회, 글 번호 조회, 알람 기록

⚠️ **`pw.txt` 파싱 주의 (2026-08-17 실측 오류)**: 앱 비밀번호에는 **공백이 포함**되어 있다. bash에서 `set -a; . pw.txt` 로 읽으면 공백 이후가 잘려 인증이 조용히 실패하고, WordPress는 401이 아니라 **`rest_invalid_param: status`** 를 반환한다(비인증 사용자에게는 `status=draft`가 허용되지 않기 때문). 이 메시지를 파라미터 오류로 착각하지 말 것 — **인증 실패 신호다.** 반드시 아래처럼 라인 단위로 파싱한다.

```python
env = {}
for line in open('/sessions/*/mnt/Claude/pw.txt', encoding='utf-8-sig'):  # BOM 있음
    line = line.strip()
    if not line or line.startswith('#') or '=' not in line: continue
    k, v = line.split('=', 1); env[k.strip()] = v.strip()
# 인증: Authorization: Basic base64(USER:APP_PASSWORD)
# 인증 성공 판정: /wp-json/wp/v2/users?context=edit 응답에 roles·email이 포함되면 성공.
#                roles가 없으면 비인증 응답이다.
```

## 등급 기준 (이슈를 잘 고르기 위한 규칙)

- 🔴 **즉시** — 검색 노출이 지금 사라지고 있거나 사이트가 죽은 상태. 항목 1·2·4, 그리고 6번의 급감
- 🟡 **이번 주** — 방치하면 커지지만 오늘 당장은 아님. 항목 3·5·7·9·11, 그리고 10의 사전 경고
- 🔴 **즉시 (항목 10 한정)** — 선점 창 마감을 이미 넘겼거나 다음 슬롯에서 넘기는 글. 하루 늦으면 그 해에는 못 쓴다
- 🔴 **즉시 (항목 12 한정)** — 대표 이미지가 없거나 이미지 URL이 죽은 글이 **예약 큐에 들어가 있는** 경우. 그대로 발행되면 이미지 없는 글이 공개된다
- ⚪ **기록만** — 알아두면 좋지만 조치 불필요. 항목 8의 경미한 변화, 항목 12의 라이브러리 alt 미기재 등

**오탐 방지 (중요 — 노이즈를 내면 이 루틴은 무시당한다)**

- 사이트 응답 실패는 **30초 간격으로 2회 재시도** 후에도 실패할 때만 알람한다. 일시적 5xx는 흔하다
- MCP 커넥터(Notion 등)가 503을 반환하면 **45초 이상 기다린 뒤 최소 1회 재시도**한다. 즉시 재시도는 거의 같은 오류를 낸다
- 이미 알려진 정상 상태는 알람하지 않는다: 0and1life의 `/feed` RSS 사이트맵 `가져올 수 없음`(불필요 등록), koreaplug의 `/forums/*` 404(플러그인 제거 완료)
- 색인 수는 ±10% 이내 변동을 알람하지 않는다. 구글 집계는 원래 흔들린다
- 같은 이상을 3일 연속 보고했는데 조치가 없으면, 4일째부터는 **등급을 한 단계 올리고** "3일째 미조치"를 명시한다

---

## 점검 항목 13개 + 항목별 해결책 플레이북

### 1. 사이트 응답 🔴

**확인**: https://koreaplug.com/ 과 https://0and1life.com/ 이 200을 반환하는가 (실패 시 30초 후 2회 재시도)

**해결책**: ① Cloudways 대시보드에서 서버·애플리케이션 상태 확인 ② 최근 24시간 내 플러그인/테마 업데이트가 있었는지 wp-admin > 알림판 > 업데이트에서 확인하고, 있으면 그 플러그인을 비활성화해 복구 시도 ③ 복구 안 되면 UpdraftPlus 백업에서 직전 시점 복원. → `사용자필요`

### 2. 홈페이지 noindex 사고 🔴

**확인**: 두 홈페이지 HTML의 `<meta name="robots">` content에 `noindex`가 있는가

**해결책**: 원인은 거의 항상 둘 중 하나다. ① wp-admin > 설정 > 읽기 > "검색엔진이 사이트를 색인하지 않도록 요청" 체크박스가 켜졌는지 확인하고 해제 ② Rank Math > 제목 및 메타 > 홈페이지 > Robots Meta에서 noindex가 걸렸는지 확인. 해제 후 GSC URL 검사로 홈 재색인 요청. → `사용자필요`

### 3. GSC 소유권 인증 🟡

**확인**:
- https://search.google.com/search-console/ownership?resource_id=https%3A%2F%2Fkoreaplug.com%2F
- https://search.google.com/search-console/ownership?resource_id=https%3A%2F%2F0and1life.com%2F

⚠️ **0and1Life는 2026-07-11에 실제로 인증이 풀린 이력이 있고, 2026-08-17 확인 결과 여전히 `Google 애널리틱스` 단일 방법에만 의존한다**(HTML 파일·HTML 태그·태그 관리자·DNS 전부 미설정). gtag.js가 빠지거나 GA 속성이 바뀌면 재발한다. KoreaPlug도 `HTML 태그` 1개 단일 의존이다.

**해결책**: 풀려 있으면 그 화면의 [소유권 확인] 버튼을 눌러 재검증한다(설정 변경 아님 — 이 루틴에 허용된 유일한 쓰기 동작). 재검증 후에는 **반드시 이중화를 권고**한다: 같은 화면 "추가 확인 방법" > HTML 태그 > 메타태그 복사 → wp-admin > Rank Math > 일반 설정 > 웹마스터 도구 > Google Search Console 칸에 붙여넣기 > 저장 → GSC로 돌아와 [확인]. 5분이면 끝나고 재발이 막힌다. → 재검증은 `루틴가능`, 이중화는 `사용자필요`

정상이고 인증 방법이 1개뿐인 상태가 이어지면 **매월 1일에만** 이중화 권고 한 줄을 덧붙인다(매일 반복 금지).

### 4. 수동 조치 / 보안 문제 🔴

**확인**: 두 사이트 각각
- https://search.google.com/search-console/manual-actions?resource_id=<RESOURCE_ID>
- https://search.google.com/search-console/security-issues?resource_id=<RESOURCE_ID>

(RESOURCE_ID: koreaplug = `https%3A%2F%2Fkoreaplug.com%2F`, 0and1life = `https%3A%2F%2F0and1life.com%2F`)

"발견된 문제 없음"이 아니면 최우선 알람.

**해결책**: **절대 자체 판단으로 조치하거나 재검토를 요청하지 말 것.** 사유 텍스트와 영향 범위(사이트 전체 / 일부 URL)를 그대로 옮겨 적고, 해당 URL 목록을 뽑아 사용자에게 에스컬레이션한다. 재검토 요청은 원인을 고친 뒤 사람이 판단해 넣어야 하며, 성급한 요청은 심사 이력에 남아 불리하다. → `사용자필요`

### 5. 사이트맵 상태 🟡

**확인**: https://search.google.com/search-console/sitemaps?resource_id=<RESOURCE_ID> 에서 `/post-sitemap.xml`·`/sitemap_index.xml` 상태가 `성공`인가 (0and1life `/feed`는 제외)

보조로 **XML 자체가 살아 있는지 curl로 먼저 확인**한다. XML은 200인데 GSC만 실패면 GSC 측 재제출 문제이고, XML부터 죽었으면 Rank Math 문제다 — 이 구분이 해결책을 갈라놓는다.

**해결책**: ① 브라우저로 해당 사이트맵 URL을 직접 열어 XML이 정상 출력되는지 확인 ② 깨졌으면 wp-admin > Rank Math > 사이트맵 설정에서 모듈이 켜져 있는지 확인하고, 껐다 켜서 재생성 ③ 정상 출력되는데도 GSC가 `가져올 수 없음`이면 GSC Sitemaps 화면에서 해당 항목 삭제 후 같은 URL로 재제출. → `사용자필요`

### 6. 색인 급변 🔴(급감) / ⚪(증가·보합)

**확인**: https://search.google.com/search-console/index?resource_id=<RESOURCE_ID> 의 `색인 생성됨` 숫자를, 직전 주간 리포트(Notion 최신 `### YYYY-MM-DD 주간 리포트`)의 색인 수와 비교. **10% 이상 감소**만 알람.

**참고치 (2026-08-17 확인)**: koreaplug 117 (미색인 192) / 0and1life 50 (미색인 63)
**이전 참고치 (2026-08-08)**: koreaplug 113 (미색인 172) / 0and1life 42 (미색인 61)

⚠️ **미색인 건수 증가는 알람하지 않는다.** GSC의 `크롤링됨 - 현재 색인이 생성되지 않음` 목록에는 **이미 삭제한 글과 이미 301을 건 URL이 몇 주간 잔상으로 남는다**(2026-08-08 실측: 0and1life 17건 중 6건이 07-17에 병합 완료된 글). 이 항목은 `색인 생성됨`의 **10% 이상 감소**만 본다. 미색인 목록의 분류·판정은 주간 루틴 STEP 2-B의 일이다.

**해결책**: 급감 원인은 순서대로 확인한다. ① 항목 2(noindex)와 항목 4(수동조치)를 먼저 재확인 — 이 둘이면 그쪽 플레이북을 따른다 ② 아니면 robots.txt를 열어(https://도메인/robots.txt) `Disallow: /` 가 들어갔는지 확인 ③ 그것도 아니면 색인 제외 사유 표에서 어느 사유가 늘었는지 짚어 보고한다(사유별 건수 비교). → 원인 파악은 `루틴가능`, 수정은 `사용자필요`

### 7. 어제 발행 루틴이 실제로 돌았는가 🟡

**확인 (v3 — REST 우선)**: WordPress REST로 두 사이트의 상태별 글 목록을 한 번에 읽는다. Chrome 없이 끝난다.

```
GET /wp-json/wp/v2/posts?status={draft|future|publish|pending|trash}&per_page=30
    &orderby=date&order=desc&_fields=id,date,modified,status,title
```

⚠️ **판정 기준일은 "어제 날짜"가 아니라 "어제 실행분"이다.** Writer 루틴은 자정 근처에 돌아 **날짜가 오늘 새벽(00:00~06:00)으로 찍히는 경우가 많다**(2026-08-17 실측: 0and1life 00:21·00:31, koreaplug 01:52·02:03·05:21 — 전부 08-16 실행분). 따라서 **어제 00:00 ~ 오늘 실행 시각까지**를 창으로 잡고, 그 안에 신규 초안이 있으면 루틴은 돈 것이다.

⚠️ **발행일(예약 발행일)과 생성일을 혼동하지 말 것.** 예약된 글의 날짜 칸은 미래 발행일이라 어제 루틴이 돌았는지를 알려주지 않는다.

⛔ **"배포 안 됨"을 보고하기 전에 3단계를 모두 확인할 것 (2026-08-08 오탐 후 신설)**

> **오탐 사고**: 08-08 알람은 "Blog #128(chuseok closures)이 Notion에만 있고 WordPress에 배포 안 됨 — **초안·예약·휴지통 전부 없음**"으로 보고했다. 같은 날 wp-admin을 직접 열어보니 **임시글이 06:53 수정 이력과 함께 멀쩡히 존재**했다. 배포 파이프라인은 고장나지 않았고 단지 예약 큐에 안 들어갔을 뿐인데, 사용자에게 "재배포하라"는 잘못된 조치가 전달됐다.

판정 순서를 이렇게 고정한다:

1. **상태별 건수부터 센다** — REST의 `draft`/`future`/`pending` 응답 길이. 임시글·예약됨 건수가 어제보다 늘었으면 루틴은 돈 것이다
2. **제목 문자열로 직접 찾는다** — Notion 산출물의 제목 핵심 단어(예: `Chuseok`)로 `?search=<키워드>&status=draft` 를 걸어 확인한다. 날짜 필터에만 의존하지 말 것
3. **상태를 전부 순회한다** — `draft`·`future`·`publish`·`pending`·`trash` 5개 모두 조회했을 때만 "존재하지 않음"으로 판정한다

세 단계 중 하나라도 글을 찾으면 **"배포 실패"가 아니라 "예약 큐 미등록"**이며, 등급은 🔴이 아니라 🟡이고 해결책은 "재배포"가 아니라 **"임시글 → 발행일 지정"**이다. 날짜 지정·공개 전환은 사용자 지침상 `사용자필요`다.

**해결책**: **2026-08-01에 두 사이트 모두 배포 실패, 2026-08-07에 두 사이트 모두 하루 지연된 전례가 있다.** 원인을 이 순서로 확인한다:

① **폴더 마운트 여부** — 배포 루틴은 `pw.txt`에서 WordPress 앱 비밀번호를 읽는다. 먼저 bash로 접근을 시도한다:
  ```
  ls /sessions/*/mnt/Claude/pw.txt
  ```
  ✅ **2026-08-17 재확인: `pw.txt`는 `C:\Users\win\Documents\Claude\pw.txt` 에 정상 존재하며 마운트된다.** 키 4종(`KOREAPLUG_WP_USER`/`KOREAPLUG_WP_APP_PASSWORD`/`ONEANDZERO_WP_USER`/`ONEANDZERO_WP_APP_PASSWORD`)과 `PEXELS_API_KEY`가 들어 있다.
  ⚠️ **舊 진단 "pw.txt를 마운트 가능한 경로로 복사"는 이미 완료된 조치이며, 다시 권고하지 말 것.** 舊 경로 `C:\Users\win\.claude\` 는 더 이상 관련 없다.
  → 위 `ls`가 실패하면 원인은 파일이 아니라 **그 실행 세션에 폴더가 마운트되지 않은 것**이다. 무인 예약 실행에서는 `request_cowork_directory` 승인 창을 띄울 사람이 없어 여기서 멈춘다. 이때는 `request_cowork_directory`(path=`C:\Users\win\Documents\Claude`)를 1회만 시도하고, 실패하면 ②로 넘어간다.

② **자격증명 유효성** — REST가 `rest_invalid_param: status` 를 반환하면 **파라미터 문제가 아니라 인증 실패**다. 먼저 위 「데이터 취득 우선순위」의 파싱 코드로 공백 포함 값이 온전히 읽혔는지 확인한다. 파싱이 정상인데도 실패하면 앱 비밀번호가 폐기·재발급된 것이므로, wp-admin > 사용자 > 프로필 > 응용 프로그램 비밀번호에서 새로 발급해 `pw.txt`를 갱신한다.

③ **Chrome 확장 로그인 상태** — REST가 아예 막힌 경우의 최후 폴백 경로다. 끊겼으면 Chrome 사이드패널에서 재로그인.

셋 다 정상인데 글이 없으면 해당 writer 루틴의 마지막 실행 로그를 확인해 보고한다. → `사용자필요`

**보조 확인 (알람 조건 아님 — 발견 시에만 🔴로 보고)**: 예약 작업 본문이나 `C:\Users\win\Documents\Claude\Scheduled\*/SKILL.md` 에 **평문 앱 비밀번호가 적혀 있으면 즉시 보고**한다. 자격증명은 `pw.txt` 한 곳에만 있어야 한다. (2026-08-08에 구버전 사본 2개에서 발견되어 제거함. 노출됐던 비밀번호는 재발급 전까지 유효하다.)

### 8. 신규 사용자 / 스팸 흔적 🔴(신규 관리자) / 🟡(신규 일반 사용자)

**확인 (v3 — REST 우선)**: `GET /wp-json/wp/v2/users?per_page=20&context=edit` — `context=edit` 를 붙여야 `email`·`roles`가 나온다. 정상은 **사용자 1명(본인, `leejcfo@gmail.com` / `administrator` + `bbp_keymaster`)** 이며 회원가입 차단(users_can_register=false) 상태다.
(REST가 막히면 https://koreaplug.com/wp-admin/users.php · https://0and1life.com/wp-admin/users.php)

**해결책**: ① 새 사용자의 권한을 확인한다 — 관리자면 침해 가능성이 높다 ② wp-admin > 설정 > 일반 > "누구나 등록할 수 있음" 체크가 풀려 있는지 확인하고, 켜져 있으면 해제 ③ 최근 게시글·페이지에 낯선 항목이 생겼는지 확인 ④ 관리자 계정이 추가됐다면 비밀번호 변경과 모든 세션 로그아웃을 권고한다. **사용자 삭제는 직접 하지 않고 사용자에게 맡긴다.** → `사용자필요`

(배경: koreaplug에 과거 bbPress 포럼을 통한 음란물·의약품 스팸 게시글 5건이 생성된 이력이 있다. 현재는 플러그인 제거로 전부 404다.)

### 9. 트랙 S(숏테일) 규칙 준수 🟡 — 2026-08-06 신설

> **왜 이게 일간 점검인가**: 성과가 아니라 **규칙 준수 여부의 이진 판정**이기 때문이다. "이 글이 잘 됐나"가 아니라 "홀수일 산출물이 규칙대로 숏테일인가"만 본다.
> **배경**: 2026-08-05~08-17 홀수일 산출물 7건 중 6건이 3~5단어 롱테일이었고 7건 전부 시의성이 없었다 — 트랙 S가 사실상 사라진 상태였다. 지침서 v10.24 / 루틴 v6로 수정했으며, **그 수정이 실제로 작동하는지 확인하는 것이 이 항목의 목적이다.**

**확인** — 어제가 **홀수일**이었을 때만 수행한다 (짝수일이면 이 항목 건너뛰고 통과 처리):

두 사이트 글 목록에서 어제 실행분의 **포커스 키워드**를 읽고 아래 3개를 판정한다.

- ⓐ **단어 수**: KoreaPlug = 2~4단어 하이브리드(영어) / 0and1Life = 2~4단어 하이브리드(한국어 어절)
- ⓑ **헤드 자격**: 헤드가 이슈·시즌·제도 중 하나인가? 에버그린 문화 용어·에버그린 돈 주제가 헤드면 불통과
- ⓒ **Track 기록 정직성**: 규칙에서 벗어난 키워드를 채택했으면 Notion 기본 정보 표 `Track` 행에 `L`로 기록됐는가? (홀수일이라는 이유로 `S`로 적혀 있으면 불통과)

**판정**: ⓐⓑ 불통과 + ⓒ도 `S`로 기록 → 🟡 알람. ⓐⓑ 불통과지만 ⓒ가 `L`로 정직하게 기록 → **알람하지 않는다**(규칙대로 대체를 선언한 정상 동작이다. 다만 연속 횟수만 세어 3회 연속이면 🟡로 보고).

**해결책**:
> ⚠️ **두 사이트의 버전 체계가 다르다 — 대조표를 먼저 볼 것** (2026-08-08 이식 완료)
>
> | | 루틴 | 지침서 | 선점 창 | 단위 |
> |---|---|---|---|---|
> | KoreaPlug | `v6` | `KoreaPlug-Writer.md` v10.24 | D-60 ~ D-21 | 영어 단어 |
> | 0and1Life | `v4` | `0and1Life-Writer.md` v1.35 | **D-45 ~ D-14** | 한국어 어절 |
>
> 0and1Life의 창이 늦은 것은 오류가 아니라 네이버 색인 속도를 반영한 의도된 차이다. 0and1Life에 KoreaPlug의 D-60 기준을 적용해 판정하지 말 것.

① 해당 글의 Notion 서브페이지 `Track`·`Head Type` 행과 STEP 8 리포트를 열어 **어느 엔진에서 끊겼는지** 확인한다 — 엔진 C 미실행이면 규칙 위반이다 (KoreaPlug 루틴 v6 / 0and1Life 루틴 v4 공통)
② 엔진 C 미실행이 반복되면 Cowork 예약 작업의 루틴 본문이 **최신 버전으로 교체되지 않았을 가능성**이 가장 크다. 루틴 본문 상단의 버전 표기(KoreaPlug `v6` / 0and1Life `v4`)와 **STEP 0-②(시즌 창 판정)** 존재 여부를 확인한다
③ 루틴은 최신인데 지침서를 구버전으로 읽고 있으면 GitHub `main` 상단의 버전 표기를 확인한다 — `KoreaPlug-Writer.md`에 `v10.24`, `0and1Life-Writer.md`에 `v1.35` (raw CDN 캐시로 최대 5분 지연)
   - 0and1Life 판정 시 추가 확인: 기본 정보 표에 `Head Type` 행이 아예 없으면 지침서가 v1.35 미만이다 (이 행은 v1.35에서 신설됐다)
④ 셋 다 정상인데 계속 롱테일이 나오면 **후보 채점표(0-6 선점성 축)가 적용되고 있는지**를 STEP 8 리포트의 `🧮 후보 채점` 줄에서 확인한다
→ 원인 파악은 `루틴가능`, 루틴 본문 교체는 `사용자필요`

**시즌 커버리지 보조 확인 (같은 항목 내, 알람 조건 아님 — 기록만)**: 지침서 0-4 캘린더에서 오늘 열려 있는 선점 창과, 창이 닫히기까지 남은 실행 회차를 한 줄로 적는다. 창이 열려 있는데 3회 연속 시즌 후보가 채택되지 않으면 🟡로 올린다.

### 10. 발행 순서 — 당겨야 할 글 🟡 — 2026-08-08 신설

> **왜 이게 일간 점검인가**: 시즌·이슈 글은 **선점 창이 닫히면 그 해에는 다시 못 쓴다.** 발행이 2일에 1편인데 writer는 매일 1편을 만들므로 대기열이 계속 쌓이고, 큐는 만들어진 순서대로 소비된다 — 즉 **급한 글이 안 급한 글 뒤에 줄 서는 일이 구조적으로 발생한다.** 이건 성과 지표가 아니라 "마감을 놓치기 전인가"라는 이진 판정이므로 일간이 맞다.

**확인 (v3 — REST 우선)**:
- 예약: `GET /wp-json/wp/v2/posts?status=future&per_page=30&orderby=date&order=asc` → 예약일 + 포커스 키워드
- 초안: `GET /wp-json/wp/v2/posts?status=draft&per_page=30&orderby=modified&order=asc` → 제목 + 포커스 키워드

**슬롯 규칙**: 발행은 2일에 1편, 예약은 홀수일 09:00 고정. 다음 빈 슬롯 = 마지막 예약일 + 2일.
⚠️ **트랙 S/L은 writer 실행일(홀수/짝수) 기준이지 발행일 기준이 아니다.** 발행 슬롯이 전부 홀수일인 것과 혼동하지 말 것.

**당김 판정** — 아래 둘만 본다:

**ⓐ 선점 창 마감** (주 기준)
1. 각 예약글·초안의 헤드가 시즌·이슈·제도인지 판정한다 (에버그린이면 이 항목에서 제외 — 마감이 없다)
2. 지침서 `0-4 캘린더`에서 그 이벤트의 **창 마감일**을 읽는다 (KoreaPlug D-21 / 0and1Life D-14 기준, 구간형은 표에 적힌 창 종료일). **하위 이벤트는 자기 D-day**를 쓴다 (예: 추석 당일 09-25이지만 공항 혼잡은 이동 피크 09-24 기준)
3. 그 글의 **예약일 > 창 마감일**이거나, **초안인데 다음 빈 슬롯이 창 마감일을 넘으면** → 🔴 당김 대상
4. 창 마감까지 **남은 홀수일 슬롯 수 ≤ 창 안의 시즌 글 수**면 → 🟡 사전 경고 (아직 안 늦었지만 여유가 없다)

**ⓑ 슬롯 정합성** (보조)
- 시즌 글이 큐 뒤쪽에 있고 앞쪽 슬롯이 전부 에버그린이면, **마감 있는 글과 없는 글의 자리를 맞바꾼다**. 에버그린은 언제 나가도 손해가 없으므로 항상 밀리는 쪽이다
- 같은 이벤트 글이 연속 슬롯에 몰려 있으면 하나를 뒤로 보낸다 (같은 날 검색층에 중복 노출). **당길 슬롯을 고를 때 이 규칙을 함께 적용한다** — 마감 안쪽이면서 같은 이벤트와 연속되지 않는 슬롯이 최선이고, 둘을 동시에 만족할 수 없으면 마감 준수를 우선한다

**출력** — 당길 글이 **있을 때만** 쓴다. 없으면 항목 10은 통과 처리하고 아무것도 적지 않는다.

```
📅 발행 순서 조정 필요 — {사이트명}
🔴 Blog #{N} — {글 제목} (WP {id}, {포커스 키워드})
   현재: {예약일 또는 '초안·미예약'} / 창 마감: {날짜} (D-{n}, 기준 이벤트: {무엇})
   사유: {한 줄}
   교체안: Blog #{A}를 {날짜} 슬롯으로 당기고, 그 자리의 Blog #{B}를 {날짜}로 밀 것
   선택 근거: {왜 그 슬롯인가 — 마감·중복 회피 관점 한 줄}
   실행: wp-admin > 글 > 빠른 편집 > 날짜 변경 (2건)
```

**해결책**: 위 교체안을 그대로 적어 준다. **루틴은 예약일을 직접 바꾸지 않는다** — 이 루틴의 읽기 전용 원칙을 유지하고, 자동 재배치가 의도치 않은 발행을 만드는 것을 막기 위해서다. → `사용자필요`

⚠️ **교체안은 반드시 짝으로 낸다.** 슬롯 수가 고정이므로 하나를 당기면 반드시 하나가 밀린다. "당겨라"만 적고 무엇이 밀리는지 안 적으면 사용자가 큐를 다시 계산해야 한다.
⚠️ 같은 글을 3일 연속 당김 대상으로 보고했는데 조치가 없으면 4일째부터 🔴로 올리고 "3일째 미조치 — 창 마감 D-{n}"을 명시한다.

### 11. 문체 지문 준수 🟡 — 2026-08-09 신설

> **왜 이게 일간 점검인가**: 문체는 조용히 무너진다. 색인 거부나 SEO 점수처럼 알람이 뜨지 않고, 한 편씩 `~습니다` 일변도로 돌아가다가 어느 날 보면 전부 AI 문체다. **매일 숫자로 봐야 되돌아가는 것을 잡을 수 있다.** 기존 항목 9(트랙 규칙)와 같은 성격의 규칙 준수 점검이다.

**확인**: 어제 Writer 리포트(현황표 하단 로그)의 `🖋️ 문체 지문` 행을 읽는다. 루틴 v4.1 STEP 8이 매일 아래 형식으로 출력한다.

```
🖋️ 문체 지문(STEP 5-3): {적용 완료 또는 조회 실패} / 평균 {X.X}어절 / 5어절 이하 {XX}% / `~습니다` {XX}% / 금지어 {N}건
```

**판정** — 기준은 `0and1Life-Writer.md` **Phase 5-6**:

| 항목 | ⚪ 정상 | 🟡 경고 | 🔴 |
|---|---|---|---|
| 금지어 | 0건 | 1~2건 | **3건 이상** |
| `~습니다` 비율 | 20% 이하 | 21~30% | **31% 이상 (일변도 회귀)** |
| 평균 문장 | 6~9어절 | 5~6 또는 9~11 | **5어절 미만(과분할) 또는 11 초과** |
| 5어절 이하 | 45% 이상 | 35~45% | 35% 미만 |
| 지문 참조 | 성공 | — | **`참조 불가` 2일 연속** |

⚠️ **평균이 5어절 아래로 내려가면 🔴다.** 짧을수록 좋다는 뜻이 아니다 — 과분할이며, 이때 표준편차가 함께 떨어져 문장 길이가 균일해진다(가이드 Phase 5-6 5-B, #80 실측). 「짧은 문장 비율만 맞추고 있다」는 신호다.

**Notion 장애 시 폴백 (v3 신설)**: Notion MCP가 45초 대기 후 재시도에도 실패하면 항목 11은 `⚠️ 미확인`으로 보고한다. 단, 그대로 끝내지 말고 **원고 본문을 REST로 받아 직접 실측한 보조 수치를 함께 낸다**. 측정 대상은 `content.raw`에서 태그·엔티티를 제거한 본문이며, 아래 3개만 낸다: 평균 어절 / 표준편차 / 5어절 이하 비율 / `~습니다` 종결 비율. **금지어 건수는 목록이 지침서에 있으므로 지침서를 읽지 못한 상태에서는 측정하지 않는다.**
⚠️ 이 실측값은 **루틴 리포트 값이 아니므로 단독으로 🟡 알람을 내지 않는다.** 문장 분할 방식이 루틴과 다를 수 있어 5~10%p 오차가 난다. 경계 구간(예: 5어절 이하 40%)이면 "재확인 필요"로만 적는다. 🔴 구간(평균 5어절 미만, `~습니다` 31% 이상)에 명백히 들어갈 때만 알람으로 올린다.

**출력** — 🟡 이상일 때만 쓴다. 전부 ⚪면 항목 11은 통과 처리하고 아무것도 적지 않는다.

```
🖋️ 문체 지문 이탈 — {사이트명} Blog #{N} (WP {id})
   {항목}: {실측값} (기준 {목표값})
   원인 추정: {한 줄}
   해결: {조치}
```

**해결책 판정**:
- 금지어 3건 이상 / `~습니다` 31% 이상 → **STEP 5-3이 실행되지 않았을 가능성**. 리포트에 `문체 지문` 행 자체가 없으면 루틴이 이 단계를 건너뛴 것이므로 🔴로 올린다 → `사용자필요`(루틴 점검)
- `참조 불가` 2일 연속 → 가이드 조회 자체가 실패하는 상태다. `https://raw.githubusercontent.com/leejc0404/blog/main/0and1Life-Writer.md` 응답을 확인하고, 200인데도 계속 실패하면 루틴 STEP 1 점검 → `사용자필요`
- 평균 5어절 미만 → 가이드 Phase 5-6 5-B 교정 순서 미준수. 다음 회차 리포트에 그 사실을 명시하도록 요청 → `루틴가능`

⚠️ **이 루틴은 원고를 직접 고치지 않는다.** 읽기 전용 원칙을 유지하고, 측정값과 조치만 제시한다.

### 12. 이미지 파이프라인 정상 작동 🔴(예약 큐에 이미지 결손) / 🟡(초안 결손) / ⚪(라이브러리 alt 미기재) — v3 신설

> **왜 이게 일간 점검인가**: 이미지 루틴(`Schd_KoreaPlug-Image.md` / `Schd_0and1Life-Image.md`)은 Writer와 **별개의 실행**이다. 원고는 멀쩡한데 이미지만 빠지는 조합이 성립하고, 이 경우 **글 자체는 정상으로 보이므로 항목 7이 통과 처리한다.** 그리고 이미지 없는 글은 발행되고 나서야 눈에 띈다 — 발행 후에 알면 이미 공개된 상태다. 켜졌나 꺼졌나의 이진 판정이므로 일간이 맞다.

**확인** — 어제 실행분 초안 + **다음 2개 예약 슬롯의 글**을 대상으로 한다 (전체 큐를 매일 훑지 않는다):

1. **업로드 발생 여부** — `GET /wp-json/wp/v2/media?per_page=40&orderby=date&order=desc&_fields=id,date,source_url,alt_text,media_details,post`
   어제 00:00 이후 업로드가 **글당 최소 4건**(hero + 본문 3) 있는가. 파일명은 `{사이트}-{슬러그}-{hero|1|2|3}.webp` 규칙을 따른다
2. **대표 이미지 지정** — 각 글의 `featured_media`가 0이 아닌가. **0이면 그 글은 썸네일 없이 발행된다** — 가장 잘 나는 결손이다
3. **본문 이미지 생존** — `content.raw`의 모든 `<img src>`에 HEAD 요청을 보내 200인가. 404·403이면 미디어가 삭제됐거나 URL이 잘못 박혔다
4. **alt 텍스트** — 본문 `<img>` 태그에 `alt`가 채워져 있는가 (SEO·접근성 필수)
5. **규격** — `media_details`의 width×height가 사이트 규격인가. **KoreaPlug 1226×768 / 0and1Life 1100×689** (evidence 스크린샷은 원본 크기 그대로이므로 이 검사에서 제외)

> ⚠️ **규격 숫자를 이 문서만 보고 판정하지 말 것 (2026-08-20 오탐 후 신설).**
> **08-20 오탐 사고**: 舊 기준 984×409 / 984×469로 재서, 정상 산출물인 KoreaPlug 1226×768 3건과 0and1Life 1100×689 1건을 "규격 이탈 🟡"로 보고했다. 실제로는 이미지 루틴이 의도적으로 바꾼 값이었고, 고장난 것은 이 점검 문서 쪽이었다.
> 두 값 모두 근거가 문서에 적혀 있다. **KoreaPlug 1226×768** — Flow 전환(v4.0)에서 워터마크(✦, 상대좌표 0.925W·0.875H)를 `cropRight=150`으로 잘라낸 결과이며, `Schd_KoreaPlug-Image.md`에 **"원본 폭 1226은 줄이지 않는다"**(dpr 2 기기는 788×2=1576px 필요)고 명시돼 있다. **0and1Life 1100×689** — `Schd_0and1Life-Image.md` v5.4(2026-08-19)에서 신설한 `TARGET_W = 1100` 다운스케일 결과다. 1024 미만으로 줄이면 WordPress가 `large`(1024) 사본을 만들지 않아 2배 DPI 화면이 뭉개지기 때문에 1100이 하한이다.
> **따라서 판정 순서는 이렇다**: ① 실측값이 위 숫자와 같으면 통과 ② 다르면 **알람하기 전에 해당 이미지 루틴 문서(`Schd_KoreaPlug-Image.md` / `Schd_0and1Life-Image.md`)의 현재 규격 표기를 읽는다** ③ 루틴 문서와 실측이 일치하면 **루틴이 갱신된 것이므로 알람하지 않고, 대신 이 문서의 숫자를 갱신하라고 한 줄 남긴다** ④ 루틴 문서와도 다를 때만 🟡 알람이다.
> 구버전 산출물이 큐에 섞여 있는 것은 정상이다 — **KoreaPlug 984×409**(Flow 이전)와 **0and1Life 984×469**(v5.4 이전)는 과거 정상 산출물이므로 알람하지 않는다.

**참고치 (2026-08-20 실측 정상 상태)**: koreaplug 초안 1건(WP 3492)에 미디어 4건 1226×768, 0and1life 초안 1건(WP 1248)에 미디어 4건 1100×689. 양쪽 모두 대표 이미지 지정 완료, 본문 이미지 전부 200, 본문 alt 누락 0건, `[FEATURED_IMAGE_URL]` 플레이스홀더 잔존 0건.
**이전 참고치 (2026-08-17)**: koreaplug 3글에 미디어 18건(hero·본문 12 + evidence 6), 0and1life 2글에 11건(8 + 3).

**판정**:
- 대표 이미지 없음 또는 본문 이미지 404 → 그 글이 **`future`(예약)** 상태면 🔴, **`draft`** 상태면 🟡 (초안은 발행 전에 고칠 시간이 있다)
- 업로드가 아예 0건 → 🔴. 이미지 루틴 자체가 돌지 않았다
- 본문 alt 누락 → 🟡
- 규격 불일치 → 🟡. **단, 위 5번의 4단계 판정을 거친 뒤에만 올린다.** 이미지 루틴 문서의 현재 표기와 일치하는 값은 불일치가 아니다
- **본문 `[FEATURED_IMAGE_URL]` 등 치환되지 않은 플레이스홀더 문자열이 남아 있음 → 대표 이미지 결손과 동일 취급** (`future`면 🔴, `draft`면 🟡). Notion 원고의 템플릿 토큰이 그대로 배포된 상태이며, HEAD 검사에서 상태코드 0으로 잡힌다
- **미디어 라이브러리의 `alt_text`가 비었지만 본문 `<img alt>`는 채워져 있는 경우 → ⚪ 기록만.** evidence 스크린샷에서 흔하며, 렌더링되는 alt는 본문 쪽이라 SEO 영향이 없다. **이걸 🟡로 올리지 말 것 — 매일 뜨는 노이즈가 된다**

**출력** — 🟡 이상일 때만 쓴다.

```
🖼️ 이미지 결손 — {사이트명} Blog #{N} (WP {id}, {상태})
   {증상}: {실측}
   해결: {조치}
```

**해결책**:
① **업로드 0건** — `PEXELS_API_KEY`가 `pw.txt`에 있는지, Pexels API가 200을 주는지 확인한다. 키 소진·rate limit이면 그 사실을 적는다 → `사용자필요`
② **대표 이미지만 없음(업로드는 정상)** — 이미지는 만들어졌고 연결만 안 된 것이다. wp-admin > 글 > 해당 글 편집 > 오른쪽 `특성 이미지` > 미디어에서 `{슬러그}-hero.webp` 선택 > 업데이트. **미디어 ID를 알람에 함께 적어 주면 사용자가 검색 없이 찾는다** → `사용자필요`
③ **본문 이미지 404** — 미디어 라이브러리에 원본이 남아 있으면 URL 오타이므로 해당 파일의 정확한 `source_url`을 알람에 적는다. 라이브러리에도 없으면 이미지 루틴 재실행이 필요하다 → `사용자필요`
④ **규격 불일치** — 이미지 루틴 본문의 리사이즈 규격 표기를 확인한다 (현행: KoreaPlug 1226×768 = `Schd_KoreaPlug-Image.md` 크롭 결과 / 0and1Life 1100×689 = `Schd_0and1Life-Image.md` v5.4 `TARGET_W`). **루틴 문서 쪽이 실측과 같으면 고칠 것은 이 점검 문서다** — "일간 루틴 항목 12의 규격 기준을 {실측값}으로 갱신 필요" 한 줄만 남기고 알람은 내지 않는다 → `사용자필요`
⑤ **플레이스홀더 잔존** — 이미지 루틴이 그 글을 아직 처리하지 않은 것이다. Writer 직후 몇 시간 안에 이미지 루틴이 도는 것이 정상이므로, **초안이 만들어진 지 6시간 미만이면 알람하지 않고 다음 실행에서 재확인**한다. 6시간이 지났는데도 남아 있으면 해당 이미지 루틴을 1회 수동 실행 → `사용자필요`

⚠️ **이 루틴은 이미지를 새로 만들거나 대표 이미지를 지정하지 않는다.** 읽기 전용 원칙을 유지한다.

---

### 13. 내부링크·홈 정적 카드 위생 🟡 — 월요일 실행에서만 (다른 요일은 통과 처리)

> **왜 이게 점검 항목인가**: 발행 자동화는 링크를 넣을 뿐 사후 검증하지 않는다. 옛 슬러그·오타를 가리키는 내부 링크는 리디렉션·404로 조용히 쌓여 색인 위생을 깎는다. 그리고 홈(KoreaPlug page 866)의 `<div id="kp-posts">` 정적 폴백 카드는 JS를 렌더링하지 않는 크롤러(애드센스 심사기 포함)가 보는 유일한 최신글 목록이다 — 방치되면 사이트 전체가 몇 달 전에 멈춘 것으로 보인다.

**확인** (REST 읽기만 — KoreaPlug 기준, 0and1Life도 동일 절차):

1. 전 글 조회 — `GET /wp-json/wp/v2/posts?status=publish,future,draft&per_page=100&page={1,2}&context=edit&_fields=id,slug,status,content` + `GET /wp-json/wp/v2/pages?per_page=30&_fields=slug`
2. 각 글 `content.raw`에서 `href="/{slug}/"`·`href="https://{도메인}/{slug}/"` 형태의 **단일 세그먼트** 내부 링크를 전부 추출한다 (`wp-`, `blog`, `about`, `contact`, `privacy`, `category`, `tag`, `author`, `topics`, `forums` 접두어와 끝 슬래시 유무 변형은 제외·정규화)
3. 추출한 슬러그가 publish 글·페이지 목록에 없으면 문제로 기록한다. 대상이 `future`/`draft`면 "발행 대기 선링크"로 개수만 세고 알람하지 않는다 — 발행되면 자동 해소된다
4. 홈 정적 카드 — `GET /wp-json/wp/v2/pages/866?context=edit`의 `content.raw`에서 `<div id="kp-posts">` 안 카드들의 슬러그·날짜를 읽고, `GET /wp-json/wp/v2/posts?per_page=6&status=publish&_fields=slug`의 최신 6개와 대조한다. 첫 카드가 최신 6개에 없거나 카드 날짜가 실행일 기준 30일 이상 과거면 문제다

**판정**:
- 존재하지 않는 슬러그를 가리키는 내부 링크 1건 이상 → 🟡
- 홈 정적 카드가 최신 6개에서 이탈 → 🟡
- 발행 대기 선링크만 있음 → ⚪ 기록만

**출력** — 🟡 이상일 때만 쓴다.

```
🔗 링크 위생 — {사이트명}
   죽은 내부링크 {N}건: {대상 슬러그 → 걸린 글 슬러그} (최대 5건 나열)
   홈 정적 카드: {정상 | 이탈 — 첫 카드 {슬러그}, 최신글은 {슬러그}}
   해결: {조치}
```

**해결책**:
① **죽은 내부링크** — 현재 publish 슬러그 목록과 근사 매칭(difflib)으로 교정 후보를 만들어 알람에 `구 → 신` 쌍으로 적는다. 대응 글이 실제로 없는 주제면 `<a>` 태그만 풀어 앵커 텍스트를 보존한다. 교정 자체는 `POST /wp-json/wp/v2/posts/{id}`의 `content` 치환으로 수행하며, 치환 패턴은 `href="/{구슬러그}/"` 계열 4형(끝 슬래시·절대경로 변형)을 모두 잡는다 → `루틴가능`
② **홈 정적 카드 이탈** — page 866 `content.raw`의 `<div id="kp-posts">` 내부 카드 `<a>`들을 최신 발행 6개의 슬러그·제목·날짜로 치환해 `POST /wp-json/wp/v2/pages/866`으로 저장하고, 그 밖의 마크업·스타일·동적 스크립트는 그대로 둔다. 저장 후 wp-admin 상단바 Breeze > Purge All Cache → `루틴가능`
③ 교정 후 같은 감사를 한 번 더 돌려 죽은 링크 0건을 확인한다 → `루틴가능`

⚠️ **이 항목의 교정은 이 루틴이 직접 수행하지 않는다.** 읽기 전용 원칙대로 진단·해결책 제시까지만 하고, 실행은 사용자 승인 후 별도 세션이 한다.

---

## ⚠️ 주의

- 이 루틴은 **읽기 전용**이다. 명시적으로 허용한 두 가지(① GSC 소유권 재검증 버튼 ② 이상 시 Notion 알람 한 줄) 외에는 WordPress·GSC·GA4의 어떤 설정도 바꾸지 않는다. 나머지는 **해결책을 제시하되 실행하지 않는다**
- 성과 지표를 보고 싶은 유혹이 들어도 보지 않는다. 이 루틴이 성과 리포트로 변질되면 존재 이유가 사라진다
- **Chrome 확장이 연결되지 않으면 3·4·5(GSC 상태)·6번만 확인 불가다.** `⚠️ Chrome 미연결 — 3·4·6번 미확인`으로 보고하고, **1·2·5(XML)·7·8·10·12·13번은 fetch와 WordPress REST로 그대로 수행한다.** (v3 정정: 舊 버전은 "3~9번 미확인"으로 적혀 있었으나, 7·8·10·12는 REST만으로 완결된다)
- Notion MCP가 실패하면 잃는 것은 ① 항목 11의 리포트 값 ② `Blog #N` 번호 ③ 알람 기록이다. 각각 폴백이 있으므로(항목 11 실측, `Blog #미확인` 표기, 사용자에게 수동 기재 요청) **Notion 하나 때문에 실행 전체를 중단하지 않는다**
