# ✏️ 0and1Life Writer 지침

## 이 문서의 용도

`0and1life-writer`(Claude Code 루틴)가 매일 실행 시 STEP 1에서 읽는 **작성 전용 지침**이다.

**역할 경계 — 이 문서가 SSOT다.**
판정 기준·수치·품질 기준은 전부 이 문서에만 둔다. 루틴(`Routine_0and1Life-Writer.md`)은 도구이며 **운영 절차**(언제·무엇을·몇 회·어디에 기록)와 **실행 자산**(페이지 ID·API 경로·CPC 기준표·리포트 형식)만 정의한다.

- ⛔ 이 문서의 수치·판정 기준을 루틴에 복사하지 않는다. 복사본은 이 문서가 개정될 때 조용히 구버전으로 남아 상충을 만든다.
- ✅ 규칙을 바꿔야 하면 이 문서만 고친다. 루틴은 매 실행 이 문서를 읽으므로 개정이 그대로 적용된다.
- 루틴을 고치는 경우는 절차·도구·출력 형식이 바뀔 때뿐이다.

> ⛔ **(2026-09-07 확장) 복사 금지 대상은 `Routine_0and1Life-Writer.md` 하나가 아니다.** 아래 문서도 이 문서의 수치를 사본으로 두지 않는다.
> - `0and1Life-Draft.md` (Draft 판정 지침)
> - `Schd_0and1Life-Draft.md` (Draft 스케줄 프롬프트)
> - `Schd_0and1Life-Image.md` 등 파생 스케줄 프롬프트
>
> **실측 근거**: 2026-09-07 Draft 회차에서 `📐 생활 규정·계약 계산 3건 — 한도 2 초과`가 보고됐다. 그런데 사본들(Draft 지침서 7-3 · 스케줄 프롬프트 [6o])에는 **1-3 R1의 「Head Type=시즌 글은 R1 카운트에서 제외」 예외가 빠져 있었다.** 사본이 만들어진 시점 이후 이 문서에 추가된 조항이 따라오지 않은 것이다. 사본은 틀린 값을 주는 게 아니라 **불완전한 값**을 준다 — 그래서 오탐이 조용히 통과한다.
> 파생 문서가 균형 규칙을 써야 하면 **수치를 옮겨 적지 말고 이 문서 1-3 R1~R6을 그 자리에서 읽는다.**

**포함**: Phase 0(주제 선정) → 1(키워드·검증) → 2(HTML) → 3(Notion) → 5(품질).
**제외**: TOC 스크립트·배포·오류표 → 📋 0and1Life Draft 지침 참조.

**타겟 독자**: 한국의 2030~40대 초 직장인 — 돈·재테크·일·결혼·AI가 핵심 관심사. 모든 주제 판단 기준은 "이 독자가 네이버·구글에 실제로 검색할까?"다.

**화자 기준**: 독자와 눈높이가 같은 30~40대 화자. 가공된 신상(나이·직군·연차·결혼 준비 등)을 사실처럼 서술하지 않는다. 1인칭(저/제가)은 이 글을 쓰며 실제로 수행한 행위(계산·조회·비교)에만 쓰고 그 결과물을 본문에 함께 싣는다. 하지 않은 일은 3인칭·2인칭으로 쓴다. 비경험 영역(육아 등)은 주제에서 제외.

---

## Phase 0: 주제 발굴 — 두 방향만 (2026-09-21 전면 개정)

> **왜 갈아엎었나 (실측 근거 — 지우지 말 것).**
> - GA4 9/19 기준 네이버 유입 상위 6편(예초기 대여 224 · 재산세 카드 35 · 본인부담상한 환급 32 · 가다실 가격 21 · 국가성장펀드 20 · 프로포즈 목걸이 18) 중 **뉴스 헤드에서 나온 글은 0편**. 전부 "검색은 되는데 제대로 계산해준 글이 없는 생활 질문"이다.
> - 사이트 1위 글 #84 예초기 대여(2026-08-13)는 **뉴스 포착면 수확이 0인 날** 시즌 캘린더 + 자동완성 마이닝으로 나왔다. 1순위 `벌초 대행 가격`은 정리글 8건 포화였고, 재타겟한 `예초기 대여` 실비·직접 vs 대행 손익분기는 0건이었다. 즉 **뉴스 엔진이 비어 있어야 승자 경로가 열렸다** — 뉴스가 하나라도 있으면 그쪽이 1순위를 가져가 이 경로가 막혔다.
> - 추석 클러스터 9편 중 유입 상위에 든 건 2편(15·13세션). `추석 택배 마감일`(#113)은 WT **969,109**인데 BT 4,781 하나로 `N-경쟁A`를 통과했다 — OR 임계값이 레드오션을 못 막았다.
> - 결론: 뉴스 헤드에 실행 각도를 붙이는 방식은 4~5개월 피드백에도 개선되지 않았다. **뉴스 발굴을 폐지**하고 승자 패턴이 나온 경로만 남긴다.

### 0-1. 주제 발굴 원칙 ⭐ 핵심

**주제는 아래 두 방향에서만 나온다. 셋째 방향은 없다.**

| 방향 | 정의 | 판정(둘 다 실측) | 예 |
|---|---|---|---|
| **A · 블루오션 주제** | 사람들이 실제로 검색하는데(자동완성 실존) 제대로 답한 글이 없는 생활 질문 | ① 네이버 AC 실존 조합 ② 그 조합이 **경쟁도 `N-경쟁A`(AND)** | `예초기 대여 비용` · `가다실 9가 가격` · `본인부담상한 환급 지급일` |
| **B · 레드오션의 다른 방향** | 헤드는 레드오션(A 불통과)이지만, 사람들이 그 헤드 아래서 **다른 질문**을 하고 있고 그 질문은 아무도 안 답한 것 | ① 헤드가 A 불통과 ② 같은 AC 응답 안에 있는 **하위 실존 조합**이 `N-경쟁A` ③ 그 질문의 답이 **계산으로 숫자 하나에 떨어짐**(손익분기·차액·마감일) | `벌초 대행 가격`(포화) → `예초기 대여` 직접 vs 대행 분기 '5.3일' |

**원칙**
- ⛔ **뉴스에서 주제를 만들지 않는다.** 뉴스 헤드 + 실행 각도 전환은 폐지. Google Trends·네이버 뉴스 섹션·앱스토어·실검 스캔은 더 이상 발굴 엔진이 아니다. 뉴스는 방향 A/B로 이미 채택된 주제에 **도입 훅 한두 문장**을 붙일 때만 참고한다(그것도 선택).
- **경쟁도는 채점 앞에 온다.** 후보를 먼저 만들고 나중에 네이버를 재는 순서를 뒤집는다. 자동완성에서 수확한 조합 **전량**을 경쟁도 API로 재고, `N-경쟁A`가 아닌 것은 채점표에 올리지도 않는다(1-T2 T1.5). 재타겟으로 레드오션을 구제하지 않는다 — 방향 B는 '구제'가 아니라 같은 AC 응답 안에서 A등급 하위 조합을 **처음부터 고르는 것**이다.
- **기성 생활명사가 시드다.** 예초기·정수기·정장·웨딩홀·실손·연차·관리비 같은 기성 명사에서 출발한다. "새 것 신호"(시행·출시·인상…) 우선 규칙은 폐지 — 그 신호가 붙은 헤드는 언론·공공기관·대형 블로거가 같은 날 전부 쓴다.
- **시즌 캘린더(0-4)는 '언제 쓸까'를 정하지 '무엇을 쓸까'를 정하지 않는다.** 방향 A/B로 나온 후보가 캘린더 창 안에 있으면 먼저 쓴다. 캘린더 이벤트라는 이유로 후보를 만들지 않는다.
- **직접 영향 테스트**는 유지: 채택 각도가 독자 본인의 돈·시간·권리에 직접 걸리는가. 방향 B의 ③(숫자 하나로 떨어지는 답)이 이 테스트의 강화판이다.
- **네이버 우선 원칙**(GA4 실측 네이버 91%)은 유지하되, 이제 원칙이 아니라 관문이다 — A등급이 아니면 후보가 아니다.

**트랙 라벨**: 구 `S`(스파이크) 트랙은 폐지한다. 기본 정보 표(2-1)의 `Track` 행은 항상 `L`로 적고, 새로 추가한 `방향` 행에 `A` 또는 `B`와 근거를 적는다. 일간 점검 루틴은 `방향` 행을 읽어 A/B 비율을 집계한다.

### 0-2. 발굴 엔진 — 질문 채굴 (Q)

전부 Bash `curl`, 예산 미소모. **뉴스 12소스 스캔은 폐지됐다** — 돌리지 않는다. 스캔 로그도 없다.

**시드 = 생활명사 × 행동어.** 매 실행 아래 6영역 중 **2영역 이상**에서 명사를 고르고 행동어를 붙여 자동완성을 긁는다.

| 영역 | 생활명사 예시(고정 목록 아님 — 매 실행 새 명사를 더한다) |
|---|---|
| 장비·대여 | 예초기 · 정수기 · 공기청정기 · 캠핑용품 · 정장 · 카메라 · 전동킥보드 · 김치냉장고 |
| 서비스·구독 | PT · 필라테스 · 세탁 · 이사 · 청소 · 렌터카 · OTT · 통신요금 · 보험 |
| 계약·위약금 | 전세 · 월세 · 헬스장 · 웨딩홀 · 스드메 · 학원 · 휴대폰 약정 · 렌탈 |
| 요금·고지서 | 관리비 · 전기요금 · 가스요금 · 재산세 · 자동차세 · 건보료 · 통행료 |
| 행사·시즌 | 벌초 · 제사 · 결혼식 · 돌잔치 · 이사철 · 명절 상여 · 휴가비 |
| 일터·급여 | 연차 · 퇴직금 · 수습 · 성과급 · 야근수당 · 실업급여 · 이직 |

**행동어(수식)**: `대여` `구매` `비용` `가격` `환불` `위약금` `해지` `손익분기` `얼마` `언제까지` `기간` `조건` `계산` `비교` `후기` `디시`.

**절차**
1. 시드 조합 40~80개를 **네이버 AC(1순위) + 구글 AC**로 조회한다. 응답 문구를 그대로 수확한다(AI 임의 조합 금지).
2. 수확 조합을 `WP_CORPUS`·백로그와 대조해 기발행분을 뺀다.
3. **남은 조합 전량을 경쟁도 API로 잰다**(webkr + blog, 후보당 2회). 호출 상한은 두지 않는다 — API 호출일 뿐 비용이 없다. 실측 40~120회가 정상.
4. `N-경쟁A`(AND)만 후보로 남긴다. 헤드는 A 불통과인데 하위 조합이 A면 **방향 B 후보**로 표시한다.
5. 남은 후보를 0-6으로 채점한다.

**시드 회전**: 최근 14일 실행 로그에 쓴 시드 명사는 재사용하지 않는다. 회전 기록은 STEP 8 `🔎 질문 채굴` 행에 남긴다.

**커뮤니티 접미**: 수확 문구에 `디시` `더쿠` `뽐뿌` `블라인드`가 붙어 있으면 방향 B 신호다 — 사람들이 정리글이 아니라 커뮤니티에 묻고 있다는 뜻이다. 그 접미를 뗀 조합을 경쟁도로 잰다.

**백로그(엔진 D)**: STEP 1에서 읽은 '대기' 항목은 후보군 맨 앞에 두되, **똑같이 경쟁도를 다시 잰다**. 과거에 OR 임계값으로 통과한 항목이 많다 — 등급이 A가 아니면 '탈락(레드오션 재측정)'으로 상태를 갱신하고 버린다.

**월별 프로브(`{M}월부터`)는 폐지한다.** 수확물이 제도·시행 키워드라 전부 레드오션이었다(`10월부터 바뀌는 전세제도` WT 1,349,750).

### 0-3. 방향 판별

자동완성 응답과 경쟁도 실측으로 기계 판정한다(인상 판단 금지).

| 판정 | 조건 | 처리 |
|---|---|---|
| **방향 A** | 채택 조합 자체가 `N-경쟁A` | 그대로 채택. 포커스 키워드 = 그 조합 |
| **방향 B** | 헤드(시드 명사+첫 수식)는 A 불통과, 같은 AC 응답의 하위 조합이 `N-경쟁A`, 답이 숫자 하나로 떨어짐 | 포커스 키워드 = 하위 조합. 도입부에서 헤드 질문을 받고 H2에서 하위 질문으로 내려간다 |
| 불통과 | 위 둘 다 아님 | 후보 아님. 재타겟 없음 |

**후보 0개 처리**: 2영역 40개 시드로 A등급이 하나도 안 나오면 영역을 2개 더 돌린다(4영역 80개). 그래도 0개면 STOP하고 STEP 8에 `질문 채굴 0건 (시드 {n}개 / 측정 {m}회)`을 남긴다. "적합한 주제가 없다"는 판정은 이 로그가 있을 때만 허용된다. **뉴스로 내려가는 폴백은 없다.**

### 0-4. 시즌 캘린더 — 타이밍 보정 전용

**이 표는 주제를 만들지 않는다.** 방향 A/B로 나온 후보가 아래 이벤트에 걸리면 창 안에서 먼저 쓴다. 예초기 글은 추석 D-43 발행 → 12일 뒤 점화 → 4주 183세션이었다. 창: 마감형 D-14 / 구간형 D-30 또는 진행 중.

| 이벤트 | D-day | 유형 | 연결되는 생활명사 |
|---|---|---|---|
| 자동차세 연납 | 1월 | 구간형 | 자동차세 |
| 부가세 확정신고 | 1/25 · 7/25 | 마감형 | 프리랜서 · 부업 |
| 연말정산 | 1~2월 | 구간형 | 월세 · 카드 · 보험 |
| 봄 이사철 | 2~3월 | 구간형 | 전세 · 이사 · 청소 |
| 종합소득세 | 5/31 | 구간형 | 투잡 · 프리랜서 |
| 여름 전기요금 | 7~8월 | 구간형 | 에어컨 · 전기요금 |
| 재산세 | 7·9월 | 구간형 | 재산세 · 카드 |
| 추석·설 | 음력(매년 확인) | 구간형 | 벌초 · 제사 · 상여 · 귀성 |
| 김장·난방 | 11~12월 | 구간형 | 가스요금 · 난방 |
| 건강검진·IRP | 12/31 | 마감형 | 검진 · 연금 |
| 4월 월급 정산 | 3~4월 | 구간형 | 건보료 · 보수총액 |
| 코세페·블프 | 11월 | 구간형 | 직구 · 관세 |

- **하드캡: 같은 이벤트는 연 2편.** 2026 추석 클러스터는 9편으로 **폐쇄**됐다 — 추석·벌초·귀성·명절 헤드는 2026년 더 쓰지 않는다. 다른 이벤트도 2편째 발행 시 로그에 `이벤트 캡 도달: {이벤트}`를 남기고 그해 닫는다. "연속 2회면 검토" 같은 소프트 규칙은 폐지.
- 음력 이벤트는 매년 실제 날짜를 확인한다.
- 연도 갱신(1-5)은 유지 — 캡에 걸린 이벤트도 **기존 글 갱신**은 한다.

### 0-5. 헤드 자격

**헤드 = 기성 생활명사**(장비·서비스·계약·요금·행사·일터). 0-2의 6영역 안에 있으면 자격이 있다.

- 구 '이슈 헤드(72시간 내 발생)' 조항은 폐지. 신제품·신규 앱·서비스 출시는 헤드가 아니다.
- **제도·시행 헤드**(지원금·세법·노동법 개정)는 방향 A/B 판정을 **통과했을 때만** 헤드다. 통과 못 하면 시행일이 확정돼 있어도 후보가 아니다. 앵커 날짜는 통과 후 제목·도입에 명시한다.
- **하이브리드 형태**: 생활명사(1~2어절) + 행동어(1~2어절), 총 2~4어절. 자동완성 실존 조합만.
- 자리잡은 서비스의 일반 사용법(`토스 사용법`)은 여전히 헤드 자격 없음.

### 0-6. 후보 채점

경쟁도 A등급 후보만 채점한다. CPC는 동점 타이브레이커.

| 축 | 배점 | 기준 |
|---|---|---|
| **경쟁 여유** | /5 | 5 = BT ≤ 1,000 그리고 WT ≤ 30,000 / 3 = A등급 안 / (A 아님 = 채점 대상 아님) |
| **돈이 걸린 계산** | /5 | 5 = 결론이 **숫자 하나**로 떨어짐(손익분기 '5.3일'·차액·마감일) / 3 = 조건별 표 / 1 = 설명형 |
| **폭·시기** | /5 | 5 = 대상 넓음(전 세대·전 국민) **그리고** 0-4 창 안 / 3 = 둘 중 하나 / 1 = 틈새·비시즌 |

- 합계 /15 순으로 정렬. 동점: ① 방향 B(레드오션 우회는 커뮤니티 수요가 증명된 것) → ② 대상 폭 → ③ CPC.
- 구 '선점성'(뉴스 급상승)·'독자 접점(기한 강제)' 축은 폐지 — 둘 다 뉴스·제도 키워드에 구조적으로 점수를 줬다.
- 채점표에 "이 행동을 하는 사람이 누구인가" 한 줄을 기재한다.

**CPC 기준표** (타이브레이커 참조용):
- $$$ 재테크·ETF·보험·증권·대출·세금·퇴직연금 → $0.8~2.0+
- $$$ 이직/커리어 교육·웨딩(스드메·예물·신혼여행) → $0.5~1.5
- $$ AI 도구·자동화·부업·온라인 강의·구독 → $0.3~0.8
- $$ 짠테크·앱테크·가계부·소비 관리 → $0.3~0.8
- $ AI 트렌드·생활팁·뉴스·디지털 문화 → $0.1~0.3

### 0-7. 회차 누락(미실행) 처리

루틴이 아예 실행되지 않은 날은 스스로 기록할 수 없다. 다음 실행 회차가 대신 판정한다.

- **판정**: 현황표(`[0]`·`[1]` 양 계열) 어느 쪽에도 해당 날짜의 실행 로그가 없으면 누락이다. 초안 페이지 존재 여부로 판정하지 않는다 — 현황표 로그가 유일한 기준이다.
- **재시도 회차**: 같은 KST 날짜 안에 재실행되면 정상 진행한다. 날짜가 바뀌면 새 날 기준. 지난 날짜 소재를 소급 생산하지 않는다.
- **연속 누락**: 2회 연속 → 🟡 경고 + 날짜 명시. 3회 연속 → 🔴 예약 작업 점검을 사용자에게 요청한다.
- **기록**: 누락 다음 첫 성공 회차가 STEP 8에 `⛔ 회차 누락: {날짜} — 연속 {n}회`를 남긴다.
- 누락일에 보상 조치는 없다. 다음 회차는 정상 실행한다.

---

## Phase 1: 키워드 리서치 & 검증

### 1-T. 질문 수요 검증 (가장 중요)

방향 A/B 후보는 0-2에서 이미 **자동완성 실존 + 경쟁도 `N-경쟁A`(AND)** 를 통과한 상태다. 여기서는 그 위에 두 관문을 더 건다.

- **① 각도 수요 검증**: 포커스 키워드만이 아니라 **'이 글이 답하는 핵심 질문'(각도)도 자동완성 실존 문구**여야 채택한다. 헤드는 검색되는데 각도가 0건이면 순위에 들어도 클릭이 없다. 실존 0건 각도는 GAP 보조 재료로만 쓴다. 방향 B는 하위 조합 자체가 각도이므로 자동 충족 — 그래도 조회해 기록한다.
- **② 공백 판정 (경쟁 구도 관문)**: 구글 1페이지에 개인 블로그 또는 커뮤니티 질문글이 **1개 이상** 있는가. 공단·언론·대형 사업자로만 채워져 있으면 신생 도메인은 못 이기므로 탈락. **면제 없음**(구 신선 스파이크 면제 폐지). 정리글이 여럿이라는 것은 탈락 사유가 아니라 수요 증명이다 — 그 경우 GAP ⭐(같은 질문의 더 높은 해상도 — 2-5)로 이긴다.
- **③ 하이브리드 형태**: 생활명사(1~2어절)+행동어(1~2어절), 총 2~4어절. 자동완성 실존 조합만.

**채택 기준 (모두 충족)**
- 네이버 AC 실존 조합(0-2 수확분) · 네이버 검증 등급 `N-통과` 또는 `N-재타겟`(1-T2 T1)
- **경쟁도 `N-경쟁A`(AND)** — 1-T2 T1.5. 예외 없음
- 각도 수요 검증 ① 통과
- 경쟁 구도 관문 ② 통과
- 검색 의도가 정보형(방법·조건·비교·이유·계산)
- 월간 검색량 조건은 두지 않는다(측정 불가) — 자동완성 실존 + 경쟁도가 수요 증거를 대신한다

### 1-T2. 네이버 수요 검증 관문 (필수)

구글에서 통하는 롱테일이 네이버에서는 검색량 0인 경우가 흔하다. 키워드 선정 시 네이버 수요를 후보별로 검증한다.

**⛔ 관측 한계 — 이 관문이 볼 수 있는 것과 없는 것.**

| 신호 | 관측 주체 | 가능 여부 |
|---|---|---|
| 네이버 자동완성 조합 실존 | 루틴 (`ac.search.naver.com` JSON) | ✅ 자동 |
| 네이버 서브키워드 실존 문구 | 루틴 (같은 응답) | ✅ 자동 |
| **네이버 웹문서·블로그 경쟁 문서 수(total)** | 루틴 (검색 API 릴레이 — 루틴 4-A3) | ✅ **자동 (2026-09-13 신설)** |
| **네이버 웹문서 상위 10 구성·우리 글 포함 여부** | 루틴 (같은 응답 `items[]`) | ✅ **자동** — 단, 웹문서 API 기준이라 통합검색 배치와 다를 수 있음 |
| 네이버 통합검색 SERP 순위·1페이지 탭 구성 | — | ⛔ **불가** (`search.naver.com` 영구 차단) — 사용자 실측(T2)로만 |
| 네이버 월간 검색량 | — | ⛔ **불가** (데이터랩 차단) |
| 발행 후 실제 노출·클릭·순위 | 사용자 (서치어드바이저) | 🙋 **사용자 실측** (1-T2 T2) |

⚠️ **없는 신호를 추정으로 채우지 않는다.** 네이버 순위·검색량을 모델 지식으로 단정하는 것은 금지다. 판정은 자동완성 실존 여부만으로 한다.

**T1. 자동 관문 (매 회차 · 루틴 수행)**

숏리스트 1순위 후보부터 순서대로, **후보당 1회 · 회차당 최대 3회** 조회한다. 엔드포인트·파싱은 루틴 STEP 4-A에 둔다.

판정은 3등급이다. 탈락시키지 않고 **강등**시키는 것이 핵심이다 — 완전 탈락으로 만들면 규정·계산 계열이 전멸해 발행이 멈춘다.

| 등급 | 조건 | 처리 |
|---|---|---|
| **N-통과** | 네이버 AC에 **핵심 조합이 그대로 노출** | 그대로 채택. 서브키워드는 **네이버 AC 실존 문구를 우선**으로 구성한다 |
| **N-재타겟** | 조합은 없으나 **헤드 단독 또는 인접 조합**이 노출 | 네이버 AC가 준 실존 문구로 Focus Keyword를 **1회 교체 시도**. 교체가 성립하면 N-통과로 승격 |
| **N-구글전용** | 헤드·조합 모두 미노출 | 탈락이 아니다. 채택은 가능하되 **숏리스트에서 N-통과·N-재타겟 후보보다 뒤로 밀린다.** 기본 정보 표에 `N-구글전용`을 명시하고, 그 회차 로그에 사유를 남긴다 |

- **AC 조회 실패(차단·타임아웃)**: 그 후보는 `N-미검증`으로 기록하고 구글 기준으로 진행한다. **STOP 아님.** 회차당 2회 연속 실패하면 그 회차는 네이버 검증을 건너뛰고 로그에 `네이버 AC 접근 실패`를 남긴다.
- **N-구글전용 상한**: 7일 창에 `N-구글전용`이 **4건 이상**이면 다음 회차는 **N-통과 후보를 1순위로 강제 검토**한다(R6과 연동).
- 네이버 AC 응답 문구는 **그대로** 쓴다. AI 임의 조합 금지(1-0과 동일).

**T1.5 네이버 경쟁도 관문 (검색 API — 2026-09-13 신설 · 2026-09-21 AND로 강화 · 채택의 전제 조건)**

트래픽의 원천이 네이버이므로 **"네이버에서 이길 수 있는가"가 후보 자격 자체다.** 사이트 릴레이 API(호출 스펙은 루틴)로 **WT**(웹문서 total)·**BT**(블로그 total)·**상위 10 중 개인 블로그 수**를 잰다.

**측정 시점**: 0-2 절차 3단계 — **채점 전, 수확 조합 전량**. 숏리스트를 만든 뒤 재는 구 순서는 폐지(그때는 이미 같은 풀에서 고른 후보뿐이었다). 호출 상한 없음.

| 등급 | 조건 | 처리 |
|---|---|---|
| **N-경쟁A** | BT ≤ 5,000 **그리고** WT ≤ 100,000 | 후보 자격. 0-6 채점 대상 |
| **N-레드오션** | 그 외 전부 | 후보 아님. 재타겟 없음 — 같은 AC 응답의 하위 조합이 A면 그것이 **방향 B 후보**가 될 뿐, 헤드 자체를 구제하지 않는다 |

- 구 `N-경쟁B`(BT ≤ 30,000 그리고 WT ≤ 1,000,000)는 **폐지**. 구 OR 조건(BT ≤ 5,000 **또는** WT ≤ 100,000)도 폐지 — `추석 택배 마감일` WT 969,109가 BT 4,781로 A를 받았다.
- **면제 없음.** 구 '신선 스파이크 면제'·'시즌·제도 선점형은 레드오션만 아니면 통과' 조항 폐지.
- **임계값 근거 (2026-09-13 실측)**: 네이버 상위 10 노출 자사 글은 WT 4,341~62,086 · BT 17~424, 묻힌 글은 WT 40만~1,497만 · BT 2.2만~9.7만. **주간 점검이 매주 유입 상위 10편의 WT/BT를 출력하고 임계값 조정을 제안한다**(1-5). 한 번 잡고 끝내지 않는다.
- **실패 처리**: 릴레이 호출 실패(401·5xx·타임아웃)는 재시도 1회, 그래도 실패면 그 후보는 `N-경쟁-미검증`으로 **후보에서 제외**한다(추정으로 채우지 않는다). 회차 전체가 실패하면 `네이버 경쟁도 API 접근 실패`를 기록하고 그날은 STOP — 경쟁도 없이 채택하지 않는다.

**T2. 사용자 실측 (주 1회 · `사용자필요`)**

SERP 순위와 경쟁 구도는 루틴이 볼 수 없다. 사용자가 자기 크롬에서 확인해 넘긴다. 이 값이 없으면 그 주는 T1만으로 판정하고, 없다는 사실을 로그에 남긴다(추정으로 대체 금지).

1. 네이버 서치어드바이저 → 콘텐트 노출/클릭 → **검색 키워드 TOP 20**을 그대로 복사
2. 직전 주 발행분 중 1편의 Focus Keyword를 네이버 검색창에 넣고 **① 우리 글 순위(숫자) ② 1페이지 10개 중 개인 블로그·카페 개수**를 기록
3. 두 값을 `🎯 0and1Life 키워드 백로그` 페이지 상단 `네이버 실측` 블록에 붙여넣기

**T2가 들어오면 다음 회차부터 적용되는 것**
- 1페이지 10개 중 개인 블로그·카페가 **0개**인 키워드군은 1-3 경쟁 구도 관문에서 **네이버 기준 불통과**로 처리하고 같은 계열 신규 후보를 만들지 않는다
- 서치어드바이저 검색 키워드에 **노출 ≥10 · 클릭 0**인 문구가 있으면 그 문구를 **B등급(CTR 결함)** 으로 보고 1-5 절차대로 제목·메타를 재작성한다 — 신규 글보다 우선한다

### 1-T3. (폐지 — 2026-09-21)

신선 스파이크 경로는 뉴스 발굴 폐지(0-1)와 함께 삭제됐다. 자동완성 미형성·경쟁 관문 면제는 더 이상 없다. 구 '시즌·제도 헤드 별도 경로'(자동완성 미노출 통과)도 같이 삭제 — 후보는 0-2에서 자동완성 실존으로만 수확되므로 미노출 후보 자체가 생기지 않는다.

### 1-0. 핵심키워드 → 서브키워드 → 제목 전략

- 핵심키워드(2어절 권장) → 서브키워드(핵심+1단어, 자동완성 기준 2~4개) → SEO Title 순으로 확정.
- 서브키워드는 자동완성·검색결과에 실제 노출된 문구만. AI 임의 조합 금지.
- 서브키워드 인텐트가 글 목적과 일치해야 한다. 방법형 글에 조회형 키워드(~조회·~번호·~검색) 혼입 금지.

### 1-1. 황금 키워드 조건 (에버그린 롱테일)

- 월간 검색량 1,000~10,000 · 3~5어절 질문형·계산형 · 공백 실측 통과 · 에버그린.
- 생활 6개 영역 시드에서 자동완성 마이닝(에버그린 발굴은 여기서만 한다).
- 자동완성에 없는 인위적 조합({키워드}+직장인 등) 금지.
- **네이버 경쟁도 `N-경쟁A` 구간(1-T2 T1.5)을 우선한다** — '예초기형' 승자 패턴(0-1)의 ⓒ 요소. 에버그린은 선점 시급성이 없으므로 레드오션 후보를 굳이 채택할 이유가 없다.

### 1-2. 리서치 소스

- 자동완성 API(**네이버 1순위·구글 보조 — 0-2 질문 채굴**) · 경쟁도 릴레이 API(1-T2 T1.5) · **`WP_CORPUS` 전 코퍼스 캐시**(`GET /wp-json/wp/v2/posts?status=any&per_page=100&_fields=id,slug,status,title`, 세션당 1회, **응답 헤더 `x-wp-total` 동시 확인 필수 — 1-3 절단 판정**) · GSC 승자 패턴 · **네이버 서치어드바이저 실측(1-T2 T2)** · 키워드 백로그.
- ⚠️ **기존 글 확인의 원천은 Notion 현황표가 아니라 워드프레스다.** 현황표는 회차 카운트·번호 시퀀스·실행 로그 전용이며, "같은 각도의 글이 이미 있는가"는 반드시 `WP_CORPUS`로 묻는다.
- ⚠️ **예외 하나 — 같은 날 앞 슬롯 산출물만은 현황표가 원천이다.** 앞 슬롯 글은 Notion 초안으로만 존재하고 Draft 루틴 배포 전이라 워드프레스에 없다.
- 커뮤니티(블라인드·에브리타임 등)는 실제 질문 인용 근거로만.
- ⛔ 네이버 데이터랩·`search.naver.com` SERP는 영구 차단이다. 검색량·통합검색 순위를 **모델 지식으로 추정해 채우지 않는다** — 없으면 없는 대로 `N-미검증`으로 기록한다. 단, **경쟁 문서 수(WT·BT)와 웹문서 상위 10은 검색 API 릴레이(1-T2 T1.5·루틴 4-A3)로 실측 가능**하므로 이 둘은 실측으로 채운다.

### 1-3. 키워드 검증 체크리스트

**공통**
- [ ] **검색 이전 중복 대조(최우선)**: **`WP_CORPUS` 전 코퍼스 캐시**와 대조를 모든 검증보다 먼저 끝냈는가. 단어가 아니라 **각도**로 판정한다 — 같은 대상·같은 질문·같은 목적이면 중복, 단어만 겹치고 각도가 다르면 중복 아님. 최근 회차가 같은 조문·같은 계산표를 소진했으면 재사용하지 말고 내부링크로 넘긴다.
  ```
  GET /wp-json/wp/v2/posts?status=any&per_page=100&_fields=id,slug,status,title   (세션당 1회, 앱 비밀번호 Basic 인증)
  ※ 응답 헤더 x-wp-total · x-wp-totalpages 를 반드시 함께 캡처한다 (curl이면 -D 필수)
  ```
  - 🔴 **절단 판정 없는 조회는 결과를 신뢰하지 않는다 (최우선).** `x-wp-total`을 읽지 않은 조회는 "중복 없음"이 아니라 **'절단 미판정'**이다. 반환 건수가 `per_page`와 정확히 같으면 그게 전부인지 잘린 것인지 응답 본문만으로는 **구분할 수 없다**. 따라서 모든 WP REST 조회는 헤더를 함께 캡처하고, 아래를 그 자리에서 판정한다.
    - `x-wp-total ≤ per_page` → 전량 확보. 정상
    - `x-wp-total > per_page` → **절단이다.** `page=2`를 이어 받아 전량을 채운다(⚠️ WP REST `per_page` 상한 = 100 — 100을 넘겨 올릴 수 없다). 절단 상태로 중복 판정을 내리지 않는다
    - 헤더를 못 읽었다 → 판정 불가. STEP 8에 🔴 「중복 대조 절단 미판정」으로 기록하고, 그 회차의 "중복 없음" 결론에 이 단서를 붙인다
    - ⚠️ **코퍼스가 100건에 임박했다.** 100건을 넘는 날부터 헤더를 읽지 않으면 주 경로가 조용히 잘리기 시작한다 — `x-wp-totalpages` 만큼 `page`를 승계한다
  - ⚠️ **`?search=` 후보별 조회를 기본 경로로 쓰지 않는다.** `per_page` 소값 + 정렬 미지정은 WP 기본이 날짜 내림차순이라 **오래된 진짜 중복이 잘려 나간다**. 보조로 쓸 때는 **반드시 `&orderby=relevance`**를 붙이고 **`&per_page=100`**으로 조회하며, 위 절단 판정을 그대로 적용한다
  - ⚠️ **판정은 제목 매치 우선.** `?search=`는 본문까지 훑어 노이즈가 크다. 다중 어절도 구(phrase)가 아니라 **토큰 AND**라 오탐이 난다. 본문에만 걸린 결과는 참고 신호이며 그것만으로 탈락시키지 않는다
  - ⚠️ **단일 일반명사 단독 조회 금지** — `계산`·`비용`·`기준`·`신청`·`카드` 류는 코퍼스의 절반 이상이 걸려 필터 기능을 못 한다. 포커스 키워드 전체 또는 헤드+수식 2어절 이상으로 묻는다
  - ⚠️ `_fields` 생략 금지 — 미지정 시 응답이 수천 토큰까지 부풀어 오른다
  - 🔴 **인증(`WP_USER`·`WP_APP_PASS`)은 필수다.** 미설정 시 `status=any`가 HTTP 400(`rest_forbidden_status`)이 되고 발행분만 보여 **draft·pending·future가 사각지대**가 된다. 발동 시 `status`를 빼고 진행하되 STEP 8에 🔴 「기존 글 확인 인증 실패 — 초안·예약 {n}건 미대조」로 기록한다. 정상 폴백이 아니라 이상 신호다
  - ⚠️ 포커스 키워드는 REST에 노출되지 않는다(Rank Math 메타 미등록). 제목 대조로 갈음하며, 정밀 확인이 필요한 후보만 현황표의 해당 행 하나를 연다
- [ ] **WebSearch 한도**: 4-B 전용 · 글당 최대 4회(공백 실측 → 2순위 비교 → 재타겟 → 예비). STEP 1~4-A는 WebSearch 미사용(자동완성이 실존 문구를 주는 더 정확한 도구). 소진해도 STOP 아님 — 4-B는 숏리스트 내 비교로 승자를 정한다.
- [ ] **자동완성 실패 시 대체 수요 증거**: 커뮤니티 최근 1년 동일 질문 3건 이상. 없으면 탈락 — 검증 생략 후 진행 금지.
- [ ] 카테고리에 명확히 속하고 **`WP_CORPUS`에** 중복이 없는가(같은 날 앞 슬롯은 현황표로 별도 확인).
- [ ] **YMYL 주제**(연금·세금·보험·투자): 공단·금융사가 1페이지를 점령한 헤드는 회피하고 질문형·계산형 각도로만 접근 + 공식 출처 외부링크 필수 + **2-8 E-E-A-T 신뢰 블록 필수**.
- [ ] **1급 자료 조달 계획(후보 확정 전 필수)**: 이 주제로 1급 원본 자료를 어떻게 확보할지 한 줄로 적는다. 못 적으면 채택하지 않는다.
  - `루틴가능` — 로그인 없이 조작 가능한 공개 계산기·공식 페이지 결과 화면, 실제 실행 테스트 결과. **우선 채택.**
  - `사용자필요` — 현장 사진·실제 견적서·영수증·고지서. 주 1~2건 이내.
  - `사용자필요(앱전용)` — 모바일 앱 안에서만 보이는 화면. Draft 루틴이 브라우저로 캡처 불가. 채택 시 웹 버전·공식 소개 페이지로 대체 가능한지 먼저 확인하고, 캡처 일정을 사용자와 합의한다.
  - `불가` — 후보 탈락.
- [ ] **반려 로그 대조**: 최근 7일 행에서 같은 사유 코드 2회 이상 반복 유형은 제외. `조달 주체=사용자` 미해결 3건 이상이면 이번 회차는 `루틴가능` 주제만.
- [ ] **경쟁 구도 관문(불통과 시 폐기 — 면제 없음)**: **구글** 1페이지에 개인 블로그 또는 커뮤니티 질문글이 1개 이상 있는가. 공단·은행·보험사·대형 언론·전업 상위노출 사업자로만 채워져 있으면 신생 도메인은 못 이기므로 탈락. 검색량·CPC 무관. 사전 예측 지표: 자동완성 커뮤니티 접미(디시·뽐뿌·더쿠·블라인드) 순위 — 상위 3위 안이면 통과.
  - ⚠️ **네이버 1페이지는 루틴이 볼 수 없다**(`search.naver.com` 영구 차단). 네이버 쪽 경쟁 구도는 1-T2 T2로 들어온 사용자 실측이 있을 때만 판정한다. 실측이 없으면 **구글 기준으로만 통과 처리**하고 `네이버 경쟁구도 미확인`을 로그에 남긴다 — 봤다고 쓰지 않는다.
- [ ] **네이버 수요 검증(1-T2 T1)**: 채택 후보의 네이버 AC 등급을 판정하고 기본 정보 표에 기재했는가. R6 하한에 걸리지 않는가.
- [ ] **각도 수요 검증(1-T ①)**: '이 글이 답하는 핵심 질문'이 자동완성(구글 또는 네이버)에 실존 문구로 존재하는가.
- [ ] **경쟁도 `N-경쟁A`(AND)** 실측값(WT·BT)을 기본 정보 표에 적었는가. A가 아닌 후보를 채택하지 않았는가(1-T2 T1.5). **방향(A/B)** 행을 근거와 함께 적었는가.
- [ ] **유명 툴 일반 사용법 배제**: `{유명 툴}+사용법/활용법/가이드` 형태 탈락. 허용 각도(비교·특수 용도·신생 툴·실측 후기) 중 하나에 속해야 한다. 🤖 AI 정보 후보는 여기에 더해 **'AI 카테고리 조건부 해제' 조항의 허용 4각도**에 명확히 속해야 한다.

**카테고리 균형 규칙 (R1~R6)**

> 🔒 **이 블록이 R1~R6의 SSOT다 (2026-09-07 표식).** 다른 문서·프롬프트는 이 수치를 옮겨 적지 않고 **이 블록을 조회해 그대로 적용**한다.
> 조회 경로: `https://raw.githubusercontent.com/leejc0404/blog/main/0and1Life-Writer.md?cb={epoch_ms}` → `**카테고리 균형 규칙 (R1~R6)**` 이후 코드블록.
> R1을 셀 때 **예외 조항(시즌 제외 · 하루 N건 배수 · R2-D)까지 함께 읽는다.** 상한 숫자만 떼어 읽으면 오탐이 난다.

```
R1. 7일 창(오늘 포함) 동일 카테고리 상한
    💰 재테크 2 · 💼 대기업 2 · 📐 규정·계약 2 · 💍 웨딩 2 · 🏠 Life 2 · 🤖 AI 1
    - Head Type=시즌 글은 R1 카운트에서 제외(창이 열려 시즌이 몰리는 것은 편중이 아니다).
      Head Type=제도·— 는 제외 대상 아님. 시즌 제외로 한도를 넘긴 회차는 로그에 `시즌 예외 {n}건` 기록.
    - 하루 N건 모드(DAILY_TARGET≥2): 위 상한을 전부 N배로 읽는다(N=2면 💰4·💼4·📐4·💍4·🏠4·🤖1).
      AI는 배수 제외(N과 무관하게 주 1건). 웨딩은 배수하되 같은 날 2건 금지.
R2. 3연속 금지 — 직전 2건이 같은 카테고리면 3번째는 다른 카테고리(R1 미달이어도 적용).
    R2-D: 하루 N건 모드에서 같은 날 두 글은 카테고리가 달라야 한다. 직전 2건을 셀 때 같은 날 앞 슬롯도 발행 순서대로 포함.
R3. 순환 하한 — 7일 창에 서로 다른 카테고리가 3개 미만이면 가장 오래 안 쓴 카테고리를 그날 1순위로 검토.
R4. 전면 봉쇄 시 — R1~R3으로 모든 후보가 막히면 멈추지 말고 상한 여유가 가장 큰 카테고리를 1건 초과 허용하고 그 사실을 '균형 규칙 예외'로 로그.
R5. 형식 로테이션 — 직전 3건이 같은 형식(계산·역산표 / Q&A / 비교표)이면 4번째는 다른 형식.
R6. 네이버 수요 하한 — 7일 창의 네이버 검증 등급(1-T2)을 세어
    `N-통과`가 2건 미만이거나 `N-구글전용`이 4건 이상이면,
    그날은 N-통과 후보를 1순위로 강제 검토한다. 후보가 없으면 그 사실을 로그에 남긴다.
    ⚠️ R6은 카테고리가 아니라 '수요 검증 등급'을 세는 규칙이다. R1과 독립적으로 적용한다.
```

**발행량 가드**: 최신 GSC 리포트의 F등급(색인 거부) 실질 비율이 30%를 2주 연속 초과하면 실행 로그에 명시, 3주 연속이면 발행 축소안을 사용자에게 제시. 신규 발행이 F등급을 재생산할 가능성을 배제하지 않는다.

**AI 카테고리 조건부 해제**

**수량**: 🤖 AI 정보(WP 26) 신규 발행 **주 1건**(R1). 하루 N건 모드에서도 배수하지 않는다.

**✅ 허용 각도 (아래 4종에 명확히 속할 때만)**

| 각도 | 정의 |
|---|---|
| **AI 부업·수익화** | AI로 돈을 버는 구체 절차·수익 구조·세금 |
| **실생활 문제해결** | 툴이 아니라 **독자의 문제**가 주어인 글(고민 상담·문서 정리·일정 관리) |
| **특정 작업 실행법** | `{툴}로 {구체 작업}` — 작업이 검색어의 중심 |
| **실측 비교** | 2개 이상 툴의 **요금·한도·제약을 직접 조회해 대조**한 표 |

**⛔ 금지 유형 (예외 없음)**
- `{유명 툴} + 사용법 / 활용법 / 가이드 / 완전정복 / 총정리` (1-3 '유명 툴 일반 사용법 배제'와 동일 기준)
- 공식 문서·요금표를 재정리한 기능 나열형
- 실사용하지 않은 툴의 체험 서술. 화자 프로필상 **Claude(Max)·Gemini(Pro)·ChatGPT·퍼플렉시티 외에는 '짧게 확인해봤다' 수준까지만** 쓸 수 있다

**🚦 재동결 게이트 (자동 — 주간 GSC 점검 1-5가 판정)**
- 해제 이후 발행한 AI 글이 **발행 4주 시점에 `크롤링됨-미색인`인 건이 2건 연속** 발생하면 **즉시 주 0건으로 재동결**하고 그 사실을 리포트에 기록한다. 사용자 승인 대기 없이 적용한다.
- 재동결 후 재해제는 사용자 판단으로만 한다. 루틴이 스스로 다시 풀지 않는다.
- **판별 애매하면 🤖로 본다** — 수단의 중심이 AI면 🏠·💼로 우회 배정하지 않는다(2-1).

**분리 원칙**: 기존 색인 거부 글의 재작성·병합·삭제는 **이 조항과 별개 트랙**이다. 신규 발행 재개를 기존 글 처리의 전제 조건으로 걸지 않는다.

### 1-4. Focus Keyword 형식
```
Focus Keyword (핵심, 2어절 권장): 노션 자동화
Sub Keywords (핵심+1단어, 자동완성 기준): 노션 자동화 기능 / 노션 자동화 무료 / 노션 자동화 방법
```

### 1-5. 발행 후 GSC 피드백 루프

주간 "SEO 주간 점검"이 실행한다(일일 writer와 별개). 결과는 📈 GSC 리포트 페이지에 기록.

**등급·조치**
- **F 색인 거부(최우선)**: `크롤링됨-현재 색인 안 됨`. 실적 탭에 안 보이고 색인 리포트에만 보인다. 조치: ① 1급 자료 넣어 전면 재작성 후 재요청 ② 같은 주제 여럿이면 병합(410) ③ 가치 없으면 삭제. 방치하면 사이트 전체 평가·애드센스에 직접 영향.
- **A 승자**(클릭≥2): 내부링크 허브, 같은 계열 후속 파생. 건드리지 않는다.
- **B CTR 결함**(노출≥50 & 순위≤13 & CTR<1.5%): 제목·메타를 질문형/구체 수치형으로 재작성. 효과 가장 빠름.
- **C 순위 부족**(노출≥30 & 순위 13~30): 콘텐츠 보강 + A등급에서 내부링크 추가.
- **D 경쟁 과다**(순위>30): 대형 키워드 포기, 롱테일 재타겟.
- **E 무반응**(60일+ & 노출<10): 색인 확인 → 재타겟 또는 통합.
- **G 크롤 위생(주간 필수 — 신설)**: GSC 색인 리포트의 `찾을 수 없음(404)` · `리디렉션이 포함된 페이지` · `NOINDEX 제외` 목록과 쿼리형 URL(`?p=`)·`/feed/` 크롤 흔적을 매주 수집해 처리한다.
  - ① 404 URL: 사이트맵·내부링크·본문에 남은 참조를 찾아 제거하고, 수요가 있던 슬러그(Rank Math 404 모니터 히트 포함)는 주제 인접 글로 301을 건다. 참조도 수요도 없으면 그대로 둔다(자연 소멸).
  - ② 리디렉션 URL: 301 목적지가 **주제 인접 글**인지 확인한다. 무관한 글로 가는 301은 목적지를 교정하거나 410으로 바꾼다.
  - ③ NOINDEX 제외: 의도된 것인지 확인한다. 의도가 확인 안 되는 본문 글의 NOINDEX는 알람으로 올린다(`사용자필요`).
  - ④ GSC 잔상(이미 삭제·301 처리 완료 URL)은 조치 목록에 올리지 않는다 — 잔상 판정은 `redirect:manual` fetch + WP REST 실존 확인으로 한다.
  - 처리 결과를 리포트에 "크롤 위생: 404 {n}건 중 조치 {m} / 리디렉션 {n}건 중 교정 {m} / NOINDEX {n}건" 형식으로 남긴다.
- **🧿 경쟁도 임계값 피드백(주간 필수 — 2026-09-21 신설)**: 네이버 유입 상위 10편과 하위 10편의 **WT/BT**(현황표 요약 셀 `[N WT/BT/등급]`)를 표로 출력하고, 상위군 최대값·하위군 최소값을 1-T2 T1.5 임계값(BT 5,000 / WT 100,000)과 대조해 조정안을 제안한다. 상위 10편 중 뉴스 헤드 출신이 있으면 그 사실도 적는다(0-1 개정 검증용).
- **시즌 선행 발행 단서**: D-day 전에 발행한 시즌·제도 글의 초기 노출·클릭 0은 정상이다(수요가 아직 안 터졌다). D-day 이후 2주까지는 B~E등급 판정을 유보한다 — 선행 발행분을 무반응으로 오판해 재타겟하지 않는다.

**🤖 AI 재동결 판정 (매 주간 점검 필수)**
- 조건부 해제 이후 발행한 🤖 글을 **발행 4주 경과분만** 골라 GSC 색인 상태를 확인한다(4주 미만은 판정 유보 — 구글 재크롤링 전이다).
- `크롤링됨-미색인`이 **2건 연속**이면 그 자리에서 **주 0건 재동결**하고 리포트에 `AI 재동결 {날짜} — 근거 {슬러그 2건}`을 남긴다. 사용자 승인을 기다리지 않는다.
- ⚠️ 판정 전 반드시 **잔상 제외**를 먼저 한다 — `redirect:manual` fetch로 301 여부, WP REST `posts?slug=`로 실존 여부. 이미 삭제·병합된 글을 근거로 재동결하는 것을 막는다.

**🟢 네이버 실측 편입 (1-T2 T2가 들어온 주에만)**
- 서치어드바이저 검색 키워드 중 **노출 ≥10 · 클릭 0**인 문구는 **B등급(CTR 결함)** 으로 등록한다. 구글 GSC의 B등급 기준과 별개로 센다.
- 네이버 노출 상위 문서 5개를 기록하고, **직전 4주 발행분이 그 목록에 0편**이면 리포트에 `⚠️ 신규 발행분 네이버 노출 0` 을 명시한다. 이건 주제 축이 네이버에서 안 먹히고 있다는 신호이며, 다음 회차 R6 판정의 근거가 된다.
- T2 미제출 주는 `네이버 실측 미제출`로 기록만 하고 넘어간다. **추정으로 채우지 않는다.**

**연도 갱신 루프**: 연도 조합 검색어("2025 추석 ○○"→"2026")는 매년 같은 글이 다시 이긴다. 다음 해 같은 이벤트가 창(마감형 D-14 / 구간형 D-30)에 들어오면 신규 글을 쓰지 않고 기존 글의 연도·날짜·수치를 갱신해 dateModified를 살린다. `Head Type=시즌` 글은 다음 해 갱신 대상 목록에 자동 포함. 실행 로그에 `갱신: #XX {연도}`로 기록.

---

## Phase 2: 한국어 HTML 콘텐츠 작성

### 2-1. 기본 정보 표 (메타 정보)

글 작성 전 Notion 서브페이지 섹션 1에 완성한다. `Draft 일자`는 공란.

| 항목 | 형식 |
|---|---|
| Focus Keyword | S: 하이브리드 2~4어절 / L: 3~5어절 질문형 |
| Sub Keywords | 핵심+1단어 2~4개, " / " 구분 |
| SEO Title | 키워드 포함 + 훅(2-2) |
| H1 | SEO Title과 동일 |
| Slug | 영어 소문자+하이픈, 40자 이하 |
| Meta Description | 키워드로 시작, 130~155자 |
| Theme Color | [0]=파랑계 / [1]=따뜻한계 (2-4) |
| Internal Link 1·2 | 상대경로 `/slug/` |
| Wrapper ID | `z-[slug-첫단어]-report` |
| Category | 카테고리명 + WP ID (예: `📐 생활 규정·계약 계산 (WP 30)`) |
| **Track** | 항상 `L` (S 트랙 2026-09-21 폐지 — 일간 점검 호환용으로 행은 유지) |
| **방향** | `A`(블루오션 주제) 또는 `B`(레드오션의 다른 방향) + 근거 한 줄. B면 `헤드 {문구}(WT/BT) → 하위 {문구}(WT/BT)` 형식으로 두 측정값 병기 (0-3) |
| **Head Type** | 0-4 창 안에서 발행하면 `시즌 + 이벤트명 + 앵커 날짜`(R1 카운트 제외·연도 갱신 대상), 제도 시행일이 걸리면 `제도 + 시행일`, 아니면 `—` |
| **Problem Hook** | `[도입 문제 1줄] → [답하는 H2 제목]`. 신기술·앱 필수, 그 외 권장 |
| **네이버 검증** | `N-통과` / `N-재타겟` / `N-구글전용` / `N-미검증` 중 하나 + 네이버 AC 실존 문구 그대로 병기 (1-T2). 예: `N-통과 — "예초기 대여 비용"` |
| **네이버 경쟁도** | `WT {n} / BT {n} / 상위10 개인블로그 {n} / N-경쟁A` — A가 아니면 이 표를 만들 수 없다 (1-T2 T1.5) |

**카테고리 배정 결정 트리** — 한 문장 테스트로 판정하고 겹치면 좁은 쪽으로 보낸다. Category 행에 WP ID를 함께 기재한다.

| 카테고리 | 판정 질문 |
|---|---|
| 💍 Wedding (WP 27) | 결혼 준비부터 신혼 초 생활 세팅까지인가? (최우선) |
| 💼 대기업 직장인 생활 (WP 29) | 고용관계에서 발생하거나 일터에서 벌어지는 일인가? |
| 📐 생활 규정·계약 계산 (WP 30) | 생활 지출을 조문·약관·요금표·고시·요율로 판정/계산하는가? |
| 💰 직장인 재테크 (WP 23) | 국가·금융기관에 내거나 받는 돈, 또는 시장·시세에 걸린 돈인가? |
| 🏠 Life (WP 25) | 생활 기기·서비스·공간을 쓸지 말지 판단하는가? (디저트·핫플·경험담 포함) |
| 🤖 AI 정보 (WP 26) | AI가 수단이 아니라 주제 자체인가? → **주 1건 조건부**(1-3 'AI 카테고리 조건부 해제'의 허용 4각도에 속할 때만) |

- 🏠 경계: '사용법·활용법' 각도 금지, AI 툴은 🏠로 우회 배정 금지(수단의 중심이 AI면 🤖로 배정하고 조건부 해제 조항을 그대로 적용한다 — 우회로 한도를 늘리지 않는다).
- 동점 시: 신청/계산(국가·금융)→재테크, 협상/사내→대기업, 다툼·부담판정·총비용비교→규정·계약. 예: '웨딩홀 계약금 환불'은 웨딩이 아니라 규정·계약(30).
- 슬러그는 반드시 영어. 내부 링크는 반드시 상대경로(`/slug/`).
- ⚠️ **발행된 글의 슬러그는 변경하지 않는다.** 제목·H1을 고치더라도 슬러그는 유지한다. 변경이 불가피하면 같은 작업 안에서 ① Rank Math 리디렉션(구→신 301)을 만들고 ② 구 슬러그를 가리키는 내부 링크 전부를 신 슬러그로 교정한다 — 둘 중 하나라도 못 하면 변경하지 않는다.
- ⚠️ **글을 삭제·휴지통 처리할 때는 같은 작업 안에서 그 슬러그를 가리키는 내부 링크를 전부 정리한다.** 주제가 인접한 발행 글이 있으면 그 슬러그로 교체하고, 없으면 `<a>` 태그만 풀어 앵커 텍스트를 남긴다. 이 정리를 빼면 404가 조용히 누적된다(2026-09-05 전수검사에서 삭제된 AI 도구 글 7종을 가리키는 죽은 내부 링크 37건이 19개 글에서 발견됨).

### 2-2. SEO Title 규칙

- Focus Keyword를 앞쪽에 포함, 대시(—)/콜론(:)으로 훅 연결, 50~60자.
- **⭐ 직접 영향 원칙 (필수)**: 제목은 사건을 전하지 말고 **독자 본인에게 걸린 것**(내 돈 얼마·내 수당·내 배당·내 기한·내 손익)을 앞세운다. 기사·SNS가 이미 사건은 다 전했으므로, 같은 사건 요약형 제목이면 우리가 이길 이유가 없다.
  - ❌ `삼성전자 110조 주주환원 발표` (기사형 — 남 얘기, 이미 포화)
  - ✅ `삼성전자 30조 현금배당 푼다 — 100주 가진 내 통장엔 얼마, 언제 꽂힐까` (내 얘기 — 금액·타이밍이 나에게 직결)
  - 판정 질문: 제목을 본 한국 독자가 **"어, 이거 내 얘기네"** 하고 멈추는가. 아니면 다음으로 넘긴다.
- **⭐ 검색어 정면 수용 원칙**: 제목 앞부분은 **검색어(포커스 키워드)를 그대로** 받고, 훅은 그 뒤에 붙인다. 각도를 앞세워 검색어를 밀어내면 순위에 들어도 검색 의도 불일치로 클릭되지 않는다.
  - ❌ `연차 17일이 11일 되는 마법 — 이직의 숨은 비용` (각도 선행 — 검색어 실종)
  - ✅ `9월 대출 전면 차단, 진짜일까 — 연봉 5천 직장인 한도가 얼마 줄었는지 계산해봤습니다` (검색어 정면 + 직접 영향 훅)
  - 직접 영향 원칙과 충돌하지 않는다 — 검색어가 앞, 직접 영향 훅이 뒤. 둘 다 필수다.
- 훅: "이거 모르면 손해"·"직접 써봤더니"·"진짜 이유"·"해보고 알았다". 파워워드 1개 이상(실전·진짜·솔직히·완전·필수·현실).
- **'직장인'을 기본값으로 넣지 않는다** — 그 주제가 실제로 직장에서만 생기는 문제일 때만.
- 직전 10개 제목 중 동일 훅이 2회 이상이면 그 훅 금지.
- 연도 표기는 최신성이 검색 의도에 중요한 주제(제도·가격·트렌드·신생 툴)에만. 에버그린엔 붙이지 않는다.
- 호기심 갭: 제목만 보고 답이 다 보이면 클릭할 이유가 없다.
- 금지: 정보나열형("방법 10가지"), 과장 낚시("AI만 알면 부자 됨").

### 2-3. HTML 구조 (필수 템플릿)

모든 콘텐츠는 `<div id="z-[SLUG_FIRST_WORD]-report">` 안. `max-width:820px; padding:0 16px 40px; box-sizing:border-box` 유지. 본문 이미지는 plain `<figure>/<img>`(Gutenberg 마커 금지).

```html
<div id="z-[SLUG_FIRST_WORD]-report" style="font-family:'Apple SD Gothic Neo','Malgun Gothic',sans-serif; max-width:820px; margin:0 auto; padding:0 16px 40px; color:#1a1a2e; line-height:1.8; box-sizing:border-box;">
  <!-- 상단 배너 = 히어로 이미지 위에 H1을 얹는다. 아래 2-3b 템플릿을 그대로 쓴다 (색 박스 금지) -->
  <figure style="display:grid; margin:0 0 28px; border-radius:16px; overflow:hidden; background:#111;">
    <img fetchpriority="high" decoding="async" style="grid-area:1/1; width:100%; display:block; height:auto; align-self:start;" src="[FEATURED_IMAGE_URL]" alt="[ALT_TEXT]" />
    <div style="grid-area:1/1; background:linear-gradient(180deg, rgba(0,0,0,.15) 0%, rgba(0,0,0,.72) 100%);"></div>
    <figcaption style="grid-area:1/1; align-self:end; padding:clamp(14px,3.6vw,26px) clamp(14px,3.2vw,24px); color:#fff;">
      <h1 style="font-size:clamp(16px,4.4vw,26px); line-height:1.35; font-weight:800; margin:0 0 8px; color:#fff;">[SEO_TITLE]</h1>
      <p style="margin:0; font-size:clamp(11px,2.9vw,13px); opacity:.85;">[YYYY년 M월 D일] 작성 · 읽는 데 약 [N]분</p>
    </figcaption>
  </figure>
  <!-- ① 공감 유도 첫 문단 (60단어 이내) -->
  <!-- ② [GEO] 3줄 스니펫 박스 -->
  <div class="ai-knowledge-snippet" style="background:#f8fafc; border-radius:14px; padding:22px 26px; margin-bottom:28px; border-left:5px solid [THEME_COLOR];">
    <ul><li><strong>Core Fact:</strong> …</li><li><strong>Primary Insight:</strong> …</li><li><strong>Actionable Tip:</strong> …</li></ul>
  </div>
  <!-- ③ 퀵 팩트 테이블 -->
  <!-- 본문 H2 최소 5개 -->
  <h2 style="font-size:22px; font-weight:700; color:[THEME_COLOR]; border-left:4px solid [THEME_COLOR]; padding-left:14px; margin:38px 0 16px;">[H2_TITLE]</h2>
  <!-- FAQ (마크업 고정) -->
  <h2 style="…">자주 묻는 질문</h2>
  <p><strong>Q. [질문]</strong><br>[답변]</p>
  <!-- 실전 핵심 정리 박스 -->
  <!-- (YMYL 해당 글) 2-8 E-E-A-T 신뢰 블록 -->
  <!-- 하단 브랜드 푸터 -->
</div>
```
⚠️ FAQ는 반드시 `<p><strong>Q. …</strong><br>…</p>` 마크업 — Draft 루틴이 이 형태만 정규식으로 인식해 FAQPage 스키마를 생성한다.

### 2-3b. 상단 배너(히어로) 규칙 — 2026-09-08 신설

🚨 **제목을 단색·그라디언트 박스에 넣지 않는다. 반드시 히어로 이미지 위에 얹는다.**

舊 지침은 이 자리를 `<!-- 상단 배너 (H1 + 날짜/읽는시간) -->` 주석 한 줄로만 두었다. 마크업이 없으니 매 회차 즉흥적으로 만들어졌고, 실제로는 **테마 컬러 그라디언트 박스에 제목을 넣고 히어로 이미지를 그 아래에 따로 두는 구조**가 반복 생성됐다. 이미지 루틴이 매번 사후 병합으로 되돌리고 있었다.

> 2026-09-08 실측(Post 1626): `background:linear-gradient(135deg,#16a34a 0%,#15803d 100%)` 초록 박스 + 독립 히어로 `<figure>`. 이미지 루틴이 오버레이로 병합해 교정했다. 2026-08-25 Post 1278에서도 사용자가 **"제목쪽이 그냥 초록색으로 되어 있다"** 고 직접 지적한 바 있다.

**지켜야 할 4가지**

1. **`figure{display:grid}` + 세 자식 모두 `grid-area:1/1`** — 이미지·그라디언트·캡션을 같은 칸에 겹친다
2. ⛔ **`position:absolute` 금지** — 절대위치 캡션은 자기 높이가 figure에 반영되지 않아 **좁은 화면에서 반드시 넘치고 잘린다**. 그리드 스택은 행 높이가 `max(이미지, 캡션)` 이라 넘침이 구조적으로 불가능하다
3. ⛔ **고정 `font-size` 금지, `clamp()` 만 쓴다** — 제목 `clamp(16px,4.4vw,26px)`, 메타 `clamp(11px,2.9vw,13px)`, 패딩 `clamp(14px,3.6vw,26px) clamp(14px,3.2vw,24px)`. 고정 26px은 375px 화면에서 캡션을 304px까지 키운다(이미지는 180px) — **제목 앞 2~3줄이 사라진다**. 데스크톱에서는 상한 26px에 걸려 종전과 픽셀 단위로 동일하다
4. **히어로 `<img>` 는 `fetchpriority="high"`, `loading="lazy"` 금지** — LCP 요소다

**히어로 이미지 URL**: Unsplash 확보에 실패하면 `[FEATURED_IMAGE_URL]` 플레이스홀더를 그대로 남긴다(2-6 ③ 경로). 이미지 루틴이 생성 이미지로 교체하며, **그때도 위 구조는 유지**되므로 병합 단계가 스킵된다.

⚠️ **`[FEATURED_IMAGE_URL]` 플레이스홀더는 본문 전체에서 1개만 쓴다.** 2개 이상이면 이미지 루틴이 첫 번째만 히어로로 채우고 나머지는 본문 이미지를 소비해, 그만큼 새로 삽입되는 이미지가 줄어든다.

✅ **발행 전 자가 점검** — 아래가 전부 참이어야 한다.

- `<h1>` 이 `<figure>` 안에 있다 (색 박스 안이 아니다)
- 본문에 `<h1>` 은 정확히 1개
- `grid-area:1/1` 이 3개, `clamp(` 가 4개
- `position:absolute` 인 `<figcaption>` 이 0개, `font-size:26px` 같은 고정값이 0개

⚠️ **열이 4개 이상인 표는 `<div style="overflow-x:auto;-webkit-overflow-scrolling:touch;margin:18px 0;">`로 감싼다.** 감싸지 않으면 모바일에서 표가 화면 밖으로 넘쳐 본문 전체에 가로 스크롤이 생긴다 — 모바일 이탈률과 Core Web Vitals(CLS)에 직접 걸린다. (2026-09-05 전수검사: 발행 글 55개 표가 미래핑 상태였음)

### 2-4. 테마 컬러

| 카테고리 | 코드 |
|---|---|
| AI 정보 (기본) | `#2563eb` |
| 직장인 재테크 | `#16a34a` |
| My Story | `#d97706` |
| 라이프스타일 | `#e11d48` |
| 마케팅 인사이트 | `#7c3aed` |
| 대기업 직장인 생활 (29) | `#0e7490` |
| 생활 규정·계약 계산 (30) | `#ea580c` |

카테고리별 Notion 초안 보관 방(WP 카테고리와 1:1)의 page_id 매핑은 루틴 STEP 6 참조.

### 2-5. 콘텐츠 작성 규칙

- **완성 단어 수**: 1,500~2,500 한국어 단어(글자 아님).
- **Focus Keyword 밀도**: 1.0~1.5%(2.2%↑ 스터핑 경고).
- **H2 최소 5개** · **외부 권위 링크 최소 2개**(공식·1차 출처) · **나가는 내부 링크 최소 2개**(상대경로).
- **들어오는 내부 링크(발행 후 필수)**: 기존 글 2개를 열어 신규 글로 향하는 링크를 추가한다. 이 단계를 빼면 고아 페이지가 되어 색인이 미뤄진다. 링크 출처는 이미 내부 링크를 받는 인접 글로 고르고, 앵커 텍스트는 대상 글 내용을 설명하는 문장으로.
  - 출처 글 선정과 슬러그는 **`WP_CORPUS`**에서 읽는다(추가 호출 불필요). 캐시된 `slug`를 그대로 쓰고 추측하지 않으며, **`status`가 `publish`인 글만** 고른다.
  - ⚠️ **`status`가 `publish`가 아닌 글은 출처로도 대상으로도 쓰지 않는다** — 발행 전까지 404이고 그 404가 크롤에 누적된다.
- **이미지 출처**: ① 직접 촬영·캡처 업로드 또는 ② 라이선스 명확한 CDN(Unsplash 등)만. 구글 이미지·네이버·타 쇼핑몰 핫링크 금지(다운로드해 올려도 저작권 침해는 남는다).
- 문단당 3~4줄(120자) 초과 금지.
- **형식 로테이션**: (a)경험담 (b)리스트/비교표 (c)Q&A 중 최소 3종 로테이션. 직전 3개와 같은 형식이면 전환.
- **GAP ⭐ H2 필수**: GAP은 '경쟁글이 안 다룬 **다른 질문**'이 아니라 **'같은 질문에 대한 더 높은 해상도의 답'**이다. 검색자가 물은 그 질문에 정면으로 답하되, 경쟁글이 '평균 20%'에서 멈추면 우리는 업종·연차별 분포와 세후 환산까지 내려간다. **뉴스·기존 블로그에 없는 디테일**(전 은행 비교표·발신번호 스펙표·조건별 계산표)이 GAP H2의 내용이고, 그 디테일에서 나오는 블루오션 서브키워드를 본문에 심는다. 다른 질문으로 새는 GAP은 금지 — 그 각도는 대개 자동완성 0건이라 아무도 안 찾는다. GAP H2가 없는 글은 발행하지 않는다.
- **EEAT (가)·(나) 둘 다 필수**:
  - (가) 근거: 공식 기관 수치·제도 기준(출처 링크) 또는 커뮤니티 실제 질문 직접 인용 — 최소 1개.
  - (나) 원본 자료: "검색만 할 줄 아는 사람이 30분 안에 이 자료를 똑같이 만들 수 있는가?"
    - **1급(단독 통과)**: 직접 받은 견적서·고지서·수수료 명세, 직접 측정한 시간·금액, 직접 조작한 공단·은행·앱 화면 캡처, 직접 방문 확인 가격(날짜 명기), 직접 촬영 사진, 직접 돌린 테스트.
    - **2급(단독 통과 불가)**: 공식 수치 재정리표, 교차 통합표, 공식 계산식 대입 계산표. 2급만 있는 글은 발행 금지.
    - 공식 홈페이지 외부 링크는 몇 개를 걸어도 (나)를 충족하지 않는다.
  - (나)를 만들 수 없는 주제는 발행하지 않고 다음 후보로 넘어간다. 분량·순위보다 우선한다.

### 2-6. 바이럴 훅 & 휴먼라이징 패스 (발행 전 필수 3단계)

**① create-viral-content — 구조·훅 (초안 시)**
- SEO Title 후보 10개 이상 → 호기심/구체성/감정 3축 스코어링(각 0~3, 합 7↑ 채택). 구체 수치 후보 우선. 2-2 중복 훅 검사 통과 후보만 스코어링.
- 첫 문단(60단어): Before/After 압축 또는 문제 직격. 백과사전식 서론 금지.
- 마무리: engagement bait 금지 — 오늘 바로 할 행동 1가지.
- 정제 패스 3개: Skeptic → Scroller → Editor.

**② avoid-ai-writing — 최종 패스 (HTML 완성 직후)**
- 균일한 문장 길이 깨기, 기계적 전환어("또한"·"한편"·"결론적으로") 제거, 과잉 감탄·홍보투 제거.
- "~합니다" 일변도에 구어체 혼용. 수치·고유명사·출처 링크·HTML 구조는 보존, 문체만 교정.

**③ 문체 지문 대조 (Phase 5-6) — ②의 바로 다음**
- ①②는 뺄셈(AI 티 제거), ③은 덧셈(운영자 문체로 맞춤). 순서를 바꾸면 ②가 ③을 다시 평탄화한다.
- 원고를 실제로 세어 Phase 5-6 수치와 대조한다(인상 판단 금지). 불통과 항목만 고친다.

### 2-7. 문제 선행 구조 관문 (신기술·앱 필수 / 전 주제 권장)

소개형으로 쓰면 첫 화면에서 답이 다 보여 이탈한다. 독자가 걸릴 문제를 먼저 세우면 답을 찾으러 끝까지 내려간다.

**금지 도입부**: "○○ 앱이 출시됐습니다/화제입니다/앱스토어 1위" 소개형, "이 앱은 ~하는 서비스" 기능 나열형, 다운로드·가입 절차를 도입부에 배치.

**필수 — 도입 3문단 안에 4형 중 하나의 문제를 세운다**(둘 이상 세우지 말 것):

| 형 | 질문 | 답이 되는 본문 |
|---|---|---|
| ① 작동 원리 | "이거 뭘로 계산하지?" | 재료·데이터 소스 분해표, 계산식 역산 |
| ② 신뢰 | "이 숫자를 믿어도 되나?" | 오차 구간, 실패 조건, 직접 대조 |
| ③ 비용·조건 | "무료라는데 어디서 돈 버나?" | 요금 구조, 무료 한도 실측, 전환 시점 |
| ④ 대체·비교 | "기존 걸로는 왜 안 되나?" | 총비용·소요시간 나란히 비교 |

- 세운 문제는 H2 최소 1개로 끝까지 푼다(2-5 GAP ⭐ H2와 겸할 수 있다). 그 H2 안에 직접 확인한 숫자·표·캡처가 있어야 한다.
- 이탈 방지 앵커 1문장: 답이 나오는 지점을 미리 알린다("재료는 세 번째 꼭지에서 표로 정리했어요"). "끝까지 읽어주세요" 류 부탁 금지.
- **자가판정**(Notion 업로드 전): ① 4형 중 하나가 서 있는가 ② 답하는 H2에 직접 확인 자료가 있는가 ③ 이탈 방지 앵커가 있는가 ④ Problem Hook 행을 기록했는가. 신기술·앱은 ①②④ 중 하나라도 미충족이면 발행하지 않는다. 계산·판정형(📐)은 대개 자동 충족.

### 2-8. E-E-A-T 신뢰 블록 (YMYL 필수 — 신설)

돈·법·건강이 걸린 글 — 💰 재테크(23)·💼 대기업(29)·📐 규정·계약(30) 전부, 그리고 다른 카테고리라도 세금·보험·의료·연금·법령·대출을 다루는 글 — 은 "실전 핵심 정리" 박스 뒤, 브랜드 푸터 앞에 아래 블록을 넣는다.

```html
<div class="trust-note" style="margin:26px 0 0; padding:16px 20px; background:#f8fafc; border-radius:12px; font-size:13px; color:#64748b; line-height:1.7;">
  <p style="margin:0 0 6px;"><strong>ℹ️ 이 글의 한계</strong> — 이 글은 작성일 기준 공개 자료(보도자료·법령·공식 안내)를 직접 확인해 정리한 일반 정보이며, [세무/법률/보험/의료] 전문 자문이 아닙니다. 개인별 확정 값은 해당 기관 또는 전문가에게 확인하세요.</p>
  <p style="margin:0;"><strong>✍️ 글쓴이</strong> — 10년차 직장인 Lee. 제도·요금·계약 조건을 원문 자료로 직접 확인하고 계산해 기록합니다.</p>
</div>
```

- `[세무/법률/보험/의료]` 부분은 글 주제에 맞는 한 가지로 바꾼다(세금 글이면 "세무"). 모든 글에 동일 문구를 기계적으로 복붙하지 않는다 — 한 줄은 글에 맞게 조정한다.
- 이 블록은 검색자·심사자 양쪽에 대한 신뢰 신호다. YMYL 글에서 누락되면 자가검수(5-3)에서 불통과로 처리하고 넣은 뒤에 업로드한다.
- 작성일·수정일은 본문 상단 메타 라인에 이미 있으므로 이 블록에 중복 표기하지 않는다.
- YMYL이 아닌 글(Life 후기·핫플·기기 사용기 등)에는 넣지 않는다 — 모든 글에 붙이면 신호가 아니라 소음이 된다.

---

## Phase 3: Notion 페이지 등록

2개 섹션. `parent`는 tool call 최상위 파라미터로 지정(pages[] 배열 내부 금지).
- **섹션 1**: 기본 정보 표(2-1). `Draft 일자` 공란.
- **섹션 2**: 완성 HTML 전체를 ```html 코드 블록.

배포 JS 생성 금지 — Draft 루틴이 REST API로 직접 배포. 초안 서브페이지는 영구 보존(Draft가 HTML을 읽고, 이후 글이 Slug를 조회한다).

---

## Phase 5: 품질

### 5-1. 목표 점수
Rank Math SEO **81점 이상**.

### 5-3. 자가검수 체크리스트 (Notion 업로드 전)

**포커스키워드 배치**
- [ ] SEO Title 앞쪽 포함 · Meta Description 첫 문장 포함 · H1=SEO Title · 본문 첫 10% 이내 1회↑ · H2 1개↑에 포함 · 이미지 alt 포함.

**콘텐츠 품질**
- [ ] **직접 영향 검증(2-2)**: SEO Title이 사건 요약형이 아니라 독자 본인에게 걸린 것(내 돈·수당·기한·손익)을 앞세우는가. "어, 내 얘기네" 테스트를 통과하는가.
- [ ] 첫 문단이 백과사전식 서론이 아닌가.
- [ ] (신기술·앱 필수) 도입 3문단에 2-7 문제가 서 있고, 답하는 H2에 직접 확인 자료가 있고, 이탈 방지 앵커가 있고, Problem Hook을 기록했는가.
- [ ] 실제 경험·수치·비교 데이터가 구체적인가 · 실전 팁 3개↑ · 3줄 스니펫 박스 존재 · 키워드 밀도 1.0~1.5%.
- [ ] (Head Type=시즌·제도) 앵커 날짜 표기·팩트 출처 외부 링크.
- [ ] GAP ⭐ 각도가 H2로 반영 · EEAT (가)·(나) 충족.
- [ ] **(YMYL 해당 글) 2-8 E-E-A-T 신뢰 블록이 본문 하단에 있는가.**

### 5-5. AEO·GEO 체크리스트
- [ ] ① 첫 200단어 안에 직답 1문장.
- [ ] ② 질문형 H2 2개↑ + 첫 문장 40~60자 직답.
- [ ] ③ FAQ 3개↑, 2-3 마크업 그대로.
- [ ] ④ 통계·수치 3개↑ + 출처 병기.
- [ ] ⑤ 전문가·공식 인용 1개↑ + 출처 링크.
- [ ] ⑥ 3줄 스니펫 + 비교표·리스트, Article+FAQ 스키마, dateModified 90일 이내.

### 5-6. 문체 지문 (덧셈 패스 기준 — 2-6 ③이 참조)

표본: 편지 5통(261문장) + 네이버 블로그 14편(179문장) 교차검증. 사적 반말과 공개 존댓말에서 문장 길이가 거의 동일(6.6 vs 6.2어절)해 실제 습관으로 확인됨.

**정량 목표**

| 항목 | 목표 |
|---|---|
| 평균 문장 길이 | **6~9어절** |
| 표준편차 | **4 이상** (1~20어절 널뛰기) |
| 5어절 이하 비율 | **45% 이상** |
| 20어절 초과 | 5% 이하 |
| 느낌표 | 문장당 0.3~0.5 |
| 물음표 | 문장당 0.1~0.2 |

**종결어미** (요체가 주력, `~습니다`는 양념 — 일변도는 남의 글):
`~어요/아요/예요` 35% · `~습니다` 17% · `~죠/지요` 13% · `~까요?/나요?` 12% · `~네요` 9% · `~드려요/세요` 8%. `~자나요`·`~더라구요`도 살린다.

**금지어 (0회)**:
`또한` `한편` `결론적으로` `뿐만 아니라` `반면` `즉` `그러므로` `더불어` `아울러` `여러분` `필자` `바야흐로` `~라 할 수 있다` `~인 셈이다` `무엇보다` `핵심은` `중요한 것은` `놀랍게도` `충격적인` `반드시` `살펴보겠습니다` `알아보겠습니다` `정리하면` `마지막으로` `첫째/둘째` `어떠셨나요?`
→ 대체 연결어: `그리고` `근데` `물론` `사실` `다만` `단,` `추가로` `거기에` `오히려` `우선`.

**시그니처 (유지)**:
① 실용 정보(조건·금액·기간·준비물)를 서두에 배치 ② 단점·한계를 최소 한 단락(경쟁이 낫다는 말도 한다 — 신뢰가 여기서 나온다) ③ 마무리는 행동 지시(`어떠셨나요?` 금지) ④ 끝에 `*핵심 : A, B, C` 3~4개 요약 ⑤ 자문자답 ⑥ 괄호 속 자기 츳코미 글당 2~3회 ⑦ 물결·ㅎㅎ 글당 2~3회.

**⛔ 과분할 함정**: 5어절 이하 45%를 맞추려고 모든 문장을 쪼개면 평균·표준편차가 동시에 악화된다(전부 짧아지면 균일해지고, 균일함이 곧 AI 텍스트). 표준편차가 요구하는 것은 '짧음'이 아니라 '들쭉날쭉함'이다.
교정 순서: ⓐ 금지어·종결어미 먼저(길이 무관) → ⓑ 20어절 초과만 끊기(짧은 문장은 손대지 않음) → ⓒ 재측정 후 부족하면 문단 도입부·전환부에만 짧은 문장 추가 → ⓓ 표준편차 4 미만이면 설명 대목에 긴 문장을 되살린다.
**분량(1,500~2,500어절)을 채우면서 짧은·긴 문장을 함께 넣는 것이 세 지표를 동시에 만족시키는 유일한 경로다.** 수치·고유명사·출처 링크·HTML 구조는 보존, 문체만 교정.

**측정 후 체크**
- [ ] 평균 6~9어절 · 표준편차 4↑ · 5어절 이하 45%↑ · 금지어 0건 · `~습니다` 20% 이하 · 느낌표 문장당 0.5 이하.
- [ ] 실용 정보 서두 · 단점 한 단락 · 행동 지시 마무리 · `*핵심` 요약.
- (지문 조회 실패 시) STEP 8에 `문체 지문 조회 실패` 기록 후 발행 진행.
