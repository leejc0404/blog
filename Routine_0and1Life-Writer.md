[루틴 : 0and1life-Writer — 웹 전용, 하루 N건 슬롯 + 포착면 + 재시도 가드]

*예약된 트리거 시간이 되면 해당 일자에 먼저 루틴이 실행되었더라도 무조건 다시 진행한다(가드가 중복을 막는다).
날짜: 실행 시점의 실제 KST 날짜를 사용한다.

⚠️ **역할 경계 — 이 루틴은 도구다.** 판정 기준·수치·품질 기준의 단일 기준(SSOT)은 전적으로 GitHub 작성 가이드(`0and1Life-Writer.md`)다. 이 루틴은 **운영 절차**(언제·무엇을·몇 회·어디에 기록)와 **실행 자산**(Notion 페이지 ID·API 경로·CPC 기준표·리포트 형식)만 정의한다.
- ⛔ 가이드의 수치·판정 기준을 이 루틴에 복사하지 않는다. 규칙을 가리킬 때는 **조항 번호로 참조**한다(예: "가이드 0-6 채점표를 그대로 적용").
- ✅ 규칙을 바꿔야 하면 **가이드를 고친다. 루틴은 고치지 않는다.** 루틴 수정은 절차·도구·출력 형식이 바뀔 때뿐이다.
- 상충이 발견되면 **가이드를 따르고** STEP 8 리포트에 기록해 사용자에게 알린다.

⚠️ **실행 환경**: Claude Code on the web — Chrome MCP 없음. 브라우저 의존 절차 전면 금지. 모든 자동완성·트렌드·커뮤니티 수집은 WebFetch/Bash(예산 미소모)로 시도한다. 조직 네트워크 정책상 WebFetch가 외부 도메인 전체에 대해 차단될 수 있다 — 차단 판정은 STEP 0에서 세션당 1회 수행하고 이후 모든 STEP이 그 결과(WEBFETCH_STATUS)를 그대로 쓴다. 403/차단 확인 시 즉시 WebSearch로 대체하며, 이는 정상 대체 경로이지 오류가 아니다(매번 보고하지 않는다).

⚠️ **자동완성 API는 Bash 경로를 쓴다.** WebFetch는 구글 자동완성 API의 EUC-KR 응답을 깨뜨려 한글 판독이 불가하다. WEBFETCH_STATUS가 정상이어도 자동완성 수확은 `curl` + Python `euc-kr` 디코드로 돌린다(예산 미소모 동일).
```
curl -s --max-time 15 "https://suggestqueries.google.com/complete/search?client=firefox&hl=ko&q={URL인코딩 키워드}"
→ 응답 바이트를 euc-kr → cp949 → utf-8 순으로 디코드 후 JSON 파싱, [1] 배열이 제안어
```

⚠️ **차단 확정 — 처음부터 접속하지 않는다**: 디시인사이드 · 클리앙 · help.3o3.co.kr · jobkorea.co.kr · law.go.kr · labor.moel.go.kr · unsplash.com(검증만) · korea.kr RSS(`/rss/policy.xml` 404) · 긱뉴스 · nate/news.nate.com · 뽐뿌 · 더쿠 · 앱스토어 순위(itunes/apps.apple.com/rss.marketingtools 전부) · Google News RSS · 한경/매경 RSS · trends dailytrends API · signal.bz · 네이버 데이터랩. 이 목록은 관찰되는 대로 추가한다.

🎯 **타겟 독자**: 한국의 2030~40대 초 직장인 — 돈·재테크·일·결혼·AI가 핵심 관심사. 모든 주제 판단 기준은 "이 독자가 네이버·구글에 실제로 검색할까?"다.

✍️ **화자 기준**: 독자와 눈높이가 같은 30~40대 화자. 가공된 신상(나이·직군·연차·결혼 준비 등)을 사실처럼 서술 금지. 1인칭(저/제가)은 이 글을 쓰며 실제로 수행한 행위에만 쓰고 그 결과물(계산·캡처·비교표)을 본문에 함께 싣는다. 하지 않은 일은 3인칭·2인칭으로. 비경험 영역(육아 등)은 주제에서 영구 제외. 상세는 가이드 2-6.

📂 **확정 카테고리** (WP ID):
💰 직장인 재테크(23) / 💼 대기업 직장인 생활(29) / 📐 생활 규정·계약 계산(30) / 🤖 AI 정보(26 — **주 0건 동결**) / 🏠 Life·My Story(25) / 💍 Wedding(27)
주간 한도·3연속 금지·순환 하한은 **가이드 카테고리 균형 규칙 R1~R5**를 그대로 따른다(수치는 루틴에 적지 않는다).

⚠️ **WebSearch 사용 — STEP 4-B 전용 · 글당 최대 4회.** 순서: ① 1순위 후보 공백 실측 1회(필수) → ② 1순위 포화면 2순위 비교 1회 → ③ 둘 다 포화면 재타겟 각도 검증 1회 → ④ 예비 1회. STEP 2(브레인스토밍)와 STEP 4-A(수요검증)는 WebSearch를 쓰지 않는다 — 자동완성 API가 실존 검색 문구를 그대로 주는 더 정확한 도구이기 때문이다(서브키워드는 실존 문구만 허용). 막혀 있으면 발행 이력·모델 지식 기반 대체 신호로 판단하며 WebSearch로 보완하지 않는다. 관리 단위는 '횟수'가 아니라 '컨텍스트 비용'이다 — 필요 없는 검색은 안 하되, 포화 후보를 밀어붙이려고 검색을 아끼지는 않는다.

품질 관문은 어떤 경우에도 유지: 서브키워드는 자동완성 또는 검색 결과에 실제 노출된 문구만(AI 임의 조합 금지), GAP ⭐ H2 필수, EEAT 강제 조건 불변. 후보 탈락 시 새 주제를 즉흥 발명하지 않고 STEP 3 숏리스트/예비 리스트 순위로만 이동한다. STOP은 STEP 3·4-A에서 채택 가능한 후보가 완전히 0개가 됐을 때만 발생한다.

🔢 **하루 발행 건수 — `DAILY_TARGET = 2`** (값을 바꾸려면 이 줄만 고친다. 1로 되돌리면 하루 1건)
- `TODAY_COUNT` = 오늘 현황표(양 계열 합산)에 이미 기록된 `트렌드 초안 작성 실행: {TODAY_KST}` **개수**
- `SLOT` = `TODAY_COUNT + 1` — 이번 실행이 오늘 몇 번째 글인지. 모든 STEP이 이 값을 참조한다

**슬롯 운영** (주제 우선순위 판정 자체는 가이드 0-1이 SSOT):
- **모든 슬롯은 가이드 0-1 우선순위(스파이크 > 시기성 ≤14일 임박 롱테일 > 에버그린)를 그대로 따른다.** 요일·홀짝을 보지 않는다.
- **슬롯 1**은 가장 이른 트리거이자 선점 자리다. 그날 가장 신선한 스파이크를 여기에 태운다.
- **슬롯 2 이상**도 0-1을 따른다. 슬롯 1이 이미 그날 최상위 스파이크를 가져갔으므로, 슬롯 2는 대개 다음 순위(다른 스파이크 / 임박 롱테일 / 백로그·에버그린)로 내려간다 — 자연히 백로그 소진 자리가 된다(GSC 대기 ≤35건 목표). 슬롯 2에서 슬롯 1보다 더 신선한 스파이크를 발견했으면 백로그에 적재하고 **다음 날 슬롯 1**로 올린다(선점 자리를 뒤로 미루지 않는다).

⚠️ **같은 날 슬롯 간 충돌 방지 — 3개 전부 강제**:
1. **카테고리가 서로 달라야 한다**(R2-D). 같은 날 두 글이 같은 카테고리면 7일 창을 하루에 2칸 채우고 R2에 바로 걸린다.
2. **클러스터가 서로 달라야 한다.** 같은 날 두 글이 같은 조문·계산표·이벤트를 다루면 자기잠식한다.
3. **앞 슬롯 산출물을 반드시 읽고 중복 대조에 포함한다**(STEP 1·3) — 방금 추가된 현황표 행이라 놓치기 쉽다.

⚠️ **동시 실행 금지 — 번호 충돌의 유일한 원인**: MAX_NUM과 TODAY_COUNT는 각 실행의 STEP 0-⓪ 시점에 읽는다. 두 슬롯이 겹쳐 돌면 둘 다 같은 번호를 잡아 현황표가 깨진다. 트리거 간격은 **최소 3시간**을 둔다(1회차 소요 실측 25~30분).

---

STEP 0-⓪ — 중복 실행 방지 가드 (모든 STEP보다 먼저 실행)

1차 트리거와 재시도 트리거가 같은 프롬프트를 공유한다. 아래 가드가 재시도를 정상일에는 무동작으로, 미달일에만 실제 실행으로 만든다.

① **오늘 날짜 고정**: `TZ=Asia/Seoul date '+%Y-%m-%d'`로 KST 날짜를 확정해 `TODAY_KST`로 둔다. 이후 모든 STEP이 이 값을 쓴다(재판정 금지).

② **현황표 두 계열을 먼저 fetch한다**:
- `[0]` 계열 — `37cbfe4a-2ae1-819f-8664-ff3d38fffe56`
- `[1]` 계열 — `37cbfe4a-2ae1-81b9-a9ce-d0b937edd344`

⚠️ 여기서 읽은 결과를 **STEP 1에서 재사용한다**(같은 페이지를 두 번 fetch하지 않는다). MAX_NUM은 **양 계열 합산**이다 — `[0]`만 보고 번호를 잡지 않는다.

③ **오늘 몇 건이 끝났는지 센다** (판정 기준은 가이드 `0-7 ①` — 현황표 로그가 유일한 기준, 초안 페이지 존재 여부로 판정하지 않는다):
**양 계열 합산**으로 `트렌드 초안 작성 실행: {TODAY_KST}` 문자열의 **출현 횟수**를 세어 `TODAY_COUNT`로 둔다.
- `TODAY_COUNT >= DAILY_TARGET` → **즉시 종료.** 한 줄로 끝낸다: `⏭️ {TODAY_KST} 오늘 목표 {DAILY_TARGET}건 완료 — 무동작 종료 (#XX·#YY)`. 종료 시 아무것도 쓰지 않는다(현황표·백로그·반려 로그 수정 금지, 알림 금지).
- `TODAY_COUNT < DAILY_TARGET` → `SLOT = TODAY_COUNT + 1`로 두고 전체 루틴을 정상 실행한다.

④ **`RETRY_MODE` 판정 — 슬롯 1에만 적용.** `RETRY_MODE = true`는 `SLOT = 1`이면서 그 슬롯의 예정 트리거 시각이 이미 지났을 때만 세운다(뒤 시간대 트리거가 빈 슬롯 1을 메우는 경우). 슬롯 2 이상은 애초에 뒤 트리거이므로 재시도가 아니다. 이 표기는 1차 트리거 실패 빈도를 집계하기 위한 것이다(STEP 7 로그). 회차 누락 판정·기록은 가이드 `0-7`을 따른다 — 누락 판정은 '그날 0건'이 아니라 '그날 `DAILY_TARGET` 미달'로 읽는다. **누락에 대한 강제 시즌 채택 같은 보상 조치는 없다**(가이드 v2.0 — 다음 회차는 그날의 0-1 우선순위를 정상 실행한다).

⚠️ **이 가드는 잠금이 아니라 시차 확인이다.** 실행 로그는 STEP 7에서야 기록되므로, 1차 실행이 진행 중인 시점에 재시도가 뜨면 가드를 통과해 같은 날 글이 2개 만들어지고 번호가 충돌한다. 재시도 트리거는 1차보다 **최소 3시간 뒤**에 둔다.
⚠️ **재시도는 같은 KST 날짜 안에 끝낸다.** 자정을 넘기면 날짜가 바뀌어 그날 선점 기회가 사라진다(가이드 0-7). 재시도를 저녁으로 미루지 않는다.

📅 **예약 설정 (Cowork에서 직접 생성)**

| 트리거 | 시각 (KST) | 반복 | 담당 | 비고 |
|---|---|---|---|---|
| 1차 | ≈05:25 | 매일 | 슬롯 1 | 선점 자리 |
| 재시도 | 11:00 | 매일 | 슬롯 1 백업 | 1차 실패 시 슬롯 1을 메운다 |
| 슬롯 2 | 15:00 | 매일 | 슬롯 2 | 백로그 소진 자리 |
| 슬롯 2 재시도 | 20:00 | 매일 | 슬롯 2 백업 | 자정까지 4시간 — 더 늦추지 않는다 |

- **트리거를 몇 개 걸든 안전하다** — 가드가 `TODAY_COUNT >= DAILY_TARGET`이면 무동작 종료하므로, 이미 2건이 찼으면 나머지 트리거는 Notion fetch 2회만 쓰고 끝난다. 앞 트리거가 실패하면 뒤 트리거가 빈 슬롯을 자동으로 메운다.
- **간격은 최소 3시간**. 05:25 → 11:00 → 15:00 → 20:00은 이 조건을 만족한다.
- **프롬프트는 모든 트리거에서 완전히 동일한 것을 쓴다.** 재시도용 별도 프롬프트를 만들지 않는다(두 벌 관리가 상충의 원인).

⚠️ **이 가드가 막지 못하는 것 (정직 기록)**: 1차·재시도가 둘 다 실패하는 경우(같은 클라우드 환경이라 반나절 이상 장애면 둘 다 죽는다), 원인 자체(컨테이너 프로비저닝 실패는 플랫폼 사안이라 루틴 권한 밖 — 가드는 원인이 아니라 노출을 줄인다).

---

STEP 0 — 네트워크 상태 판정 (세션당 1회)

**WEBFETCH_STATUS**: 구글 자동완성 API(hl=ko) 1회 조회로 정상/차단을 확정하고, STEP 1~8이 이 값을 그대로 쓴다(재판정 금지). 이는 '조직 네트워크 차단' 여부 판정이며 개별 사이트 자체 실패(특정 RSS 타임아웃 등)와는 별개다 — WEBFETCH_STATUS=정상이어도 특정 도메인이 개별 실패할 수 있으며, 이 경우 전체를 차단으로 재판정하지 않고 해당 소스만 건너뛰고 나머지를 계속 진행한다.

⚠️ **트랙은 STEP 0에서 판정하지 않는다.** 트랙(S/L)은 요일이 아니라 STEP 2~3에서 채택 키워드의 3종 판별 결과로 정해진다(가이드 0-1·0-3). 홀짝·시즌 창·L_FALLBACK 판정은 없다.

STEP 1 — Read Reference Materials from GitHub (검색 없이 먼저 실행)

아래를 동시에 읽는다:
- **작성 가이드**: WebFetch → https://raw.githubusercontent.com/leejc0404/blog/main/0and1Life-Writer.md (반드시 준수)
- **전체 글 현황표**: STEP 0-⓪ ②에서 이미 fetch한 두 계열 결과를 재사용(재fetch 금지)
  → 기존 제목 전체 + 포커스 키워드 + 현재 최고 번호 MAX_NUM 확인(**양 계열 합산**)
  → 최근 2주 발행 패턴 확인(같은 계산법·같은 형식이 연속됐으면 재사용 자제)
  → **직전 날짜들의 실행 로그 존재 여부 확인** — 누락된 회차가 있으면 가이드 `0-7`에 따라 판정하고 STEP 8에 기록
  → **`SLOT >= 2`이면 오늘 앞 슬롯 산출물을 반드시 확인한다** — 제목·포커스 키워드·카테고리·클러스터(조문·계산표·이벤트)를 읽어 STEP 3 중복 대조와 카테고리 배정의 입력으로 쓴다
  → **`Head Type = 시즌` 글 목록 확인**(연도 갱신 대상 식별 — 가이드 1-5)
  → **가이드를 읽은 뒤 확인할 조항**: `0-1 주제 우선순위` / `0-2 포착면` / `0-3 3종 판별` / `0-4 시즌 캘린더` / `0-5 헤드 자격·앵커` / `0-6 채점·타이브레이커` / `0-7 회차 누락` / `1-T` / `1-5 연도 갱신`
- **키워드 백로그** (Notion): https://www.notion.so/3a9bfe4a2ae181e08fc9dc8e035388af
  → 상태='대기' 항목 전체 확인 — STEP 2 엔진 D의 입력. 상태='날짜 대기' 항목은 앵커 날짜 확정 여부를 먼저 확인하고, 확정됐으면(또는 이벤트가 D-14 이내로 들어왔으면) '대기'로 승격해 검토
- **발행 반려 로그** (Notion): https://www.notion.so/3adbfe4a2ae1817994f0f901de5c8dec
  → 최근 7일 행 확인 — 같은 사유 코드가 2회 이상 반복된 유형은 이번 회차 제외. '조달 주체=사용자' 미해결 행이 3건 이상이면 이번 회차는 루틴 조달 가능 주제만 선정(가이드 1-3 관문 입력)
- **최신 주간 GSC 리포트** (Notion, `398bfe4a-2ae1-81ba-9ec6-fdae2acd6b2d`): "## 리포트 (최신순)" 맨 위 섹션의 특이사항·다음 조치를 후보 우선순위에 반영

STEP 2 — Candidate Brainstorm (후보 10~15개 생성, WebSearch 0회 — 항상)

**요일을 보지 않는다. 매 실행 아래 엔진을 모두 돌려 가이드 0-1 우선순위(스파이크 > 임박 롱테일 ≤14일 > 에버그린)로 후보를 모은다.** 실행·우선 순서: 엔진 D(백로그) → 엔진 B(포착면 10소스 — 스파이크 최우선 소스) → 엔진 A(자동완성 확인·판별) → 엔진 C(시즌 캘린더 — D-14 이내 임박 이벤트만). 엔진당 최소 3개, 합계 10~15개.
WebFetch/네트워크 차단 시: 엔진 B는 성공한 소스만으로 진행(개별 실패는 스캔 로그에 기록), 전멸 시 생략을 스캔 로그에 명기. 엔진 C·A는 차단과 무관하게 가능.

**엔진 D — 키워드 백로그 (트랙 공통 최우선)**
STEP 1에서 읽은 '대기' 항목을 후보군 맨 앞에 배치한다.
- 출처=GSC쿼리 항목은 구글이 이미 노출시키는 실측 쿼리이므로 4-A 수요 검증 통과로 간주(STEP 3·4-B는 동일 적용).
- 출처=네이버AC·데이터랩 항목은 네이버 실측 신호이므로 4-A에서 구글 자동완성 미노출이어도 그 이유만으로 탈락시키지 않는다.
- 그 외 출처는 일반 후보와 동일하게 4-A를 거친다.
- 채택되면 STEP 7 완료 후 해당 행 상태를 '채택 #XX'로, 탈락하면 '탈락(사유)'로 갱신(re-fetch 후 old_str 구성).
- 백로그가 비었거나 전부 부적합이면 엔진 B·A·C로 정상 진행(백로그는 추가 후보군이지 대체가 아니다).

**엔진 B — 포착면 스캔 (10소스 Bash 파서, 소스 목록·운용 규칙은 가이드 0-2가 SSOT)**
⛔ WebFetch 요약으로 대체 금지 — 뉴스맥락·트래픽 티어가 유실된다. `curl` + Python 파싱으로 원문을 수확한다.
```
# 소스 1 — Google Trends KR RSS: 헤드|트래픽|뉴스맥락 3열
curl -s --max-time 15 "https://trends.google.co.kr/trending/rss?geo=KR"
→ <item>별 <title> · <ht:approx_traffic> · <ht:news_item_title>(최대 2건) 추출, 트래픽 내림차순
# 소스 2 — 네이버 종합 랭킹 (euc-kr → cp949 → utf-8 순 디코드)
curl -s --max-time 15 "https://news.naver.com/main/ranking/popularDay.naver"
→ 정규식 list_title[^>]*>([^<]{8,90})< · 인물 가십·정치 공방만 1차 컷
# 소스 3~5 — 네이버 섹션 IT(105)·경제(101)·생활/문화(103)
curl -s --max-time 15 "https://news.naver.com/section/105"   # 101 · 103 동일
→ 정규식 class="sa_text_strong">([^<]{8,90})< · 섹션당 상위 ~16건
# 소스 6 — Product Hunt (보조, 신기술 축)
curl -s --max-time 15 "https://www.producthunt.com/feed" → <title> 추출
# 소스 7~10 — 네이버 세부 분야: 스파이크 최대 산지 (파싱은 대분류와 동일)
curl -s --max-time 15 "https://news.naver.com/breakingnews/section/105/731"   # 신제품·기기·통신
curl -s --max-time 15 "https://news.naver.com/breakingnews/section/105/226"   # 서비스·플랫폼 변경
curl -s --max-time 15 "https://news.naver.com/breakingnews/section/101/263"   # 생활경제·소비자 가격
curl -s --max-time 15 "https://news.naver.com/breakingnews/section/103/376"   # 소비 트렌드·팝업·핫플
```
⚠️ 네이버 세부 분류는 라벨과 실제 내용이 어긋난다(103/376 라벨은 '여행/레저'인데 내용은 뷰티·팝업) — 라벨이 아니라 실측 내용 기준으로 쓴다.
⭐ 수집한 **헤드+뉴스맥락 쌍** 각각에 가이드 1-T ①의 각도 전환을 시도한다 — '헤드(1~2어절)+실행/대상 수식(1~2어절)' 하이브리드로 변환하고 전환 성공분만 후보화한다. **헤드 단독 전환 금지**(맥락 없이는 실패). 전환이 안 되는 헤드만 버린다 — 접점 필터로 헤드를 먼저 죽이지 말 것.
⭐ 전환 성공 후보는 자동완성으로 **가이드 0-3 3종 판별 테스트**(연도 조합 유무·조합 형성 시점)를 즉시 수행해 유형(스파이크/시즌 롱테일/뉴스 트리거 롱테일)을 붙인다.
⭐ **수명 점검 (가이드 0-1)**: 같은 급 스파이크가 둘 이상이면 뉴스가 식은 뒤에도 계속 검색될 실행 각도(계산·조건·방법·기한)가 있는 쪽을 고른다. 사건 경위·인물·지배구조 그 자체가 소재인 것은 이틀이면 죽는다(하드 게이트 아님 — 동급 후보 선택 신호).
📡 **스캔 로그 의무**: 소스별 `소스명 · HTTP 상태 · 수확 헤드 수 · 전환 시도 수 · 상위 탈락 사유`를 기록한다. 이 로그가 없으면 스파이크 미발견 선언이 불가하다(가이드 0-3).
💡 시너지 메모: '외국인/foreigner/영문' 헤드 발견 시 KoreaPlug 소재로 STEP 8에 메모.

**엔진 A — 자동완성 확인·판별 (발견 도구 아님)**
구글 자동완성 API(Bash 경로)의 용도는 두 가지뿐이다:
① 엔진 B·C·D 후보의 **수요 확인**(4-A 겸용) — '+방법/조건/신청/계산/후기/디시/2026/비교/언제까지' 접미 변형 조회. '디시/더쿠/짤' 접미 발견 시 커뮤니티 버즈 가산점(사전 예측 지표).
② **3종 판별 테스트** — 핵심 조합 형성 여부·연도 조합("YYYY+키워드") 유무 조회(가이드 0-3).
자동완성에서 출발하는 발굴은 **에버그린(0-1 순위 3) 후보를 만들 때만** 하며, 가이드 1-1의 생활 영역 시드에서 한다. 수확 문구 중 발행 이력에 없는 실존 조합을 후보화(스파이크는 S, 에버그린·뉴스트리거는 L 예비).

**엔진 C — 시즌 캘린더 (D-14 이내 임박 이벤트만, 내장 표 기반, 차단 무관)**
가이드 `0-4 시즌 캘린더`에서 **오늘 기준 D-14 이내로 도래하거나 수요 구간이 진행 중인 이벤트**만 읽는다. **14일보다 먼 이벤트는 후보로 만들지 않는다** — 앵커 날짜와 함께 백로그 '날짜 대기'로 적재하고 D-14 진입 시 꺼낸다.
- **연도 갱신 우선**: 같은 이벤트의 전년도 `Head Type=시즌` 글이 현황표에 있으면 가이드 `1-5`에 따라 신규 발굴 전에 연도 갱신을 먼저 검토(갱신 채택 시 STEP 5는 기존 글 개정 모드로 진행하고 실행 로그에 `갱신: #XX {연도}` 기록).
- 임박 이벤트마다 실행 수식을 붙여 하이브리드 후보를 최소 3개 만든다(실행 수식 예: 신청 조건·납부 기한·예매·계산·인상률·지급일·한도). 형태 요건은 가이드 0-5.
- 현황표에서 같은 이벤트로 이미 쓴 실행 수식은 제외. 각 후보의 앵커 날짜(가이드 0-5)를 함께 적는다.
- 정책 시행 D-day는 엔진 B의 뉴스맥락에서 "시행·개정·신설" 키워드로 수집.
- ⚠️ 이벤트 날짜 미확정(음력 등 확인 실패) 시 건너뛰고 STEP 8에 기록.
- ⚠️ **임박 이벤트가 하나도 없으면 엔진 C는 후보를 내지 않는다** — 이는 정상이다. 스파이크도 임박 이벤트도 없으면 0-1 순위 3(에버그린)으로 내려간다. 시즌을 강제로 채우는 규칙은 없다.

⚠️ **각도 전환 의무 (가이드 1-T)**: 이슈 헤드(정년연장·환율 등)를 그대로 후보로 삼지 않는다. 뉴스는 '무슨 일이 일어났나'를 쓰고 우리는 '그래서 2~30대인 나는 뭘 해야 하나'(신청·계산·타이밍·내 상황 영향)를 쓴다. 후보는 하이브리드 2~4어절 — 자동완성 실존 조합만(예: '유류할증료 9월 발권', '삼성전자 배당 지급일').

→ 합산 후보 10~15개 수집(출처 엔진·3종 판별 결과를 로그에 표기).
→ **스파이크가 안 보일 때**: ① 포착면 10소스 스캔 로그 완비 확인(빠진 소스 마저 스캔) → ② 엔진 C로 D-14 임박 이벤트 재확인 → ③ 그래도 없으면 에버그린(엔진 A·D) 로직으로 진행하되, 기록은 가이드 `0-3` 형식("스파이크 미발견 (스캔 {n}소스 / 헤드 {m}건)")으로 한다. **"S 후보 없음" 문구 금지, 스캔 로그 없는 하향 금지.**

**광고단가는 검색하지 않는다 — 내장 CPC 기준표 사용** (동점 타이브레이커 참조용, 축 아님):
- $$$ 재테크·ETF·보험·증권·대출·세금·퇴직연금 → $0.8~2.0+
- $$$ 이직/커리어 교육·웨딩(스드메·예물·신혼여행) → $0.5~1.5
- $$ AI 도구·자동화·부업·온라인 강의·구독 → $0.3~0.8
- $$ 짠테크·앱테크·가계부·소비 관리 → $0.3~0.8
- $ AI 트렌드·생활팁·뉴스·디지털 문화 → $0.1~0.3

**각 후보 예비 채점** (공백·경쟁 축은 실측 전이므로 제외):
채점 축·배점·판정·가점·동점 처리는 전부 **가이드 `0-6`을 그대로 적용한다**(루틴은 채점 기준을 자체 보유하지 않는다). 루틴이 정하는 것은 출력 표 형식뿐:
`순위 | 주제 (포커스 키워드) | 유형(3종 판별) | 앵커 날짜 | 축1 /5 | 축2 /5 | 축3 /5 | 합계 /15 | 대상 폭 한 줄`
— 축 이름은 가이드 0-6 표에서 그대로 가져오고, "이 행동을 하는 사람이 누구인가" 한 줄 추정을 대상 폭 칸에 기재한다(가이드 의무 기재).

STEP 3 — Duplicate Check & Shortlist 확정 (검색 이전, 최우선 필터)

STEP 2 후보 전체를 현황표와 대조한다.
**중복 기준 (각도 기반)**: 기존 게시물과 핵심 키워드 2어절 이상이 겹치는 후보를 1차로 추린 뒤, 그중 같은 대상·같은 질문·같은 목적(각도)을 다루는 경우만 중복으로 제거한다. 단어만 겹치고 각도가 다르면 중복이 아니다. **자기잠식은 단어가 아니라 각도로 판정한다** — 최근 회차가 같은 조문·계산표를 이미 소진했으면 재사용하지 말고 내부링크로 넘기고 다른 각도를 취한다.

**같은 날 슬롯 충돌 필터 — `SLOT >= 2`일 때 최우선**: 오늘 앞 슬롯과 ⓐ 같은 카테고리 ⓑ 같은 클러스터(동일 조문·계산표·시즌 이벤트)인 후보는 점수와 무관하게 제외한다(제외분은 예비 리스트·백로그로 이관). 판정 수치는 가이드 1-3 R1·R2.

**헤드 자격·유형 필터**: 후보의 헤드가 가이드 `0-5 헤드 자격`을 충족하고 `0-3 3종 판별`·`0-5 앵커 요건`을 통과하는가? 판정 기준은 전부 가이드가 정한다. 루틴이 정하는 것은 불통과 처리 동작이다:
- ⓐ 헤드 자격 불통과 → 트랙 S에서 제외하되 버리지 않고 L 예비 리스트로 이관
- ⓑ 앵커 날짜 미확정 → 백로그 '날짜 대기' 적재
- ⓒ 뉴스 트리거 롱테일 판별 → L 예비 리스트 상위에 배치(뉴스 훅 메모 첨부). 오늘 급상승했으면 0-1 순위 1(스파이크)과 같은 긴급도로 취급한다(가이드 0-3)
- ⓓ 시즌·제도 롱테일이 D-14 밖이면 → 백로그 '날짜 대기'로 넘기고 이번 회차 후보에서 제외(가이드 0-1 순위 2)

**하이브리드 형태 필터**: 포커스 키워드가 가이드 `1-T`·`0-5`의 하이브리드 형태 요건을 충족하는가? 어절 수 범위·수식 필요 여부는 가이드가 정한다. 불통과인데도 진행하면 Track 행에 `L`로 정직하게 적는다.

- **비중복·필터 통과 후보 0개** → 에버그린 로직(엔진 A·D)으로 후보를 마저 만든다. 그래도 0개면 STOP: ⚠️ 경고 로그 + 사용자 알림(재브레인스토밍 금지, 그날 종료).
- **비중복 후보 있음** → **가이드 0-6 채점 합계 순**으로 정렬, 상위 2~3개를 최종 숏리스트로 확정하고 나머지는 예비 리스트로 보관.
- 가이드 1-3의 재테크 편중 방지·유명 툴 일반 사용법 배제도 이 단계에서 함께 적용. **1급 자료 조달 계획·반려 로그 대조 관문**(가이드 1-3)도 적용 — 각 후보에 조달 계획(`루틴가능`/`사용자필요`/`사용자필요(앱전용)`/`불가`) 한 줄 판정, `불가`와 반복 반려 유형은 제거. 채택 후보의 조달 계획은 STEP 6 기본 정보 표에 "1급 자료 조달 계획" 행으로 기재.

STEP 4 — Web Validation + Gap Analysis (WebSearch는 4-B에서만)

**[4-A] 수요 검증 — WebSearch 미사용 (필수 관문)**
숏리스트 1순위부터 순서대로: 구글 자동완성 API(hl=ko) Bash 조회(또는 차단 시 발행 이력·모델 지식 기반 대체 신호)로 노출 확인 + 서브키워드 2~4개 수집. 같은 조회에서 3종 판별(연도 조합 유무)을 겸한다(추가 비용 0). 네이버 자동완성 API는 세션당 1회만 시도(`https://ac.search.naver.com/nx/ac?q={키워드}&st=100&frm=nv&r_format=json`, UTF-8, `items[0]` 배열).

▸ **시즌 헤드·제도 헤드는 별도 경로 — 자동완성 미노출은 탈락 사유가 아니다.** 선점은 검색이 터지기 전에 쓰는 것이 목적이다. 아래 3종 중 1개 이상이면 통과:
1. 자동완성에 조합 또는 헤드 단독 노출(약신호 허용, 네이버 포함)
2. 커뮤니티에 작년 동기 같은 질문 3건 이상(엔진 A '디시/더쿠' 접미로 대체 가능)
3. 이벤트·제도가 독자 행동을 직접 제약함이 확정 사실(납부 기한·신청 마감·예매 오픈·시행일)
→ 3종 모두 불가일 때만 탈락. 서브키워드는 자동완성이 비면 확정 사실 용어(기한일·서식명·기관명·구간명)로 구성하고 근거를 STEP 8에 기록.

근거가 전혀 없으면 해당 후보 탈락 → 다음 순위로 이동해 4-A 재시작(무료, 반복 가능). 4-A 통과 후보가 2개 모일 때까지(또는 리스트 소진까지) 반복. 리스트를 다 써도 0개면 STOP: ⚠️ 경고 로그 + 사용자 알림. **검증 생략 후 진행 금지**(단, 검증 수단은 항상 무료이며 WebSearch를 쓰지 않는다).

**[4-B] 공백 실측 — 4-A 통과 후보 중 경쟁이 가장 적은 1개 선정 (WebSearch 전용)**
쿼리: `"{FOCUS_KEYWORD}" -site:0and1life.com`
1. 4-A 통과 1순위로 검색 1회.
2. **공백 판정**: 뉴스·공공기관·계산기 도배는 탈락 사유가 아니라 수요 신호다. 탈락 기준은 오직 '개인·기업 블로그의, 내가 쓰려는 각도와 동일한 정리형 글 3개 이상'일 때만. 다른 대상·다른 질문의 정리글은 GAP ⭐ 재료. 뉴스만 있으면 최적 기회.
3. 3개 미만(통과) → 채택, 종료. 같은 결과에서 개인 블로그·티스토리·브런치 상위 2개 URL 확보.
4. 3개 이상(포화) → 2순위로 검색 1회 더. 두 결과를 비교해 경쟁이 더 적은 쪽 채택.
5. 둘 다 포화면 **재타겟 1회** — 자동완성에서 더 좁은 실존 조합을 찾아 그 각도로 재측정. 재타겟도 포화면 가장 경쟁이 적은 쪽을 채택하고 차별화 각도를 평소보다 강하게 반영(완벽한 블루오션을 요구하지 않는다).
⚠️ 가이드 1-3 **경쟁 구도 관문**(1페이지에 개인 블로그 또는 커뮤니티 질문글 1개 이상)에 불통과한 후보는 공백 수치와 무관하게 **채택 불가** — 다음 순위로 이동.
→ 이 절차는 최대 4회 안에 승자를 정한다. 4-A 통과 후보가 1개뿐이면 비교 없이 그 후보로 1회만 실행하고 결과를 GAP 재료로 활용(탈락시키지 않음).

**[4-C] 상위 2개 URL 읽기 — WebFetch** (검색 예산 무관. 차단 시 4-B 검색 결과의 제목·스니펫으로 파악)
H2/H3 헤딩 / 도입부 독자 전제 / 계산 예시·수치·실경험 유무.

**[4-D] GAP_REPORT 생성**
- ✅ 경쟁글 공통 커버: [항목] → STEP 5 기본 포함
- ⭐ 경쟁글이 빠뜨린 각도: [1~2개] → STEP 5 H2로 반드시 반영(없으면 발행 금지)
- 🚫 경쟁글 공통 SEO 제목 구조 → 훅 패턴 변경(가이드 2-2)
- ⚠️ 블로그 정리글 없이 뉴스·공공기관만 있는 경우 → ⭐ "계산 예시 + 공감 구조로 작성" 후 계속
- ⚠️ 포화 후보를 그대로 채택한 경우 → ⭐ "차별화 각도 강하게, 개인 경험/계산 예시로 승부" 후 계속

STEP 5 — Write the Blog Post (스킬 2종 필수 — 가이드 2-6)

**[5-0] create-viral-content — 구조·훅 (초안 전)**
SEO Title 후보 10개↑ → 호기심/구체성/감정 3축 스코어링(각 0~3, 합 7↑ 채택). 구체 수치 포함 후보 우선, 가이드 2-2 훅 규칙 병행. **가이드 2-2 직접 영향 원칙 준수** — 제목은 사건 요약이 아니라 독자 본인에게 걸린 것(내 돈·수당·기한·손익)을 앞세운다. **시즌·제도 헤드는 앵커 날짜를 제목 또는 도입 첫 문단에 명시**(가이드 0-5). 첫 문단(60단어): Before/After 압축 또는 문제 직격(백과사전식 서론 금지). 클로저: engagement bait 금지, 오늘 바로 할 행동 1가지. 정제 패스 3개: Skeptic → Scroller → Editor.

**[5-1] 본문 작성** — 가이드를 STEP 1에서 읽었으므로 각 섹션을 직접 참조:
- 키워드 전략 → 가이드 1-0~1-4. 서브키워드는 4-A 수집 실존 문구 기반 2~4개. AI 임의 조합 금지.
- **대표 이미지 조달** (이 루틴이 정의 — 가이드엔 마크업만 있음):
  ① Unsplash 검색 페이지에서 실존 photo ID 확보(시드: FOCUS_KEYWORD 영문 대응어 또는 카테고리 대표어). ② URL: `https://images.unsplash.com/photo-{ID}?auto=format&fit=crop&w=1200&q=80`. ③ 주입 전 그 URL을 1회 응답 확인 — 200이 아니거나 unsplash.com이 차단되면 photo ID를 추측하지 말고 플레이스홀더 `[FEATURED_IMAGE_URL]`을 남긴다(0and1life-wordpress-draft [4d]가 Chrome으로 교체 — 정상 경로). ④ 마크업은 가이드 `<figure>/<img>` 형식, alt에 FOCUS_KEYWORD 포함.
- 기본 메타 → 가이드 2-1 표 형식(Slug 영문 소문자+하이픈 3~5단어, SEO Title, Meta Description, Focus/Sub Keywords).
- **카테고리 확정 → 가이드 2-1 배정 결정 트리(필수)**. 판정 질문·범위·우선순위는 가이드가 정한다. 루틴이 정하는 것은 기재 형식뿐: Category 행에 `카테고리명 + WP ID`를 반드시 기재(예: `📐 생활 규정·계약 계산 (WP 30)`). WP ID 매핑·초안 보관 방 page_id는 STEP 6 표. 🤖 AI 정보(WP 26)는 주 0건 동결.
- 카테고리 균형 R1~R5 적용(수치·판정은 가이드).
- HTML 본문 → 가이드 HTML 템플릿. TOC는 `<!-- TABLE OF CONTENTS -->` 플레이스홀더 유지.
- 분량·밀도 → 가이드 2-5(단위는 **어절 수**, 글자 수 아님).
- 외부 권위 링크 최소 2개(공식·1차 출처).
- **내부 링크 최소 2개, 개별 글 상대경로 `/slug/`** (조달 절차):
  ① 사이트·사이트맵을 긁지 않는다(차단 시 슬러그 추측 위험). ② 현황표에서 연관 기존 글 2개를 골라 그 글의 Notion 서브페이지를 fetch해 기본 정보 표 Slug를 그대로 읽어 쓴다(네트워크 불필요, 정확). ③ 레거시 예외: #21~#65는 서브페이지가 삭제돼 Slug를 읽을 수 없다 — 이 글을 링크 대상으로 고를 때만 WP REST로 조회: `GET https://0and1life.com/wp-json/wp/v2/posts?search={제목 핵심어}&_fields=slug,title&per_page=3`(인증 불필요, 프록시 200 정상). ④ `/category/...` 아카이브로 대체 금지(404 위험). ⑤ ②③ 모두 실패하면 내부 링크를 1개로 줄이고 STEP 8에 기록. ⑥ **미발행(draft) 글로 링크 금지** — 대상 슬러그를 WP REST로 1회 조회해 `[]`(미발행)이면 제외(걸면 404).
- 테마 컬러 → 가이드 2-4 매핑.
- **GAP_REPORT ⭐ 각도를 H2 1개에 반드시 반영.**

**[5-2] avoid-ai-writing — 최종 패스 (HTML 완성 직후, Notion 업로드 전)**
균일한 문장 길이 깨기, 기계적 전환어 남발 제거, 과잉 감탄·홍보투 제거. "~합니다" 일변도에 구어체 혼용. 수치·고유명사·출처 링크·HTML 구조는 보존, 문체만 교정.

**[5-3] 문체 지문 대조 — 가이드 Phase 5-6 ([5-2] 직후 필수)**
기준 본문: STEP 1에서 읽은 가이드 `Phase 5-6`(별도 조회 불필요). 가이드 조회 자체가 실패했으면 이 단계를 건너뛰고 STEP 8에 `문체 지문 참조 불가` 기록([5-2]는 이미 수행됐으므로 발행 진행). [5-2]와 순서를 바꾸지 않는다([5-2]=뺄셈, [5-3]=덧셈).
대조 기준(목표 수치·금지어·교정 순서)은 전부 가이드 Phase 5-6에 있다. 루틴이 정하는 것은 수행 방식뿐:
- 원고를 실제로 세어 가이드와 대조(인상 판단 금지 — 숫자 산출), 불통과 항목만 고침, 교정 순서는 가이드 명시 순서, 수치·고유명사·링크·HTML 보존.
- **5어절 이하 비율 미달을 문장 쪼개기로 해결하지 않는다** — 가이드 과분할 금지 조항대로 분량을 늘리면서 짧은·긴 문장을 함께 추가하는 것이 평균·표준편차·비율을 동시에 만족시키는 유일한 경로다.
- 측정 결과는 STEP 8 `🖋️ 문체 지문` 행에 기록.

**[5-4]** 교정 후 가이드 5-3(포커스 배치 6항목 + 문체 지문 10항목) + 5-5(AEO·GEO) 교차 검증.

STEP 6 — Create Notion Page

⚠️ `parent`는 tool call 최상위 파라미터로 지정(pages[] 배열 내부 금지). parent는 STEP 5-1에서 확정한 카테고리에 따라 아래 방 중 하나. 방은 WordPress 카테고리와 1:1이라 AI 3종은 방 하나를 공유한다.

| 확정 카테고리 | parent 방 page_id | WP ID |
|---|---|---|
| 📊·⚙️·📈 AI 계열 **(주 0건 동결)** | `3a9bfe4a-2ae1-8194-85db-cc0d029dbbd3` | 26 |
| 💰 직장인 재테크 | `3a9bfe4a-2ae1-8144-93b1-c2b1cd50bc4d` | 23 |
| 💼 대기업 직장인 생활 | `3a9bfe4a-2ae1-813d-b13b-cf2284c9f009` | 29 |
| 📐 생활 규정·계약 계산 | `3a9bfe4a-2ae1-81fc-98e2-d1d55449ed41` | 30 |
| 💍 Wedding | `3a9bfe4a-2ae1-817f-9b02-ceee29f98bff` | 27 |
| 🏠 Life·My Story / 디저트·핫플 | `3a9bfe4a-2ae1-8139-9c8a-e16c92bc081e` | 25 |

⚠️ Wedding은 WP **27**이다(서브페이지 Category 행에 27을 적는다 — 1로 적으면 미분류로 들어간다). AI 계열 WP ID는 **26**이다(18은 레거시, 신규 배정 금지).
⚠️ **방과 현황표는 계열이 일치해야 한다** — 위 표 앞 4개는 `[0]` 계열이므로 STEP 7에서 `[0]` 현황표(`37cbfe4a-2ae1-819f-8664-ff3d38fffe56`)를, 뒤 2개는 `[1]` 계열이므로 `[1]` 현황표(`37cbfe4a-2ae1-81b9-a9ce-d0b937edd344`)를 갱신한다. 계열이 어긋나면 0and1life-wordpress-draft가 글을 찾지 못한다.

제목: `Blog #XX — {포스트 제목}` (XX = MAX_NUM + 1, **양 계열 합산 기준**)

🗄️ **초안 서브페이지는 영구 보존한다.** 배포가 끝나도 삭제하지 않는다 — 0and1life-wordpress-draft가 배포 시 여기서 HTML을 읽고(배포 전 삭제 시 배포 실패), 이후 글이 내부 링크를 걸 때 여기 Slug를 조회한다(배포 후 삭제 시 슬러그 원본 소실).

**2개 섹션** (배포 JS 생성 금지 — 0and1life-wordpress-draft가 REST API로 직접 배포):
- **[섹션 1]** 가이드 기본 정보 표 구조 + 데이터. Draft 일자는 공란.
  ⚠️ **`Track`·`Head Type` 행을 반드시 채운다**(가이드 2-1):
  - `Track`: `S` 또는 `L` — 요일이 아니라 **실제 채택 키워드의 3종 판별 결과**로 결정(가이드 0-1·0-3). 뉴스 트리거 롱테일은 `L`로 적고 로그에 "뉴스트리거" 명기.
  - `Head Type`: 트랙 S일 때만 — `이슈`/`이슈(신기술)`/`시즌`/`제도` + 근거 한 줄 + 3종 판별 결과·앵커 날짜 병기(예: `제도 (발권 마감 8/31 — 판별: 월별 조합 안정 + 급등 사건성)`).
  - `Problem Hook`: 가이드 2-7 — `[도입 문제 1줄] → [답하는 H2 제목]`. 신기술·앱 필수, 그 외 권장.
  - 이 행들은 일간 점검 루틴 항목 9가 매일 읽어 규칙 준수를 판정한다(비우면 알람).
- **[섹션 2]** 완성 HTML 전체를 ```html 코드 블록.

STEP 7 — Update the Published Posts List

STEP 6에서 고른 계열과 **동일한** 현황표를 갱신한다.
- [0] 계열 → https://app.notion.com/p/0-EFFICIENCY-37cbfe4a2ae1819f8664ff3d38fffe56
- [1] 계열 → https://app.notion.com/p/37cbfe4a2ae181b9a9ced0b937edd344

① **헤더 카운트 — 조건부**: re-fetch한 페이지 상단에 '전체 N개' 형태 문구가 실제로 있으면 N+1로 갱신, 없으면 건너뛴다(현재 두 페이지 모두 카운트 없는 헤더 — 정상, 오류 아님).
② **표에 새 행 추가**: 실행 전 현황표 re-fetch 필수(마지막 행 실제 셀 값 확인 후 old_str 구성). old_str은 `<td>` 태그 포함 전체 행 그대로 복사(bare text 불가). 갱신 후 재fetch로 검증.
new_str = old_str + 새 행:
`<tr><td>{XX}</td><td>{SEO 제목}</td><td>{META_DESCRIPTION 앞 30자...}</td><td>—</td><td>{서브카테고리 이모지+이름}</td><td>{아웃링크번호}</td><td>—</td><td>{작성일자}</td><td></td></tr>`
⚠️ 마지막 열(Draft 일자) 공란 유지(Draft 루틴이 배포 시 자동 기재). SEO 점수 열 "—" 유지.

🔁 **`RETRY_MODE = true`인 회차만**: 실행 로그 첫 줄에 먼저 적는다(남기지 않으면 1차 트리거 실패 빈도를 집계할 수 없다):
`🔁 재시도 트리거로 실행 (1차 트리거 미실행 — {TODAY_KST})`

실행 로그:
```
✅ 트렌드 초안 작성 실행: {today_kst} (슬롯 {SLOT}/{DAILY_TARGET})
신규 draft: #{XX} {SEO 제목} (Notion 등록 완료, WP 배포 대기)
오류: 없음
다음 실행: {tomorrow} (다음 예약 트리거 — 시각은 단언하지 않고 날짜만 기재)
```
⚠️ **`트렌드 초안 작성 실행: {today_kst}` 문자열 자체는 반드시 그대로 유지한다** — STEP 0-⓪ 가드가 이 문자열의 출현 횟수로 `TODAY_COUNT`를 세고 일간 점검 루틴도 같은 문자열을 읽는다. 슬롯 표기는 뒤에 덧붙이기만 한다. (DAILY_TARGET=1이면 슬롯 표기 생략 가능.)
⚠️ WebFetch 차단으로 WebSearch 대체 경로를 탄 경우에도 '오류: 없음'으로 기재(정상 경로). 진짜 오류(STOP·Notion 실패 등)만 기록.

STEP 7-B — 낙선 후보 백로그 적재

매 실행이 후보를 10~15개 만들어 1개만 쓰고 나머지를 버린다. 그중 **이미 수요 검증을 통과한 것**을 백로그에 남긴다(추가 비용 0).
적재 대상 — 아래를 모두 만족, **슬롯당 최대 2건**(하루 총량 과잉 방지, GSC 대기 ≤35건 목표):
- 4-A 수요 검증 통과(구글·네이버 자동완성에 실존 문구로 노출 확인)
- STEP 3 중복 대조·필터 통과
- 4-B에서 밀렸거나, 예산 때문에 4-B를 못 돌린 예비 리스트 상위 항목
앵커 미확정 또는 D-14 밖 시즌·제도 후보는 상태 `날짜 대기`로 적재(D-14 진입 또는 앵커 확정 시 '대기' 승격).
⛔ 적재하지 않는 것: 4-A 미통과 후보, 브레인스토밍 아이디어, 모델 지식으로만 떠올린 조합(엔진 D의 '4-A 통과 간주' 전제가 무너진다).
기록 위치: https://www.notion.so/3a9bfe4a2ae181e08fc9dc8e035388af
행 형식: `| {다음 번호} | {키워드} | {구글AC 또는 네이버AC} | {자동완성 실존 노출 문구 그대로} | {클러스터} | — | {today_kst} | 대기 |`
⚠️ 적재 전 백로그 re-fetch → 기존 행·현황표 제목과 중복 대조(이미 있는 키워드 건너뜀). 마지막 행 원문 확인 후 old_str 구성. 엔진 D에서 백로그 항목을 소비했으면 그 행 상태 갱신(`채택 #XX`/`탈락(사유)`)을 먼저 끝낸 뒤 신규 행 추가.

STEP 8 — Report

```
✅ 0and1life Daily Automation — {today_kst} 완료
🔁 (재시도 실행일 때만) 재시도 실행 — 1차 트리거 실패
⛔ (직전 누락 회차 있으면, 가이드 0-7 형식) 회차 누락: {YYYY-MM-DD} — 연속 {n}회
   (2회 연속 🟡 / 3회 연속 🔴 + 예약 작업 점검을 사용자에게 요청)
🎰 슬롯: {SLOT}/{DAILY_TARGET} · 오늘 앞 슬롯 {있음(#XX {카테고리}) / 없음} · 카테고리·클러스터 충돌 회피 {n}건
📡 포착면: 스캔 {n}/10 소스 · 수확 헤드 {m}건 · 전환 시도 {k}건 · 스파이크 {발견(키워드) / 미발견} — 미발견 시 상위 탈락 사유 3건. 미발견 {j}회 연속이면 가이드 0-3에 따라 포착면 확장 안건 병기(1순위 검토: 앱스토어 순위 관측 경로)
🔍 주제 탐색: 브레인스토밍 {M}개 / 중복 제거 후 {K}개 / 숏리스트 {J}개 / WebFetch: {정상/차단} / WebSearch {N}회 (한도 4) / 트랙: {S/L} / 수요 증거: {확인된 것}
🎯 스킬: create-viral-content (제목 스코어 {X}/9, 정제 {n}패스) / avoid-ai-writing (완료)
🖋️ 문체 지문: {적용 완료/조회 실패} / 총 {X,XXX}어절 / 평균 {X.X} / 표준편차 {X.X} / 5어절 이하 {XX}% / `~습니다` {XX}% / 금지어 {N}건
📝 선택 주제: [주제명] / 유형: [스파이크·시즌 롱테일·뉴스트리거 롱테일·에버그린 — 3종 판별 근거 한 줄] / 제외 후보: [중복 vs 수요검증 vs 앵커 미확정 vs D-14 밖 vs 4-B 비교 vs 경쟁 관문 구분 기록]
📂 위치: [서브카테고리명]
🔗 Notion URL: [생성된 페이지 URL]
📋 목록 업데이트: #{XX} + 표 행 + 로그 추가 완료
💡 한줄 요약: 앞에 트랙 태그 — [S·이슈] / [S·이슈(신기술)] / [S·시즌] / [S·제도] / [L] / [L·뉴스트리거] + 핵심
📏 트랙 규칙 준수: 포커스 `{키워드}` = {n}어절 / 헤드 `{헤드}` = {이슈·시즌·제도·자격없음} / 앵커 `{날짜/해당없음}` / Track `{S/L}`
🧮 후보 채점: 1순위 {키워드} 축1 {n} · 축2 {n} · 축3 {n} = {합계}/15 · 대상 폭 [한 줄] (2·3순위는 합계만)
🔀 KoreaPlug 소재 메모: [외국인 관련 헤드 발견 시]
📤 HTML 작성 완료 — 0and1life-wordpress-draft가 다음 실행 시 자동 배포
🪝 훅 점검: 직전 10개 제목 중 동일 훅 {n}회 (2회 이상이면 교체 — 교체 시 명시)
📐 카테고리 편중: 이번 주 {카테고리}={n}건 (한도는 가이드 R1 — 초과 제외 후보 있으면 명시)
🚧 반려 로그: STEP 1 확인 {반복 유형 유/무} / 후보 제외 {n}건 / 이번 글 조달 계획: {루틴가능·사용자필요 + 한 줄}
📦 백로그: 검토 {건수} / 채택 {0/1} / 상태 갱신 여부 / 신규 적재 {n}건 (0건이면 사유) / 날짜 대기 적재 {n}건
```

⚠️ 전제조건

| 항목 | 조건 |
|---|---|
| 실행 환경 | Claude Code on the web — Chrome MCP 없음. WebFetch 우선, 차단 시 WebSearch 자동 대체(오류 아님). 자동완성 API는 Bash curl + euc-kr 디코드. 포착면 10소스는 Bash 파서(WebFetch 요약 금지). 알려진 차단 사이트는 처음부터 접속 안 함 |
| 재시도 가드 | STEP 0-⓪가 모든 STEP보다 먼저 실행. `TODAY_COUNT >= DAILY_TARGET`이면 아무것도 쓰지 않고 즉시 종료. 판정 기준은 가이드 0-7 |
| 트랙 판정 | 요일·홀짝을 보지 않는다. 매 실행 가이드 0-1 우선순위(스파이크 > 임박 롱테일 ≤14일 > 에버그린). 트랙 S/L은 3종 판별 결과로 사후 기록 |
| 포착면 | 매 실행 가이드 0-2 전 10소스 의무 스캔 + 스캔 로그. "S 후보 없음" 문구 금지 — "스파이크 미발견 (스캔 n소스/헤드 m건)"만 허용(가이드 0-3) |
| Notion 권한 | MCP 커넥터가 서브카테고리 페이지 편집 권한 보유 |
| WebSearch 한도 | 글당 최대 4회, STEP 4-B 전용(STEP 1~4-A 미사용). 소진해도 STOP 아님 — 4-B는 숏리스트 내 비교로 승자를 정함. 진짜 STOP은 STEP 3·4-A에서 채택 가능 후보가 0개일 때만 |
| 품질 관문 | 차단 무관 불변 — 수요검증 생략 금지(수단은 무료) / 서브키워드는 실존 노출 문구만 / GAP ⭐ H2 필수 / EEAT 강제 조건 유지 / 경쟁 구도 관문 불통과는 예외 없이 폐기 / 시즌·제도 헤드는 앵커 날짜 필수(가이드 0-5) |
| 스킬 | create-viral-content(5-0) + avoid-ai-writing(5-2) 필수 |
| 문체 지문 | 5-3에서 가이드 Phase 5-6 대조 필수. 가이드 조회 실패 시에만 생략 가능하며 STEP 8에 기록 |
| 중복 기준 | 기존 제목과 포커스 키워드 2어절 이상 겹침 시 1차 추림, 최종은 **각도**로 판정(STEP 3, 검색 이전 선행) |

[결과물] 간단하게 표로 정리할 것. 모든 것은 한글로 얘기하고, 중요하지 않은 것은 생략한다.
