[루틴 : 0and1life-Writer — v4.3 (웹 전용, 트랙 S 검증 장치 + 문체 지문 대조 + 역할 경계 정리 + 재시도 가드)]

*예약된 트리거 시간이 되면 해당 일자에 먼저 루틴이 실행되었더라도 무조건 다시 진행한다.
날짜: 실행 시점의 실제 KST 날짜를 사용한다 (이 프롬프트에 적힌 고정 날짜가 있어도 무시한다).

⚠️ 실행 환경: Claude Code on the web — Chrome MCP 없음. 브라우저 의존 절차 전면 금지.
모든 자동완성·트렌드·커뮤니티 수집은 WebFetch/Bash(예산 미소모)로 시도한다. 단, 이 환경은 조직 네트워크 정책상 WebFetch가 외부 도메인 전체에 대해 차단될 수 있다. 차단 여부 판정은 STEP 0에서 세션당 1회 수행하며, 이후 모든 STEP은 그 결과(WEBFETCH_STATUS)를 그대로 사용한다 (판정 기준·개별 사이트 예외 처리는 STEP 0 참조). 403/차단이 확인되면 즉시 WebSearch로 전면 대체한다 — 이는 정상 대체 경로이며 오류로 취급하거나 매번 보고하지 않는다 (가이드 v1.19).

⚠️ **(v4.3) 자동완성 API는 Bash 경로를 우선한다.** WebFetch는 구글 자동완성 API의 **EUC-KR 응답을 깨뜨려** 한글 판독이 불가하다(2026-08-16 실측). WEBFETCH_STATUS가 정상이어도 자동완성 수확은 `curl` + Python `euc-kr` 디코드로 돌린다 — 예산 미소모는 동일하다.
```
curl -s --max-time 15 "https://suggestqueries.google.com/complete/search?client=firefox&hl=ko&q={URL인코딩 키워드}"
→ 응답 바이트를 euc-kr → cp949 → utf-8 순으로 디코드 시도 후 JSON 파싱, [1] 배열이 제안어
```

⚠️ 알려진 차단 사이트는 처음부터 접속 시도하지 않는다 (v1.20): 디시인사이드·클리앙은 봇 차단이 잦아 WebFetch 성공률이 낮다. 이 목록은 실행 중 관찰되는 대로 계속 추가한다 (현재: 디시인사이드, 클리앙, help.3o3.co.kr, jobkorea.co.kr, law.go.kr, labor.moel.go.kr, unsplash.com).

⚠️ **역할 경계 — 이 루틴은 도구다 (v4.2 개정, v4.3 유지)**
이 루틴은 **운영 절차(언제·무엇을·몇 회·어디에 기록)**와 **실행 자산(Notion 페이지 ID·API 경로·CPC 기준표·리포트 출력 형식)**만 정의한다.
**판정 기준·수치·품질 기준의 단일 기준(SSOT)은 전적으로 GitHub 작성 가이드**(`0and1Life-Writer.md`)다.
- ⛔ **가이드에 있는 수치·판정 기준을 이 루틴에 복사해 적지 않는다.** 복사본은 가이드가 개정될 때 조용히 구버전으로 남아 상충을 만든다. 실측 사고 — 2026-08-11 가이드 v1.37이 채점 타이브레이커를 3단으로 바꿨는데, 루틴 STEP 2에 복사돼 있던 舊 문장("광고단가는 동점 시 타이브레이커로만")이 그대로 남아 정면 충돌했다. **v4.2에서 이런 복사본을 전부 제거하고 조항 참조로 바꿨다.**
- ✅ 대신 **가이드의 조항 번호를 가리킨다** (예: "가이드 `0-6` 채점표를 그대로 적용한다").
- ✅ 규칙을 바꿔야 하면 **가이드를 고친다. 루틴은 고치지 않는다.** 루틴 수정이 필요한 경우는 절차·도구·출력 형식이 바뀔 때뿐이다.
- 그럼에도 상충이 발견되면 **가이드를 따르고**, STEP 8 리포트에 기록해 사용자에게 알린다.

🎯 타겟 독자: 한국의 2030~40대 초 직장인 — 돈 · 재테크 · 일 · 결혼 · AI가 핵심 관심사. 모든 주제 판단은 "이 독자가 네이버·구글에 실제로 검색할까?"가 기준이다.

✍️ 화자 기준 (v1.21 — 고정 페르소나 폐기): 독자와 눈높이가 같은 30~40대 직장인 화자. **가공된 신상(나이·직군·연차·결혼 준비 등)을 사실처럼 서술 금지.** 1인칭(저/제가)은 이 글을 쓰며 실제로 수행한 행위에만 쓰고, 그 결과물(계산 과정·캡처·비교표)을 본문에 함께 싣는다. 하지 않은 일은 3인칭·2인칭으로 쓴다. 舊 '직접 알아본/계산해본 프레임' 우회로는 폐기 — 실제 계산 과정과 결과 표를 공개할 때만 허용. 비경험 영역(육아 등)은 주제 후보에서 영구 제외. 상세는 작성 가이드 2-6.

📂 확정 카테고리 (v1.34 — 2026-08-08 WP REST 실측 반영):
[0] 💰 직장인 재테크(WP 23) / 💼 대기업 직장인 생활(WP 29) / 📐 생활 규정·계약 계산(WP 30, 舊 ⏱️ 시간·돈 절약 시스템) / 🤖 AI 정보(WP 26 — **주 0건 동결**)
[1] 🏠 Life·My Story(WP 25) / 💍 Wedding(WP 27) · +트랙 S 한정 소재: 디저트·핫플 '효율 공략'(리뷰 금지)
⚠️ AI 계열 WP ID는 18이 아니라 **26**이다(18은 레거시 4건, 신규 배정 금지). 주간 한도·3연속 금지·순환 하한은 **가이드 카테고리 균형 규칙 R1~R5**를 그대로 따른다 (v4.2 — 수치는 루틴에 적지 않는다).

⚠️ WebSearch 예산 (v1.20 — 하루 5회는 Claude 플랫폼 전체 한도이며 0and1life·KoreaPlug 두 블로그가 공유한다):
이 루틴은 글당 최대 2회로 자체 상한을 둔다 (WEBFETCH_STATUS 정상/차단과 무관하게 동일 — 두 블로그 합산 최대 4회, 나머지 1회는 여유분).

우선순위 (예산은 이 순서로만 쓴다):
1순위 — STEP 4-B 공백실측(경쟁 비교) 1회: 반드시 확보. WebFetch로 대체 불가능한 유일한 단계이자 주제 채택을 최종 결정하는 가장 중요한 단계.
2순위 — STEP 4-B에서 1순위 후보가 포화 판정일 때, 숏리스트 2순위 후보 비교용 1회.

STEP 2(브레인스토밍)와 STEP 4-A(수요검증)는 WebSearch를 쓰지 않는다 — WebFetch/Bash(자동완성 API 등, 예산 미소모)로만 시도하고, 막혀 있으면 발행 이력·엔진 C·모델 지식 기반 대체 신호로 판단한다. 이건 검증 생략이 아니라 무료 증거로 검증한 결과이며, WebSearch로 보완하지 않는다.

품질 관문은 어떤 경우에도 유지 (가이드 v1.19 ④): 서브키워드는 자동완성 API 또는 검색 결과에 실제 노출된 문구만 사용(AI 임의 조합 금지), GAP ⭐ H2 필수, EEAT 강제 조건 불변.
후보 탈락 시 새 주제를 즉흥적으로 발명하지 말 것 — STEP 3에서 확정한 숏리스트/예비 리스트 순위로만 이동. STOP은 STEP 3·4-A에서 채택 가능한 후보가 완전히 0개가 됐을 때만 발생한다 (각 STEP 참조).

🔴 **v4.3 변경 (2026-08-16) — 재시도 트리거 + 중복 실행 방지 가드**
2026-08-15에 예약 트리거가 `클라우드 컨테이너 설정 중` 단계에서 실패해 **두 블로그가 동시에 회차를 통째로 잃었다.** 프롬프트가 로드되기 전에 죽어 루틴이 자기 실패를 기록할 수 없었고(STOP 로그는 실행이 돼야 쓸 수 있다), 다음 날 일간 점검이 오탐(Writer/Draft 혼동)을 낼 때까지 아무도 몰랐다. 홀수일이라 **트랙 S 시즌 선점 회차 하나가 소멸**했다.
원인(플랫폼 프로비저닝)은 루틴이 고칠 수 없으므로 **노출을 줄인다** — 같은 프롬프트를 쓰는 재시도 트리거를 하나 더 두고, 정상일에는 무동작으로 끝나게 하는 가드를 맨 앞에 세운다.
**판정 기준은 전부 가이드 `0-7 회차 누락 처리`가 정한다. 이 루틴은 절차만 정의한다** (가이드 v1.39와 짝을 이룬다).

🔴 **v4 변경 (2026-08-08) — 트랙 S 검증 장치 이식 (자매 블로그 KoreaPlug v6에서 이식)**
배경: KoreaPlug이 2026-08 홀수일 산출물 7건을 전부 롱테일 에버그린으로 흘려보낸 뒤 v6에서 검증 장치를 신설했다. 이 블로그는 v1.27에서 엔진 순서·선점성 축을 먼저 도입했지만 **판정·기록 장치가 없어 규칙 준수를 확인할 방법이 없었다.** 그 부분만 이식한다.
이식 항목: ① **STEP 0-② 시즌 선점 창 판정**(SEASON_WINDOW) ② **STEP 0-③ L 대체 연속 카운트**(L_FALLBACK_STREAK) ③ **STEP 3 헤드 자격·어절 수 필터** ④ **STEP 4-A 시즌·제도 헤드 별도 경로** ⑤ **STEP 6 Track·Head Type 기록** ⑥ **STEP 8 트랙 태그**.
⚠️ **이식하지 않은 것**: KoreaPlug의 Foreigner Test·'한국 자체' 테스트·영어 단어 수 규칙은 타겟이 정반대라 가져오지 않는다. 이 블로그의 대응 관문은 **독자 접점(2030~40 직장인이 기한 안에 뭔가 해야 하는가)**이며, 단위는 단어가 아니라 **어절**이다.
가이드 **v1.35와 짝을 이룬다.**

---

STEP 0-⓪ — 중복 실행 방지 가드 (v4.3 신설, 모든 STEP보다 먼저 실행)

이 루틴은 **1차 트리거**와 **재시도 트리거**가 같은 프롬프트를 공유한다. 아래 가드가 재시도를 정상일에는 무동작으로, 1차 실패일에만 실제 실행으로 만든다.

① **오늘 날짜 고정**: `TZ=Asia/Seoul date '+%Y-%m-%d'`로 KST 날짜를 확정해 `TODAY_KST`로 고정한다. 이후 모든 STEP이 이 값을 그대로 쓴다 (재판정 금지).

② **현황표 두 계열을 먼저 fetch한다**:
- `[0]` 계열 — `37cbfe4a-2ae1-819f-8664-ff3d38fffe56`
- `[1]` 계열 — `37cbfe4a-2ae1-81b9-a9ce-d0b937edd344`

⚠️ 여기서 읽은 결과를 **STEP 1에서 재사용한다** (같은 페이지를 두 번 fetch하지 않는다).
💡 부수 효과: MAX_NUM은 원래 양 계열 합산인데 그동안 `[0]`만 보고 번호를 잡을 위험이 있었다(2026-08-10 #80 누락 사례). 이 단계가 그 확인을 강제한다.

③ **오늘 회차가 이미 끝났는지 판정한다** — 판정 기준은 가이드 `0-7 ①`을 그대로 적용한다(현황표 로그가 유일한 기준이며 초안 페이지 존재 여부로 판정하지 않는다).
두 페이지 중 **어느 한쪽에라도** `트렌드 초안 작성 실행: {TODAY_KST}` 가 있으면 → **즉시 종료**한다.
종료 출력은 한 줄로 끝낸다:
`⏭️ {TODAY_KST} 회차 이미 완료 — 재시도 트리거 무동작 종료 (해당 글 #XX)`
⚠️ 종료 시 **아무것도 쓰지 않는다** — 현황표·백로그·반려 로그 수정 금지, 알림 발송 금지.

④ **두 곳 모두에 없으면** 1차 트리거가 실패한 것이다. `RETRY_MODE = true`로 두고 **STEP 0부터 전체 루틴을 정상 실행한다.** 누락 판정·기록·시즌 창 보상은 가이드 `0-7 ②~⑥`을 따른다.

⚠️ **이 가드는 잠금(lock)이 아니라 시차 확인이다.** 실행 로그는 STEP 7에서야 기록되므로, 1차 실행이 아직 진행 중인 시점에 재시도가 뜨면 가드를 그냥 통과해 **같은 날 글이 2개 만들어지고 번호가 충돌한다.** 재시도 트리거는 1차보다 **최소 3시간 뒤**에 둔다 (1회차 소요 실측 약 25~30분).

⚠️ **재시도는 같은 KST 날짜 안에 끝나야 한다.** 자정을 넘기면 '일' 숫자가 바뀌어 트랙이 뒤집히고, 놓친 홀수일은 되돌릴 수 없다 (가이드 `0-7 ③`). 재시도를 저녁 시간대로 미루지 말 것.

⚠️ **홀수일 우선 (트랙 S 보호)**: 1차 실패의 손실은 홀수일이 훨씬 크다 — 시즌·이슈 선점 창은 날짜가 지나면 그 해에 다시 오지 않는다(가이드 `0-4`). 따라서 **재시도 트리거는 홀수일에 반드시 건다.** 짝수일은 트랙 L이라 에버그린이고 하루 밀려도 회수 가능하므로 선택이다 — 매일 걸어도 무방하다(가드가 무동작으로 끝나 비용은 Notion fetch 2회뿐이다).

📅 **예약 설정 (Cowork에서 직접 생성)**

| 트리거 | 시각 (KST) | 반복 | 프롬프트 | 비고 |
|---|---|---|---|---|
| 1차 | 현행 유지 (≈05:25) | 매일 | v4.3 | 변경 없음 |
| **재시도** | **11:00** | **홀수일** | **v4.3 (동일 프롬프트)** | 1차 대비 +5.5시간 · 자정까지 13시간 |

- 홀수일 반복을 cron으로 쓰면 `0 11 1-31/2 * *` (1·3·5·…·31일). 월말 31일 다음 1일이 연속 홀수가 되는 것은 정상이다 — 가이드 `0-3`이 연속 홀수를 허용한다.
- 스케줄러 UI가 요일 단위만 지원하면 **매일 11:00**으로 걸어도 된다. 짝수일에는 가드가 무동작으로 끝난다.
- ⚠️ **프롬프트는 1차와 완전히 동일한 것을 쓴다.** 재시도용 별도 프롬프트를 만들면 두 벌을 따로 관리하게 되고, 그게 지금까지 상충을 만들어온 패턴이다.

⚠️ **이 가드가 막지 못하는 것 (정직하게 기록)**
- 1차와 재시도가 **둘 다 실패**하는 경우 — 같은 계정·같은 클라우드 환경이라 장애가 반나절 이상 이어지면 둘 다 죽는다. 시각을 5.5시간 벌린 것이 확률을 낮출 뿐 0으로 만들지 못한다.
- **원인 자체** — 컨테이너 프로비저닝 실패는 플랫폼 사안이고 루틴 권한 밖이다. 이 가드는 원인이 아니라 **노출**을 줄인다.
- **두 블로그 동시 실패** — 근본 해소는 두 블로그의 1차 예약 시각을 벌리는 것이다(미채택). 실측상 두 작업이 **04:55~05:30의 같은 창**에 몰려 있다: 8/13 KoreaPlug 06:02 / 0and1Life 06:12 · 8/14 05:29 / 05:52 · 8/16 05:21 / 05:53.

---

STEP 0 — 트랙 판정 + 시즌 창 판정 + 네트워크 상태 판정 (최우선, 세션당 1회)
① 트랙: KST 오늘 날짜의 '일' 숫자: 홀수 → 트랙 S(숏테일 선점형) / 짝수 → 트랙 L(롱테일 공백형)

② **(v4) 시즌 선점 창 판정 — 홀수일이면 반드시 수행**: 가이드 `0-4 연간 시즌 캘린더`에서 **오늘이 포함된 선점 창**(창 폭은 가이드가 정한다)이 있는지 확인하고 `SEASON_WINDOW = 열림/닫힘`을 확정한다.
- 열림 → 이벤트명·D-day·남은 일수를 로그에 적고, STEP 2에서 **엔진 C를 최우선 실행**
- ⚠️ 음력 기반(설날·추석) 날짜는 **매년 실제 날짜를 확인**한다. 모델 기억 단정 금지 — 확인 불가 시 그 이벤트는 이번 회차 제외하고 STEP 8에 기록
- ⚠️ 이 판정은 **네트워크 상태와 무관하게 항상 수행한다** (가이드 내장 표만으로 가능)

③ **(v4) L 대체 연속 횟수 확인**: 현황표(STEP 1)에서 최근 홀수일 3회의 `Track` 기록으로 `L_FALLBACK_STREAK`를 센다. 3 이상이면 이번 회차는 **시즌 후보 강제 채택 모드**(STEP 3·4-B에 적용).
⚠️ **(v4.3) 미실행 회차는 카운트에서 건너뛴다** — 증가도 리셋도 하지 않는다 (가이드 `0-7 ②`).

④ WEBFETCH_STATUS: 구글 자동완성 API(hl=ko) 1회 조회 → 정상/차단 확정. STEP 1~8 전체가 이 값을 그대로 사용한다 (재판정 금지). 단, 이는 '조직 네트워크 차단' 여부 판정이며 개별 사이트 자체 차단(디시인사이드 봇 차단, 특정 RSS 타임아웃 등)과는 별개다 — WEBFETCH_STATUS=정상이어도 특정 도메인이 개별 실패할 수 있으며, 이 경우 전체를 차단으로 재판정하지 않고 해당 소스만 건너뛰고 나머지 엔진/소스를 계속 진행한다 (가이드 v1.20).

STEP 1 — Read Reference Materials from GitHub (검색 없이 먼저 실행)
아래를 동시에 읽는다:
작성 가이드: WebFetch → https://raw.githubusercontent.com/leejc0404/blog/main/0and1Life-Writer.md (반드시 준수)
전체 글 현황표: **STEP 0-⓪ ②에서 이미 fetch한 두 계열 결과를 재사용한다** (재fetch 금지)
→ 기존 제목 전체 + 포커스 키워드 + 현재 최고 번호 MAX_NUM 확인 (**양 계열 합산**)
→ 최근 2주 발행 패턴 확인 (예: "OO 계산법" 계열이 연속 발행됐으면 같은 패턴 재사용 자제)
→ **(v4) 최근 홀수일 3회 `Track` 기록 확인** (`L_FALLBACK_STREAK` 계산용)
→ **(v4.3) 직전 날짜들의 실행 로그 존재 여부 확인** — 누락된 회차가 있으면 가이드 `0-7`에 따라 판정하고 STEP 8에 기록
→ **(v4) 시즌 이벤트별 기발행 실행 수식 확인** (같은 이벤트 재발굴 시 각도 중복 회피)
→ **(v4) 가이드를 읽은 뒤 반드시 확인할 것**: `0-3 L 대체 상한` / `0-4 연간 시즌 캘린더` / `0-5 헤드 자격 3종` / `0-6 트랙 S 채점` / `0-7 회차 누락 처리` / `1-T ①②③`
키워드 백로그 (Notion, 2026-07-26 신설): https://www.notion.so/3a9bfe4a2ae181e08fc9dc8e035388af
→ 상태='대기' 항목 전체 확인 — STEP 2 엔진 D의 입력
발행 반려 로그 (Notion, 2026-07-30 신설): https://www.notion.so/3adbfe4a2ae1817994f0f901de5c8dec
→ 최근 7일 행 확인 — 같은 사유 코드가 2회 이상 반복된 주제 유형은 이번 회차 후보에서 제외. '조달 주체=사용자'인 미해결 행이 3건 이상이면 이번 회차는 루틴 조달 가능 주제만 선정 (가이드 1-3 v1.26 관문의 입력 자료)
최신 주간 GSC 리포트 (Notion, 페이지 ID 398bfe4a-2ae1-81ba-9ec6-fdae2acd6b2d): "## 리포트 (최신순)" 맨 위 섹션의 특이사항·다음 조치 제안 확인 — 후보 우선순위에 반영 (예: "프로포즈/웨딩·AI상담 계열 후속 우선" 제안이 있으면 그 클러스터 후보를 상위 배치)

STEP 2 — Candidate Brainstorm (v1.20 3중 발굴 엔진 — 후보 10~15개 생성, WebSearch 0회 원칙 — 항상, 예외 없음)
[짝수일 · 트랙 L]
내장 CPC 기준표 + STEP 1에서 읽은 발행 이력 + 엔진 A 시드 마이닝(아래)으로 3~5단어 질문형·계산형 에버그린 후보 10~15개 생성.
WebFetch 차단 시: 엔진 A 시드 마이닝을 생략하고 내장 CPC 기준표 + 발행 이력만으로 브레인스토밍한다(수요 확인은 STEP 4-A로 이관 — 검증 자체는 절대 생략하지 않는다).
최근 반복 발행된 소재 패턴은 회피하고 서브카테고리를 다변화한다.

[홀수일 · 트랙 S] 아래 3개 엔진을 모두 실행 — 우선 WebFetch/Bash, 예산 미소모. 실행·우선 순서 (v1.27): 엔진 D(백로그) → **엔진 B(실시간 급상승 이슈 — 트랙 S 최우선 소스)** → 엔진 A(자동완성 — B에서 수확한 헤드의 수요 확인·서브키워드 수확 보조로 활용) → 엔진 C. 舊 순서(A 요일 시드 우선)는 고정 카테고리에서 출발해 매일 비슷한 에버그린 돈 후보만 만들었다 — 트랙 S의 출발점은 '오늘·어제 가장 핫한 것'이다 (가이드 0-2 v1.27). B가 차단으로 생략되면 엔진 A 상시 시드('요즘 화제'/'요즘 이슈')가 실시간 헤드 수확을 대신한다. WebFetch 차단 시: 엔진 A는 엔진 C(시즌 캘린더)·발행 이력 기반 대체모드로 전환(WebSearch 사용 안 함 — STEP4-B 예산 보호, v1.20), 엔진 B는 생략, 엔진 C는 원래도 차단 무관 실행. 로그에 "WebFetch 차단 → 엔진 A 대체모드, 엔진 B 생략" 기록. 엔진당 최소 3개, 합계 10~15개 후보.

엔진 D — 키워드 백로그 (2026-07-26 신설 — 트랙 공통 최우선, 예산 미소모)
STEP 1에서 읽은 백로그의 '대기' 항목을 후보군 맨 앞에 배치한다.
- 출처=GSC쿼리 항목은 구글이 이미 이 사이트를 노출시키는 실측 쿼리이므로 STEP 4-A 수요 검증 통과로 간주한다. 단 STEP 3(중복 대조·유명 툴 배제·재테크 편중 방지)과 STEP 4-B(공백 실측)는 동일하게 적용.
- 출처=네이버AC·데이터랩 항목은 이 블로그의 실제 유입 주전장(네이버) 실측 신호이므로, 4-A에서 구글 자동완성 미노출이어도 그 이유만으로 탈락시키지 않는다 (네이버 증거를 수요 증거로 인정).
- 그 외 출처는 일반 후보와 동일하게 4-A를 거친다.
- 백로그 항목이 최종 채택되면 STEP 7 완료 후 백로그 해당 행의 상태를 '채택 #XX'로, 필터·검증에서 탈락하면 '탈락(사유)'로 갱신한다 (re-fetch 후 old_str 구성).
- 백로그가 비어 있거나 전부 부적합이면 아래 엔진 A~C로 정상 진행 (백로그는 추가 후보군이지 대체가 아님 — 합산 10~15개 목표는 동일).

엔진 A — 자동완성 시드 마이닝 (수요 직접 수확)
구글 자동완성 API(hl=ko)를 시드별로 8회 이상 조회해 2~30대가 '지금' 검색 중인 조합을 직접 수확:
https://suggestqueries.google.com/complete/search?client=firefox&hl=ko&q={시드}
⚠️ **(v4.3) 이 조회는 Bash `curl` + euc-kr 디코드로 수행한다** (프롬프트 상단 참조). WebFetch는 한글이 깨진다.
요일 무관 상시 시드 (v1.20 — 매일 포함): "요즘 화제" / "요즘 이슈" — 그날 지정 카테고리 밖에서 터진 트렌드를 놓치지 않기 위한 보완 시드.
⚠️ **(v1.34 개정) 요일별 시드 로테이션을 폐기한다.** 舊 v1.17 표(월=재테크·지원금 / 화=세금·연금 / 수=부동산·신혼집 / 목=대기업·커리어 / 금=결혼 준비 / 토=AI·효율·절약 / 일=디저트·핫플)는 카테고리를 요일에 못 박는 방식이라, 그 카테고리가 한도로 막혀 있으면 그날 발굴 자체가 통째로 공전했다. 가이드 v1.32의 6개 생활 영역과 **3회차 연속 구조적 충돌**(8/6·8/7·8/8)이 보고됐고, 실제로 8/6·8/8은 요일 시드를 쓰지 않고 생활 영역으로 발굴해 **2회 연속 채택**(#77·#78)으로 이어졌다.

**시드 선정 v1.34 — 요일과 시드를 연결하지 않는다:**
① 가이드 v1.34 **카테고리 균형 규칙(R1~R5)**으로 오늘 쓸 수 있는 카테고리 목록을 먼저 확정한다
② 그 카테고리에 해당하는 백로그 '대기' 항목을 1순위로 검토한다 (엔진 D)
③ 백로그에 적합한 것이 없으면 해당 카테고리의 **6개 생활 영역** 시드로 발굴한다:
　① 집·자취 운영 ② 기기·구독 실사용 비교 ③ 소비 판단 ④ 생활 이동 ⑤ 시즌·행사 ⑥ 생활 루틴 비용 비교
④ 엔진 B(실시간 급상승)는 요일과 무관하게 상시 병행하되, 급상승 헤드는 반드시 v1.27 각도 전환을 거쳐 카테고리에 편입시킨다
각 시드에 '+방법/조건/신청/계산/후기/디시/2026/비교/언제까지' 접미 변형 조회 (v1.20 — 비교형·시점형 추가) — '디시/더쿠/짤' 접미가 자동완성에 발견되면 커뮤니티 버즈 = 트렌디함 가산점. 수확 문구 중 발행 이력에 없는 실존 조합을 후보화 (급상승·시의성 조합은 S, 에버그린 조합은 L 예비로 보관).
(WebFetch 차단 시: 엔진 A는 실시간 조회를 하지 않고 위 대체모드로 진행 — v1.20)
💡 시너지 메모: 수확 중 '외국인/foreigner/영문' 조합(예: 캐치테이블 외국인 예약)이 발견되면 KoreaPlug 소재로 STEP 8 리포트에 메모.

엔진 B — 이슈 소스 스캔 (아래 소스를 이 순서로 스캔하고 종료, 가이드 0-1 — WebFetch 차단 시 전체 생략):
Google Trends 급상승 RSS: https://trends.google.co.kr/trending/rss?geo=KR → (v1.27) 접점 필터 없이 급상승 헤드 전체 수집 — 접점 판정은 각도 전환 후에 한다
네이버 뉴스 랭킹 WebFetch: https://news.naver.com/main/ranking/popularDay.naver → (v1.27) 접점 필터 없이 랭킹 헤드 수집 (인물 가십·정치 공방만 제외)
IT·AI 뉴스 헤드라인 WebFetch: 긱뉴스(https://news.hada.io), AI타임스 → 신생 툴·제도 변경·트렌드 신호
정책브리핑 RSS WebFetch (v1.15 — 트랙 S 주력 소스): https://www.korea.kr/rss/policy.xml — '에너지캐시백', '청년 부트캠프' 같은 신청형 제도 기사가 그대로 후보. 제목에서 2~30대 접점(지원금·세제·캐시백·청년·청약·대출)만 필터. 보조: 고용노동부·국세청·금융위 보도자료 페이지
신생 AI 툴 출시 WebFetch: 긱뉴스 Show/GN⁺, Product Hunt 상위 — 출시 6개월 이내 신생 툴(가이드 1-0 v1.7 허용 각도, 정리글 경쟁 없음)
⭐ (v1.27) 수집한 헤드 각각에 가이드 1-T ①의 각도 전환을 시도한다 — '헤드(1~2단어) + 실행/대상 수식(1~2단어)' 하이브리드 숏테일로 변환하고, 전환 성공분만 후보화한다 (예: 급상승어 '손주' → '손주 돌봄수당 신청 조건'). 전환이 안 되는 헤드만 버린다 — 접점 필터로 헤드를 먼저 죽이지 말 것.
(커뮤니티 버즈 신호는 엔진 A의 '디시/더쿠/짤' 접미 필터로 대체 — 디시인사이드·클리앙은 알려진 차단 사이트라 직접 방문하지 않음, v1.20)

엔진 C — 시즌·세금 캘린더 (**v4 승격: SEASON_WINDOW 열림 시 1순위**, 내장 표 기반, WebFetch 차단과 무관하게 항상 실행)
가이드 `0-4 연간 시즌 캘린더`에서 **오늘이 속한 선점 창**의 이벤트를 읽는다 (창 폭·마감형/구간형 판정은 가이드 0-4가 정한다). (舊 "2~4주 내"라는 어림 대신 표에서 읽는다 — 표가 없어 매 실행마다 재발명하던 것이 시즌 글 공백의 원인이었다.)
- 열린 창의 이벤트마다 **실행 수식**을 붙여 가이드가 정한 하이브리드 형태의 후보를 **최소 3개** 만든다 (개수 3은 루틴 소관, 형태 요건은 가이드 소관)
  - 실행 수식 예: 신청 조건 · 납부 기한 · 예매 · 계산 · 인상률 · 지급일 · 한도
  - 예: 추석 D-40 → `추석 기차표 예매` / `명절 상여금 세금` / `추석 연휴 병원`
- 현황표에서 **같은 이벤트로 이미 쓴 실행 수식은 제외**
- 정책 시행 D-day는 엔진 B 정책브리핑 RSS에서 수집해 창 규칙 없이 즉시 후보화
- ⚠️ 창이 닫힌 이벤트는 후보로 만들지 않는다 (노출 전에 검색이 끝난다)
- ⚠️ 이벤트 날짜 미확정(음력 등 확인 실패) 시 건너뛰고 STEP 8에 기록
- ⚠️ **(v4.3) 직전 홀수일이 누락됐고 그날 창이 열려 있었으면**, 가이드 `0-7 ④`에 따라 이번 회차 숏리스트 1순위를 시즌 후보로 강제한다

> 🔴 **(v4) 엔진 C는 생략 불가다.** 엔진 A·B가 전멸해도 엔진 C만으로 트랙 S가 성립한다. **엔진 C를 돌리지 않은 채 "S 후보 없음 → L 대체"를 선언하는 것은 규칙 위반이다.**

⚠️ 각도 전환 의무 (가이드 1-T v1.14 — 핵심 규칙): 이슈 헤드 키워드(정년연장, 환율 등)를 그대로 후보로 삼지 않는다. 뉴스는 '무슨 일이 일어났나'를 쓰고 우리는 '그래서 2~30대인 나는 뭘 해야 하나'(신청 방법·계산·타이밍·내 상황 영향)를 쓴다. 후보 키워드는 하이브리드 숏테일: 이슈 헤드(1~2단어) + 실행/대상 수식(1~2단어), 총 2~4단어 — 자동완성에 실존하는 조합만 (예: '정년연장 국민연금', '달러 환전 우대').

→ 합산 후보 10~15개 수집 (출처 엔진을 로그에 표기).
→ **(v4) 후보가 빈약할 때의 처리 순서**: ① 엔진 C 재확인(선점 창 전체 재검토) → ② 그래도 0개면 트랙 L 로직으로 대체하고 "S 후보 없음 → L 대체" 기록. **엔진 C 확인 없이 L 대체 금지** (가이드 0-3).

광고단가는 검색하지 않는다 — 내장 AdSense CPC 기준표 사용 (v1.16 확장):
$$$ 재테크·ETF·보험·증권·대출·세금·퇴직연금 → $0.8 ~ $2.0+
$$$ 이직/커리어 교육·웨딩(스드메·예물·신혼여행) → $0.5 ~ $1.5 (제휴: 이직 플랫폼·클래스101·웨딩·여행)
$$ AI 도구·자동화·부업·온라인 강의·구독서비스 → $0.3 ~ $0.8
$$ 짠테크·앱테크·가계부·소비 관리 앱 → $0.3 ~ $0.8 (핀테크 앱 제휴)
$ AI 트렌드·생활팁·뉴스·디지털 문화 → $0.1 ~ $0.3
프록시 지표: 쿠팡 파트너스/크몽/클래스101/AI 도구·건기식·핀테크 제휴 연계 가능 여부, 트랜잭션 키워드 밀도

각 후보 예비 채점 (공백·경쟁 축은 실측 전이므로 제외):

⛔ **(v4.2) 채점 축·배점·판정 기준·가점·동점 처리는 전부 가이드 `0-6 트랙 S 후보 채점`을 그대로 적용한다. 이 루틴은 채점 기준을 자체 보유하지 않는다.**
舊 v1.27 문장(트렌디함/광고단가/선점성 축 정의와 "광고단가는 동점 시 타이브레이커로만")은 **삭제했다** — 가이드가 개정될 때마다 이 복사본이 구버전으로 남아 상충했기 때문이다.

- 트랙 S: 가이드 0-6 표의 축·배점 + 가점 규칙 + 타이브레이커 순서를 **가이드에 적힌 그대로** 쓴다
- 트랙 L: 가이드 0-6 말미가 정한 트랙 L 채점 방식
- 위 CPC 기준표는 **가이드가 광고단가를 요구할 때 참조하는 데이터 자산**일 뿐이다. 축으로 쓸지·언제 쓸지는 가이드가 정한다
- 루틴이 정하는 것은 **출력 표 형식뿐**: `순위 | 주제 (포커스 키워드) | 축1 /5 | 축2 /5 | 축3 /5 | 합계 /15` — 축 이름은 가이드 0-6 표에서 그대로 가져와 채운다

STEP 3 — Duplicate Check & Shortlist 확정 (검색 이전, 최우선 필터, v1.20)
STEP 2 후보 전체를 현황표와 대조한다.
기준 (v1.21 — 각도 기반 재정의): 기존 게시물과 핵심 키워드 2단어 이상이 겹치는 후보를 1차로 추린 뒤, 그중 같은 대상·같은 질문·같은 목적(각도)을 다루는 경우만 중복으로 최종 제거한다 — 단어만 겹치고 각도가 다르면 중복이 아니다 (예: "이직 연봉협상 꿀팁" vs "이직 연봉협상 실패 후기" → 중복 아님). STEP4-B의 GAP 판정 기준과 동일한 방식이며 검색 소모는 없다.
⚠️ **(v4.3) 자기잠식은 단어가 아니라 각도로 판정한다** — 최근 회차가 같은 조문·같은 계산표를 이미 소진했으면 그 재료를 재사용하지 말고 내부링크로 넘기고 다른 각도를 취한다 (2026-08-16 실측: #79가 민법 660조 역산표를 소진해 #86은 '안 지켰을 때의 손실 계산'으로 분리).

**(v4 / v4.2 개정) 헤드 자격 필터 — 트랙 S 전용**: 후보의 헤드가 **가이드 `0-5 헤드 자격`을 충족하는가?**
- **자격 종류·판정 기준·예외·불합격 예시는 전부 가이드 0-5가 정한다** — 루틴에 옮겨 적지 않는다 (v4.2)
- 루틴이 정하는 것은 **불통과 후보의 처리 동작**이다: 트랙 S에서 제외하되 **버리지 않고 L 예비 리스트로 이관**한다 (탈락이 아니라 이관)

**(v4 / v4.2 개정) 하이브리드 형태 필터 — 트랙 S 전용**: 포커스 키워드가 **가이드 Phase 0 트랙표·`1-T`가 정한 하이브리드 형태 요건**을 충족하는가?
- **어절 수 범위·수식 필요 여부·헤드 단독형 처리는 전부 가이드가 정한다** — 루틴에 옮겨 적지 않는다 (v4.2)
- 루틴이 정하는 것은 **기록 동작**이다: 불통과인데도 진행하려면 Track 행에 `L`로 적고 "S→L 대체"로 카운트한다. 이 카운트가 `L_FALLBACK_STREAK`의 입력이다

비중복·필터 통과 후보 0개 → **엔진 C 미실행이면 먼저 실행.** 실행 후에도 0개면 STOP: ⚠️ 경고 로그 + 사용자 알림 (재브레인스토밍 금지, 그날 종료)
비중복 후보 있음 → **가이드 0-6 채점 합계 순**으로 정렬, 상위 2~3개를 최종 숏리스트로 확정하고 나머지는 예비 리스트로 보관 (v1.20 — STEP 4-B가 비교·선정 방식으로 바뀌면서 비교 대상이 필요해짐)
**(v4) SEASON_WINDOW=열림이거나 L_FALLBACK_STREAK≥3이면 숏리스트 1순위를 반드시 시즌 후보로 둔다** (v4.3 — 가이드 `0-7 ④`의 누락 보상 조건도 동일하게 적용)
가이드 1-3의 재테크 편중 방지·유명 툴 일반 사용법 배제 규칙도 이 단계에서 함께 적용. (v1.26) 1급 자료 조달 계획·반려 로그 대조 관문(가이드 1-3)도 함께 적용 — 각 후보에 조달 계획(`루틴가능`/`사용자필요`/`사용자필요(앱전용)`/`불가`) 한 줄 판정, `불가`와 반복 반려 유형은 제거. 채택 후보의 조달 계획은 STEP 6 기본 정보 표에 "1급 자료 조달 계획" 행으로 기재 (Draft 루틴이 캡처 실행 시 참조).

STEP 4 — Web Validation + Gap Analysis (WebSearch는 여기 4-B에서만, v1.20)

[4-A] 수요 검증 — WebSearch를 쓰지 않는다 (v1.20, 필수 관문)
숏리스트 1순위부터 순서대로 검증: 구글 자동완성 API(hl=ko) Bash 조회(또는 WEBFETCH_STATUS=차단 시 발행 이력·엔진C·모델 지식 기반 대체 신호)로 노출 확인 + 서브키워드 2~4개 수집.
네이버 자동완성 API는 세션당 1회만 시도(차단 환경 많음) — 성공 시 교차 확인 보조, 실패 시 재시도 없이 진행.
`https://ac.search.naver.com/nx/ac?q={키워드}&st=100&frm=nv&r_format=json` (UTF-8 응답, `items[0]` 배열)
**(v4 신설) ▸ 시즌 헤드 · 제도 헤드는 별도 경로 — 자동완성 미노출은 탈락 사유가 아니다**
선점은 **검색이 터지기 전에 쓰는 것**이 목적이므로 "지금 자동완성에 안 뜬다"는 기회의 근거다. 아래 3종 중 **1개 이상**이면 통과:
1. 자동완성에 조합 또는 **헤드 단독**이 노출 (약신호 허용, 네이버 자동완성 포함)
2. 커뮤니티에 **작년 동기** 같은 질문 3건 이상 (엔진 A의 '디시/더쿠' 접미 신호로 대체 가능)
3. 이벤트·제도가 독자 행동을 직접 제약함이 **확정 사실** (납부 기한·신청 마감·예매 오픈일·시행일)
→ 3종 모두 불가일 때만 탈락. 서브키워드는 자동완성이 비면 **확정 사실 용어**(기한일·서식명·기관명·구간명)로 구성하고 근거를 STEP 8에 기록.

근거가 전혀 없으면 해당 후보 탈락 → 숏리스트/예비 리스트 다음 순위로 이동해 4-A 재시작 (무료, 몇 번이든 반복 가능).
4-A를 통과한 후보가 2개 모일 때까지(또는 리스트 전체 소진까지) 반복한다.
→ 리스트 전체를 다 써도 4-A 통과 후보가 0개면 STOP: ⚠️ 경고 로그 + 사용자 알림 (STEP3의 0개 상황과 동급).
검증 생략 후 진행 절대 금지 — 단, 검증 수단은 항상 무료(Bash/WebFetch/대체 신호)이며 WebSearch를 쓰지 않는다.

[4-B] 공백 실측 — 4-A 통과 후보 중 경쟁이 가장 적은 1개를 고르는 선정 절차 (v1.20)
쿼리: "{FOCUS_KEYWORD}" -site:0and1life.com
1. 4-A 통과 후보 1순위로 검색 1회 실행.
2. 공백 판정 (v1.14 재정의): 뉴스·공공기관·계산기 툴 도배는 탈락 사유가 아니라 수요 신호다. 탈락 기준은 오직 '개인·기업 블로그의 내가 쓰려는 각도와 동일한 정리형 글 3개 이상'일 때만. 다른 대상·다른 질문의 정리글은 탈락 사유가 아니라 GAP ⭐ 재료. 뉴스만 있으면 최적 기회로 판정.
3. 3개 미만(통과) → 그대로 채택, 종료 (예산 1회만 소모). 같은 검색 결과에서 개인 블로그·티스토리·브런치 상위 2개 URL 확보 (추가 검색 금지).
4. 3개 이상(포화) → 4-A 통과 후보 2순위로 검색 1회 더 실행. 두 결과를 비교해 경쟁이 상대적으로 더 적은 쪽을 채택한다 (둘 다 포화라도 반드시 하나는 채택 — 완벽한 블루오션을 요구하지 않는다, v1.20).
⚠️ 단, 가이드 1-3 **v1.21 경쟁 구도 관문**(1페이지에 개인 블로그 또는 커뮤니티 질문글 1개 이상)에 불통과한 후보는 공백 수치와 무관하게 **채택 불가**다. 이 경우 다음 순위로 이동한다.
→ 이 절차는 항상 최대 2회 안에 승자를 정하고 끝난다. 4-A 통과 후보가 1개뿐이면 비교 없이 그 후보로 검색 1회만 실행하고 결과를 GAP 재료로 활용한다 (탈락시키지 않음).

[4-C] 상위 2개 URL 읽기 — WebFetch (검색 예산과 무관. 차단 시: 4-B 검색 결과의 제목·스니펫으로 경쟁글 구조를 파악)
H2/H3 헤딩 전체 / 도입부 독자 전제 / 계산 예시·수치·실경험 유무

[4-D] GAP_REPORT 생성
✅ 경쟁글 공통 커버: [항목] → STEP 5에서 기본 포함
⭐ 경쟁글이 빠뜨린 각도: [1~2개] → STEP 5 H2로 반드시 반영 (없으면 발행 금지)
🚫 경쟁글 공통 SEO 제목 구조 → 훅 패턴 변경 (가이드 2-2)
⚠️ 블로그 정리글 없이 뉴스·공공기관만 있는 경우 → ⭐ "블로그 정리글 자체가 없음 — 계산 예시 + 직장인 공감 구조로 작성" 후 계속 진행
⚠️ 4-B에서 포화 후보를 그대로 채택한 경우(비교 후에도 경쟁 존재) → ⭐ "경쟁 존재 — 차별화 각도를 평소보다 강하게 반영, 개인 경험/계산 예시로 승부" 후 계속 진행 (v1.20)

STEP 5 — Write the Blog Post (스킬 2종 필수 적용 — 가이드 2-6)
[5-0] Skill create-viral-content 호출 — 구조·훅 단계 (초안 작성 전)
SEO Title: 후보 10개 이상 생성 → 호기심/구체성/감정 3축 스코어링(각 0~3, 합 7점 이상만 채택). 구체 수치("하루 1시간", "3분 세팅") 포함 후보 우선 — 가이드 2-2 훅 규칙과 병행
첫 문단(60단어 공감 후크): Before/After 압축("매일 야근하던 내가~") 또는 문제 직격 패턴 — 백과사전식 서론 금지
클로저: "어떠셨나요?" 류 engagement bait 금지, 독자가 오늘 바로 할 행동 1가지
정제 패스 최소 3개: Skeptic → Scroller → Editor

[5-1] 본문 작성 — 가이드를 STEP 1에서 이미 읽었으므로 각 섹션을 직접 참조하여 실행:
키워드 전략 → [가이드 Phase 1-0 → 1-4]: 트랙별 규칙(S=하이브리드 2~4어절 '이슈 헤드+실행 수식' — v1.14 / L=3~5어절 질문형·계산형), 서브키워드는 4-A에서 수집한 실존 문구 기반 2~4개 확정. AI 임의 조합 절대 금지.
대표 이미지 → 조달 절차는 이 루틴이 정의한다 (⚠️ 수정 2026-07-26 — 가이드에는 `<figure>/<img>` 마크업 형식만 있고 조달 규칙이 없다):
① Unsplash 검색 페이지를 조회해 **실존 photo ID**를 확보한다 (시드: FOCUS_KEYWORD의 영문 대응어 또는 카테고리 대표어).
② URL 형식: `https://images.unsplash.com/photo-{ID}?auto=format&fit=crop&w=1200&q=80`
③ **주입 전 그 URL을 1회 응답 확인한다.** 200이 아니거나 unsplash.com이 차단되면(2026-08-16 실측 HTTP 401, 8/9~8/10은 307) **photo ID를 추측해서 넣지 말고** 플레이스홀더 `[FEATURED_IMAGE_URL]`을 그대로 남긴다 — 0and1life-wordpress-draft 루틴 [4d]가 Chrome으로 Unsplash에서 조달해 교체한다 (정상 경로, 오류 아님). 검증하지 않은 URL을 넣으면 깨진 이미지가 그대로 발행된다.
④ 마크업은 가이드의 `<figure>/<img>` 표준 형식 준수, alt에 FOCUS_KEYWORD 포함.
기본 메타 정보 → [가이드 메타 정보 표 형식]: Slug(영문 소문자+하이픈, 3~5단어), SEO Title, Meta Description, Focus Keyword, Sub Keywords.
카테고리 확정 → [가이드 v1.34 배정 결정 트리 — 필수]: **한 문장 테스트**로 판정하고 겹치면 더 좁은 쪽으로 보낸다.
　💍 Wedding(WP 27, 결혼 준비 전 과정 — 최우선) > 💼 대기업 직장인 생활(WP 29, 고용관계에서 발생하는가) > 📐 생활 규정·계약 계산(WP 30, 생활에서 나가는 돈을 조문·약관·요금표로 판정·계산) > 💰 직장인 재테크(WP 23, 국가·금융기관에 내거나 받는 돈인가) > 🏠 Life(WP 25, 공략·경험담)
　🤖 AI 정보(WP 26)는 **주 0건 동결** — 신규 배정하지 않는다.
기본 정보 표 Category 행에 '카테고리명 + WP ID'를 반드시 기재 (예: 📐 생활 규정·계약 계산 (WP 30)).
⚠️ **(v1.34) 舊 `⏱️ 시간·돈 절약 시스템` 라벨은 폐기**하고 WP 30을 `📐 생활 규정·계약 계산`으로 재정의했다. slug(`time-money-saving`)는 기존 URL 유지를 위해 변경하지 않는다.
⚠️ **카테고리 균형 규칙 R1~R5를 함께 적용한다** — 7일 창 상한·3연속 금지·순환 하한·예외 허용·형식 로테이션의 **구체 수치와 판정은 전부 가이드가 정한다** (v4.2 — 루틴에 수치를 적지 않는다).
HTML 본문 → [가이드 HTML 템플릿]: GeneratePress 기반 구조 준수. TOC는 <!-- TABLE OF CONTENTS --> 플레이스홀더 유지.
분량·밀도 → **가이드 2-5가 정한 값**을 그대로 따른다 (v4.2 — 수치는 루틴에 적지 않는다). ⚠️ 단위만 주의: **글자 수가 아니라 어절 수**다 (2026-07-26 사고 — 舊 루틴 표기 "1,500~2,500자"가 단위를 어긋나게 해 초안 재작성을 반복시켰다).
외부 권위 링크 → 최소 2개 (공식 기관·1차 출처).
내부 링크 → 최소 2개, 반드시 **개별 글 상대경로 `/slug/`** (⚠️ 신설 2026-07-26 — 조달 절차):
① 사이트(0and1life.com)·사이트맵을 긁지 않는다. 이 환경에서 자주 차단되고, 차단되면 슬러그를 추측하게 된다.
② STEP 1에서 읽은 현황표에서 이번 주제와 연관된 기존 글 2개를 고른다 → **그 글의 Notion 서브페이지를 fetch해 기본 정보 표의 Slug 값을 그대로 읽어 쓴다.** 네트워크 불필요, 항상 정확.
③ ⚠️ **레거시 예외 (2026-07-26 확인)**: #21~#65는 배포 후 서브페이지가 삭제되어 Slug를 읽을 서브페이지가 **없다**. 이 글들을 링크 대상으로 고를 때만 WordPress REST API로 슬러그를 조회한다 (인증 불필요, 공개 글):
`GET https://0and1life.com/wp-json/wp/v2/posts?search={제목 핵심어}&_fields=slug,title&per_page=3`
→ 응답 title이 현황표 제목과 일치하는 항목의 slug 사용. 2026-07-26 이후 글은 서브페이지가 보존되므로 ②로 해결되며 이 호출이 필요 없다. (2026-08-16 실측: 이 REST 경로는 프록시에서 **HTTP 200 정상 접근**된다)
④ `/category/...` 아카이브 경로로 대체하지 않는다 — Rank Math에서 category base가 제거된 경우 404가 되고, 개별 글로 가는 링크 가치도 사라진다.
⑤ ②③이 모두 실패하면 내부 링크를 1개로 줄이고 STEP 8에 그 사실을 기록한다 (추측 링크보다 낫다).
테마 컬러 → [가이드 카테고리-페이지 ID 매핑]: 서브카테고리별 헥사코드 적용.
💰 직장인 재테크 / 📊 업무에서 AI 활용법 / ⚙️ 실생활 AI 활용법 / 📈 핫한 AI 트렌드 / 🏠 Life·My Story / 💍 Wedding
(✅ 2026-07-26 확인 완료 — WP 카테고리 등록 확정: 💼 대기업 직장인 생활 = WP ID 29, slug corporate-worker-life / **📐 생활 규정·계약 계산** = WP ID 30, slug time-money-saving. 두 카테고리에 바로 배정한다 — 舊 임시 배정 규칙 폐기. 디저트·핫플 공략 글은 Life 배정. 확정된 카테고리에 대응하는 초안 보관 방은 STEP 6 표 참조 — 2026-07-26 신설)
⚠️ **(v4.1 정정 2026-08-09)** 이 줄의 舊 라벨 `⏱️ 시간·돈 절약 시스템`을 **`📐 생활 규정·계약 계산`으로 교체**했다. 가이드 v1.34가 WP 30을 재정의했는데 이 줄만 옛 표기로 남아 있어, 8/9 Writer 리포트가 표기 불일치로 보고했다. **slug(`time-money-saving`)는 기존 URL 유지를 위해 변경하지 않는다** — 라벨만 바뀐 것이며 WP ID·slug는 그대로다.
GAP_REPORT ⭐ 각도를 H2 1개에 반드시 반영.

[5-2] Skill avoid-ai-writing 호출 — 최종 패스 (HTML 완성 직후, Notion 업로드 전)
한국어 텍스트에 동일 원칙 적용: 균일한 문장 길이 깨기(짧은 문장 섞기), 기계적 전환어("또한", "한편", "결론적으로") 남발 제거, 과잉 감탄·홍보투 제거
"~합니다" 일변도 종결어미에 변화("~거든요", "~더라고요" 등 구어체 혼용 — 가이드 2-5 문체 규칙과 정합)
수치·고유명사·출처 링크·HTML 구조는 보존하고 문체만 교정

[5-3] 문체 지문 대조 — 가이드 2-6 ③ / Phase 5-6 (v4.1 신설 2026-08-09, [5-2] 직후 필수)
기준 본문: **STEP 1에서 읽은 가이드의 `Phase 5-6 문체 지문`**. 별도 조회 불필요(예산·네트워크 무관).
⚠️ 가이드 조회 자체가 실패했다면 이 단계를 건너뛰고 STEP 8에 `문체 지문 참조 불가`를 기록한다 ([5-2]는 이미 수행됐으므로 발행은 진행).
⚠️ [5-2]와 순서를 바꾸지 않는다 — [5-2]가 뒤에 오면 이 단계의 결과를 다시 평탄화한다. [5-2]는 뺄셈(AI 티 제거), [5-3]은 덧셈(운영자 문체로 맞춤)이다.

⛔ **(v4.2) 대조 기준은 전부 가이드 `Phase 5-6 문체 지문`에 있다. 목표 수치·금지어 목록·교정 순서를 루틴에 옮겨 적지 않는다.**
舊 v4.1이 복사해 두었던 항목(평균 어절·표준편차·`~습니다` 비율·금지어 나열·과분할 교정 순서)은 **삭제했다** — 가이드 Phase 5-6이 개정되면 이 복사본이 구버전으로 남기 때문이다.

루틴이 정하는 것은 **수행 방식**뿐이다:
- 원고를 **실제로 세어** 가이드 Phase 5-6과 대조한다 (인상 판단 금지 — 숫자를 산출한다)
- **불통과 항목만** 고치고 통과분은 건드리지 않는다
- 교정 순서는 가이드 Phase 5-6이 명시한 순서를 그대로 따른다
- 보존 대상: 수치·고유명사·출처 링크·HTML 구조는 그대로 둔다 (문체만 교정)
- ⚠️ **(v4.3) 5어절 이하 비율이 미달일 때 문장을 쪼개는 것으로 해결하지 않는다.** 가이드 Phase 5-6의 과분할 금지 조항대로, **분량을 늘리면서 짧은 문장과 긴 문장을 함께 추가**하는 것이 평균·표준편차·비율을 동시에 만족시키는 유일한 경로다 (2026-08-16 실측: 1,516 → 1,860어절로 확장하며 5어절 이하 26.6% → 48.6%, 평균 8.24 → 6.55, 표준편차 5.33 → 4.45로 전 항목 통과)
- 측정 결과는 STEP 8 `🖋️ 문체 지문` 행에 기록한다 (출력 형식은 아래 STEP 8 참조)

[5-4] 교정 후 [가이드 Focus Keyword 배치 체크리스트 6개 항목] + [가이드 5-5 AEO·GEO 체크리스트] + [가이드 5-3 문체 지문 체크 10개 항목] 교차 검증.

STEP 6 — Create Notion Page
⚠️ parent는 반드시 tool call의 최상위 파라미터로 지정 (pages[] 배열 내부 금지).
⚠️ 수정 2026-07-26 (2차): 카테고리별 **초안 보관 방**을 신설했다. parent는 STEP 5-1에서 확정한 카테고리에 따라 아래 방 중 하나로 지정한다. 방은 **WordPress 카테고리와 1:1**이라 AI 3종은 방 하나를 공유한다.
⚠️ **(v1.34 정정) AI 계열의 WP ID는 `18`이 아니라 `26`이다.** 2026-08-08 REST 실측 — `26 AI 정보(ai-guide)` 15건 / `18 0. 테크와 효율(tech-efficiency)` 4건. 18번은 레거시이며 신규 배정 금지.

| 확정 카테고리 | parent 방 page_id | WP ID |
|---|---|---|
| 📊 업무에서 AI 활용법 / ⚙️ 실생활 AI 활용법 / 📈 핫한 AI 트렌드 **(주 0건 동결)** | `3a9bfe4a-2ae1-8194-85db-cc0d029dbbd3` (📊 AI 활용) | **26** |
| 💰 직장인 재테크 | `3a9bfe4a-2ae1-8144-93b1-c2b1cd50bc4d` | 23 |
| 💼 대기업 직장인 생활 | `3a9bfe4a-2ae1-813d-b13b-cf2284c9f009` | 29 |
| 📐 생활 규정·계약 계산 (舊 ⏱️ 시간·돈 절약 시스템) | `3a9bfe4a-2ae1-81fc-98e2-d1d55449ed41` | 30 |
| 💍 Wedding | `3a9bfe4a-2ae1-817f-9b02-ceee29f98bff` | **27** |
| 🏠 Life·My Story / 디저트·핫플 공략 | `3a9bfe4a-2ae1-8139-9c8a-e16c92bc081e` | 25 |

⚠️ **(2026-08-10 REST 실측 정정)** Wedding은 **27**(글 4건), `1`은 Uncategorized(0건)다. `[1]` 현황표·Wedding 방 페이지에 남아 있는 'WP ID 1' 표기는 오기이며, 서브페이지 Category 행에는 **27**을 적는다 — 1로 올리면 미분류로 들어간다.
⚠️ 방과 현황표는 **계열이 일치해야 한다** — 위 표의 앞 4개는 `[0]` 계열이므로 STEP 7에서 `[0]` 현황표(37cbfe4a-2ae1-819f-8664-ff3d38fffe56)를, 뒤 2개는 `[1]` 계열이므로 `[1]` 현황표(37cbfe4a-2ae1-81b9-a9ce-d0b937edd344)를 갱신한다. 계열이 어긋나면 0and1life-wordpress-draft가 글을 찾지 못한다.
(배포 루틴 `[4a]`는 notion-search 우선 방식이라 방 구조와 무관하게 동작한다 — 2026-07-26 수정분.)
제목: Blog #XX — {포스트 제목} (XX = MAX_NUM + 1, **양 계열 합산 기준**)

🗄️ **초안 서브페이지는 영구 보존한다 (신설 2026-07-26 — KoreaPlug와 동일 정책).**
배포가 끝나도 삭제하지 않는다. 이 페이지가 두 가지 역할을 계속하기 때문이다:
- 0and1life-wordpress-draft `[4a]`가 배포 시 여기서 HTML_CONTENT를 읽는다 — 배포 **전에** 지우면 배포 자체가 실패한다.
- 이후 글들이 내부 링크를 걸 때 여기 기본 정보 표의 **Slug를 조회한다** — 배포 **후에** 지우면 슬러그 원본이 사라져 위 ③의 REST API 우회가 필요해진다.
(#21~#65가 배포 후 삭제되어 현재 0and1Life 허브에 초안 페이지가 하나도 남아 있지 않다. 이 정책은 그 재발을 막기 위한 것이다.)

2개 섹션 (섹션3 배포 JS 생성 금지 — 0and1life-wordpress-draft가 REST API로 직접 배포):
[섹션 1]: 가이드의 기본 정보 표 구조 생성 + 데이터 입력. Draft 일자는 반드시 공란.
⚠️ **(v4 필수) `Track` 행과 `Head Type` 행을 반드시 채운다** (가이드 2-1).
- `Track`: `S (숏테일 선점)` 또는 `L (롱테일 공백)` — **요일이 아니라 실제 채택 키워드로 결정한다.** S/L 판정 기준은 **가이드 0-3·0-5**가 정한다 (v4.2 — 어절 수·자격 조건을 루틴에 적지 않는다). 정직하게 적어야 `L_FALLBACK_STREAK` 상한 규칙이 작동한다
- `Head Type`: 트랙 S일 때만 — `이슈` / `시즌` / `제도` 중 하나 + 근거 한 줄 (예: `시즌 (추석 D-40, 선점 창 08-11~09-11)`). 신기술·신규 앱은 `이슈(신기술)`로 적는다 (가이드 0-5 v1.37)
- `Problem Hook`: 가이드 2-7 관문 — `[도입에서 세운 문제 1줄] → [답하는 H2 제목]` 형식. 신기술·앱 주제는 필수, 그 외 권장
- 이 행들은 일간 점검 루틴(daily-blog-health-check) 항목 9가 매일 읽어 규칙 준수를 판정한다. 비워 두면 판정 불가로 알람이 뜬다
[섹션 2]: 완성된 HTML 전체를 ```html 코드 블록으로 삽입.

STEP 7 — Update the Published Posts List
Page: STEP 6에서 고른 계열과 **동일한** 현황표를 갱신한다 (계열이 갈리면 배포 루틴이 글을 못 찾는다).
- [0] 계열 → https://app.notion.com/p/0-EFFICIENCY-37cbfe4a2ae1819f8664ff3d38fffe56
- [1] 계열 → https://app.notion.com/p/37cbfe4a2ae181b9a9ced0b937edd344

① 헤더 카운트 업데이트 — **조건부 실행** (⚠️ 수정 2026-07-26: 舊 지시는 `"전체 N개 글 현황 (YYYY-MM-DD 기준)"` 문자열을 하드코딩했으나 실제 페이지 헤더에 존재하지 않아 이 단계가 매번 조용히 실패했다)
re-fetch한 페이지 상단에 '전체 N개' 형태의 카운트 문구가 **실제로 있으면** N+1로 갱신한다.
없으면 이 단계를 건너뛰고 ②만 수행한다 — 이는 정상이며 오류로 보고하지 않는다 (현재 두 페이지 모두 카운트 없는 헤더).
② 표에 새 행 추가
⚠️ 실행 전 현황표 re-fetch 필수 — 마지막 행 실제 셀 값 확인 후 old_str 구성.
⚠️ old_str은 <td> 태그 포함 전체 행 그대로 복사 (bare text 불가).
업데이트 후 재fetch로 #XX 행 추가 여부 검증.
new_str: old_str + 아래 새 행:
<tr><td>{XX}</td><td>{SEO 제목 (한국어)}</td><td>{META_DESCRIPTION 앞 30자...}</td><td>—</td><td>{서브카테고리 이모지+이름}</td><td>{아웃링크번호}</td><td>—</td><td>{작성일자}</td><td></td></tr>
⚠️ 마지막 열(Draft 일자) 공란 유지 — 0and1life-wordpress-draft 루틴이 WP 배포 시 자동 기재
⚠️ SEO 점수 열 "—" 유지

🔁 **(v4.3) `RETRY_MODE = true`인 회차만**: 실행 로그 첫 줄에 아래를 먼저 적는다. 남기지 않으면 1차 트리거가 얼마나 자주 실패하는지 집계할 수 없고, 일간 점검 루틴이 정상 실행과 구분하지 못한다.
`🔁 재시도 트리거로 실행 (1차 트리거 미실행 — {TODAY_KST})`

✅ 트렌드 초안 작성 실행: {today_kst}
신규 draft: #{XX} {SEO 제목} (Notion 등록 완료, WP 배포 대기)
오류: 없음
다음 실행: {tomorrow} (다음 예약 트리거 시각 — 루틴 설정값)
⚠️ 수정 2026-07-26: 舊 표기 `{tomorrow} 07:00 KST`는 실제 예약 시각과 달랐고, 그 틀린 시각이 매 실행마다 Notion 실행 로그에 기록돼 왔다. 이 루틴은 자신의 예약 시각을 알 수 없으므로 **시각을 단언하지 않는다**. 날짜만 기재한다.
(WebFetch 차단으로 WebSearch 대체 경로를 탄 경우에도 '오류: 없음'으로 기재한다 — 정상 경로임. 진짜 오류(STOP, Notion 실패 등)만 오류 란에 기록)

STEP 7-B — 낙선 후보 백로그 적재 (신설 2026-07-27 — 이 블로그 백로그의 주 공급원)
매 실행이 후보를 10~15개 만들어 1개만 쓰고 나머지를 버려왔다. 그중 **이미 수요 검증을 통과한 것**을 백로그에 남긴다. 추가 검색·네트워크 비용이 0인 절차다.

적재 대상 — 아래를 **모두** 만족하는 것만, 최대 3건:
- STEP 4-A 수요 검증 통과 (구글 또는 네이버 자동완성에 **실존 문구로 노출 확인된 것**)
- STEP 3 중복 대조·필터 통과
- 4-B 비교에서 밀렸거나, 예산 때문에 4-B를 못 돌린 예비 리스트 상위 항목

⛔ 적재하지 않는 것: 4-A를 통과하지 못한 후보, 브레인스토밍 단계의 아이디어, 모델 지식으로만 떠올린 조합. 근거 없는 항목이 쌓이면 백로그가 오염되고 엔진 D의 '4-A 통과 간주' 전제가 무너진다.

기록 위치: https://www.notion.so/3a9bfe4a2ae181e08fc9dc8e035388af
행 형식: `| {다음 번호} | {키워드} | {구글AC 또는 네이버AC} | {자동완성에 실제 노출된 문구 그대로} | {클러스터} | — | {today_kst} | 대기 |`
⚠️ 적재 전 백로그 re-fetch → 기존 행 및 현황표 제목과 중복 대조. 이미 있는 키워드는 건너뛴다.
⚠️ 표 갱신 전 마지막 행 원문 확인 후 old_str 구성 (기존 Notion 갱신 규칙과 동일).
⚠️ 이번 실행이 엔진 D에서 백로그 항목을 소비했다면, 그 행의 상태 갱신(`채택 #XX` / `탈락(사유)`)을 먼저 끝낸 뒤 신규 행을 추가한다.

STEP 8 — Report
✅ 0and1life 테크와효율 Daily Automation — {today_kst} 완료
🔁 **(v4.3) 재시도 실행일 때만**: `🔁 재시도 실행 — 1차 트리거 실패`
⛔ **(v4.3) 직전에 누락된 회차가 있으면** 가이드 `0-7 ⑥` 형식으로 기록한다:
`⛔ 회차 누락: {YYYY-MM-DD} ({홀수일/짝수일}, SEASON_WINDOW {열림(이벤트 D-n)/닫힘}) — 연속 {n}회`
연속 2회면 🟡, 3회면 🔴로 표기하고 **예약 작업 점검을 사용자에게 요청**한다 (가이드 `0-7 ⑤`).
🔍 주제 탐색: 브레인스토밍 {M}개 / 중복 제거 후 {K}개 / 숏리스트 {J}개 / WebFetch 상태: {정상 또는 차단} / WebSearch 총 {N}회 (한도 2) / 트랙: {S 또는 L} / 수요 증거: {구글 자동완성 API·네이버 자동완성 API·데이터랩·엔진C·모델지식 중 확인된 것}
🎯 스킬 적용: create-viral-content (제목 스코어 {X}/9, 정제 패스 {n}개) / avoid-ai-writing (최종 패스 완료)
🖋️ 문체 지문(STEP 5-3): {적용 완료 또는 조회 실패} / 총 {X,XXX}어절 / 평균 {X.X}어절 / 표준편차 {X.X} / 5어절 이하 {XX}% / `~습니다` {XX}% / 금지어 {N}건
📝 선택 주제: [주제명] / 제외 후보: [중복 탈락 vs 수요검증 탈락 vs 4-B 비교탈락 vs v1.21 관문 탈락 구분하여 기록]
📂 위치: [서브카테고리명] — 테크와효율 블로그 글
🔗 Notion URL: [생성된 페이지 URL]
📋 목록 업데이트: #{XX} + 표 행 + 로그 추가 완료
💡 한줄 요약: **(v4) 앞에 트랙 태그를 붙인다** — `[S·시즌]` / `[S·이슈]` / `[S·제도]` / `[L]` / `[S→L 대체 {n}회 연속]` + 핵심 내용
📏 **(v4) 트랙 규칙 준수**: 포커스 키워드 `{키워드}` = {n}어절 / 헤드 `{헤드}` = {이슈·시즌·제도·자격없음} / Track 기록 `{S 또는 L}` / SEASON_WINDOW `{열림(이벤트 D-n) 또는 닫힘}` / L_FALLBACK_STREAK `{n}`
🧮 **(v4) 후보 채점**: 1순위 {키워드} 축1 {n}/5 · 축2 {n}/5 · 축3 {n}/5 = {합계}/15 (2·3순위도 합계만)
🔀 KoreaPlug 소재 메모: [엔진 A에서 외국인 관련 조합 발견 시]
📤 HTML 작성 완료 — 0and1life-wordpress-draft 루틴이 다음 실행 시 자동 배포합니다.
🪝 훅 점검: 직전 10개 제목 중 이번 제목과 동일 훅 사용 {n}회 (규칙상 2회 이상이면 다른 훅으로 교체 — 교체했으면 명시)
📐 카테고리 편중 점검: 이번 주(월~일) 카테고리별 발행 {카테고리}={n}건 (한도는 가이드 R1 기준 — 초과로 제외한 후보가 있으면 명시)
🚧 반려 로그: STEP 1 확인 {반복 유형 유/무} / 후보 제외 {n}건 / 이번 글 조달 계획: {루틴가능·사용자필요 + 내용 한 줄}
📦 백로그: 검토 {건수} / 채택 {0 또는 1} / 상태 갱신 완료 여부 / **신규 적재 {n}건** (STEP 7-B — 4-A 통과 낙선 후보. 0건이면 사유 명시)

⚠️ 전제조건
항목 | 조건
실행 환경 | Claude Code on the web — Chrome MCP 없음. WebFetch 우선 시도, 차단 시 WebSearch 자동 대체(가이드 v1.19 — 오류 아님). **자동완성 API는 Bash curl + euc-kr 디코드**(v4.3). 디시인사이드·클리앙 등 알려진 차단 사이트는 처음부터 접속하지 않음(v1.20)
재시도 가드 | STEP 0-⓪가 모든 STEP보다 먼저 실행된다. 오늘 로그가 이미 있으면 아무것도 쓰지 않고 즉시 종료. 판정 기준은 가이드 `0-7`
Notion 권한 | MCP 커넥터가 서브카테고리 페이지 편집 권한 보유
WebSearch 한도 | 글당 최대 2회, STEP 4-B 전용 (STEP 1~4-A는 WebSearch 미사용). 두 블로그 합산 최대 4회/일, 실제 플랫폼 한도(5회) 내. 예산 소진 시에도 STOP 아님 — 4-B는 숏리스트 내 비교로 항상 승자를 정함. 진짜 STOP은 STEP3·4-A에서 채택 가능한 후보가 0개일 때만
품질 관문 | 차단 여부와 무관하게 불변 — 수요검증 생략 금지(단, 검증 수단은 무료) / 서브키워드는 실존 노출 문구만(AI 임의 조합 금지) / GAP ⭐ H2 필수 / EEAT 강제 조건 유지 / v1.21 경쟁 구도 관문 불통과는 예외 없이 폐기
스킬 | create-viral-content(STEP 5-0) + avoid-ai-writing(STEP 5-2) 필수 적용
문체 지문 | STEP 5-3에서 가이드 Phase 5-6 대조 필수. 가이드 조회 실패 시에만 생략 가능하며 STEP 8에 기록. **통과·불통과 판정 기준은 가이드 Phase 5-6이 정한다** (v4.2 — 루틴에 수치를 적지 않는다)
중복 기준 | 기존 게시물 제목과 포커스 키워드 2단어 이상 겹침 시 중복 처리 (STEP 3에서 웹서칭 이전에 선행 적용). 단어가 아니라 **각도**로 최종 판정 (v4.3)

[결과물] 간단하게 표로 정리할 것. 모든 것은 한글로 얘기하고, 중요한 부분이 아닌 것은 생략한다.