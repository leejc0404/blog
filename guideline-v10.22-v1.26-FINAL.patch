diff --git a/0and1Life-Draft.md b/0and1Life-Draft.md
index df42ad5..b6b318f 100644
--- a/0and1Life-Draft.md
+++ b/0and1Life-Draft.md
@@ -92,6 +92,19 @@ wp.data.dispatch('rank-math').updateKeywords('AI 여행 계획,AI 여행 계획
 - [ ] **(이미지 출처 관문 — v1.24)** 본문 이미지가 전부 **자체 업로드 파일 또는 라이선스 소스(Unsplash 등)**인가? `gstatic.com`·`pstatic.net`·`phinf.naver.net` 등 타 사이트 핫링크가 **한 건도 없는가?** → No면 해당 이미지를 제거하거나 직접 촬영본으로 교체하기 전까지 업로드 중단
 - [ ] **(들어오는 링크 관문 — v1.23)** 이 글을 가리키는 내부 링크를 **기존 글 2개에 추가했는가?** 출처는 주제가 인접하면서 이미 내부 링크를 받고 있는 글로 고른다(고아 글에서 걸면 무효). → No면 링크를 추가한 뒤 완료 처리한다
 
+> 📋 **(v1.25 신설) 관문 불통과 시 리포트 형식 — 아래 4개를 반드시 채워서 사용자에게 알린다.** "1급 자료 없음"만 적는 보고는 무효 처리한다. 사용자가 무엇을·어디에·왜 넣어야 하는지 모르면 그 글은 그대로 방치되고, 자동화가 멈춘 것과 같아진다.
+>
+> 1. **무엇을** — 필요한 1급 자료를 구체적으로 지목한다. (예: "홈택스 → 연말정산 미리보기 → 신용카드 소득공제 계산 결과 화면 캡처")
+> 2. **어디에** — 삽입 위치를 H2 섹션 이름으로 지정한다. (예: "3장 '카드 사용액 황금 비율' 문단 바로 아래")
+> 3. **왜** — 그 자료가 본문의 어떤 주장을 뒷받침하는지 한 줄로 쓴다. (예: "체크카드 전환 시점 주장에 현재 근거가 없음")
+> 4. **어떻게** — 사용자가 직접 구하는 최단 경로를 사이트·메뉴·앱 이름까지 적는다. 촬영이 필요하면 무엇을 찍어야 하는지 명시한다.
+>
+> ⚠️ **1급 자료는 이미지에 한정되지 않는다.** 직접 측정한 소요 시간, 실제 지불한 금액, 실제로 받은 조건처럼 **본문 텍스트로 들어가는 것도 1급**이다. 이미지가 꼭 필요한 경우에만 이미지를 요구하고, 텍스트로 충분하면 텍스트로 요청한다.
+
+> 🚧 **(v1.26) 반려 시 로그 기록 — 보고만 하고 끝내지 않는다.** 위 관문에 걸려 업로드를 중단했으면, 발행 반려 로그(https://www.notion.so/3adbfe4a2ae1817994f0f901de5c8dec)의 표에 **행 1개를 반드시 추가한다.** 날짜 / 슬러그 / 사유 코드(`원본자료없음`·`가짜경험`·`이미지출처`·`들어오는링크`) / 무엇을 / 어디에 / 왜 / 어떻게 / 조달 주체(`루틴`·`사용자`) / 상태(`대기`)를 채운다.
+>
+> 이 기록이 없으면 writer는 다음 날에도 같은 방식으로 글을 쓰고, Draft는 같은 사유로 다시 반려한다. **두 단계는 서로 다른 실행이라 로그를 거치지 않으면 피드백이 전달되지 않는다.** 기록은 보고의 부속이 아니라 관문 처리의 일부다.
+
 - [ ] (공감 검증) 한국 직장인/부업러가 처음 3초에 "나 이거 해당되는데" 반응이 나오는가? → Yes여야 함.
 - [ ] (지식 검증) 직접 써보거나 검증된 정보만 담겼는가? → Yes여야 함.
 - [ ] (제목 검증) 제목만 보고 답이 금방 보이지 않고 "어떻게?", "진짜?" 궁금한가? → Yes여야 함.
diff --git a/0and1Life-Writer.md b/0and1Life-Writer.md
index f89a134..28f2653 100644
--- a/0and1Life-Writer.md
+++ b/0and1Life-Writer.md
@@ -160,6 +160,11 @@
 - [ ] **(v1.5) 서브키워드 인텐트 일치**: 서브키워드의 검색 의도가 글의 목적과 일치하는가? 방법형 글에 '조회형' 키워드(〜조회, 〜번호, 〜검색, 〜확인) 혼입 금지 — 자동완성에 노출돼도 인텐트가 다르면 순위에 도움 안 됨
 - [ ] **(v1.5) YMYL 주제 규칙**: 연금·세금·보험·투자 제도 주제인가? → 공단·공식기관·대형 금융사가 1페이지를 점령한 헤드 키워드는 회피하고 질문형·계산형 각도로만 접근 + 공식 출처 외부링크 필수. 단, 질문형이라도 뉴스가 이미 다루는 각도면 탈락이므로 반드시 1페이지 실측 (사례: '국민연금 조기수령 손해'도 뉴스 도배 확인되어 '국민연금 조기수령 손익분기점'으로 재타겟, 2026-07-12 / '가상자산 세금' → '코인 세금 언제부터' 재타겟)
 - [ ] **(v1.5) 콘텐츠 공백 실측**: 구글 1페이지에 동일 주제 정리글이 포화 상태가 아닌가? 뉴스·기업블로그 도배 키워드는 폐기했는가? (사례: 'Grok 활용법'은 기업블로그·유튜브 포화로 '그록 무료 사용법'로 재타겟, 2026-07-12)
+- [ ] **(v1.26) 1급 자료 조달 계획 — 후보 확정 전 필수, 계획 없으면 후보 탈락**: 이 주제로 **1급 원본 자료를 어떻게 확보할 것인지** 한 줄로 적는다. 적을 수 없으면 그 주제는 채택하지 않는다. 다 쓰고 나서 Draft 관문에 걸리는 것은 낭비다 — 판정은 **작성 전 이 자리에서** 끝낸다.
+  - `루틴가능` — 로그인 없이 조작 가능한 공개 계산기·공식 페이지의 결과 화면, 실제로 돌려본 테스트 결과. **이 유형을 우선 채택한다.** (화면 캡처의 실행 주체는 브라우저가 있는 Draft/Cowork 단계 — Writer는 필요한 화면과 조작 경로를 Notion 기본 정보 표에 명시해 넘긴다)
+  - `사용자필요` — 현장 사진, 실제 견적서·영수증·고지서. 채택 시 Notion 기본 정보 표에 명시하고, **주 1~2건을 넘기지 않는다.**
+  - `불가` — 아무도 확보할 수 없음 → **후보 탈락.** (2-5: 1급 자료를 확보할 수 없는 주제는 이 블로그에 맞지 않는 주제다)
+- [ ] **(v1.26) 반려 로그 대조 — 같은 실패 반복 차단**: 발행 반려 로그(https://www.notion.so/3adbfe4a2ae1817994f0f901de5c8dec)의 최근 7일 행을 읽었는가? 같은 사유 코드가 2건 이상 반복된 유형은 이번 회차 후보에서 제외했는가? `조달 주체=사용자`인 미해결 행이 3건 이상 쌓여 있으면, 이번 회차는 `루틴가능` 주제만 고른다.
 - [ ] **(v1.21) 경쟁 구도 관문 — 불통과 시 즉시 폐기, 예외 없음**: 해당 키워드 구글·네이버 1페이지에 **개인 블로그 또는 커뮤니티 질문글이 1개 이상** 있는가? 1페이지가 공단·은행·보험사·대형 언론·전업 상위노출 사업자로만 채워져 있으면 신생 도메인은 이기지 못하므로 **탈락**시킨다. 검색량·CPC가 아무리 높아도 예외 없음. (실측 2026-07-29 GSC: 3개월 노출 **93회**, 클릭 7, 노출된 쿼리 **7개** — 52편을 발행하고도 검색 유입이 사실상 0인 이유는 국민연금·전세자금대출·퇴직연금 등 국내 최고 난도 YMYL 헤드에 반복 진입했기 때문. 기존 1-3 YMYL 규칙은 '회피하라'는 서술만 있고 강제 관문이 없었다)
 - [ ] **(v1.7) 유명 툴 일반 사용법 각도 배제 — 트랙 S·L 공통**: 후보가 "{유명 툴}+사용법/활용법/가이드" 형태가 아닌가? 허용 각도(비교/특수 용도/신생 툴/실측 후기) 중 하나에 명확히 속하는가? 아래 반면교사 패턴과 겹치면 탈락.
   - 색인 거부 실측(GSC 2026-07): gemini-guide, copilot-guide, perplexity-guide, wrtn-ai-guide, gamma-ai-ppt-guide, nanobanana-ai-guide, chatgpt-prompt-guide, claude-workflow-guide, ai-meeting-notes-guide, ai-1hour-challenge, ai-side-hustle-automation, insurance-compare-ai-guide → 전부 '유명 툴/흔한 주제의 일반 가이드'
diff --git a/KoreaPlug-Draft.md b/KoreaPlug-Draft.md
index 4073f9d..4859e6f 100644
--- a/KoreaPlug-Draft.md
+++ b/KoreaPlug-Draft.md
@@ -90,6 +90,19 @@ wp.data.dispatch('rank-math').updateKeywords('korea business registration,korea
 - [ ] **(이미지 출처 관문 — v10.20)** 본문 이미지가 전부 **자체 업로드 파일 또는 라이선스 소스(Unsplash 등)**인가? `gstatic.com`·`pstatic.net`·`phinf.naver.net` 등 타 사이트 핫링크가 **한 건도 없는가?** → No면 해당 이미지를 제거하거나 직접 촬영본으로 교체하기 전까지 업로드 중단
 - [ ] **(들어오는 링크 관문 — v10.19)** 이 글을 가리키는 내부 링크를 **기존 글 2개에 추가했는가?** 출처는 주제가 인접하면서 이미 내부 링크를 받고 있는 글로 고른다(고아 글에서 걸면 무효). → No면 링크를 추가한 뒤 완료 처리한다
 
+> 📋 **(v10.21 신설) 관문 불통과 시 리포트 형식 — 아래 4개를 반드시 채워서 사용자에게 알린다.** "1급 자료 없음"만 적는 보고는 무효 처리한다. 사용자가 무엇을·어디에·왜 넣어야 하는지 모르면 그 글은 그대로 방치되고, 자동화가 멈춘 것과 같아진다.
+>
+> 1. **무엇을** — 필요한 1급 자료를 구체적으로 지목한다. (예: "홈택스 → 연말정산 미리보기 → 신용카드 소득공제 계산 결과 화면 캡처")
+> 2. **어디에** — 삽입 위치를 H2 섹션 이름으로 지정한다. (예: "3장 '카드 사용액 황금 비율' 문단 바로 아래")
+> 3. **왜** — 그 자료가 본문의 어떤 주장을 뒷받침하는지 한 줄로 쓴다. (예: "체크카드 전환 시점 주장에 현재 근거가 없음")
+> 4. **어떻게** — 사용자가 직접 구하는 최단 경로를 사이트·메뉴·앱 이름까지 적는다. 촬영이 필요하면 무엇을 찍어야 하는지 명시한다.
+>
+> ⚠️ **1급 자료는 이미지에 한정되지 않는다.** 직접 측정한 소요 시간, 실제 지불한 금액, 실제로 받은 조건처럼 **본문 텍스트로 들어가는 것도 1급**이다. 이미지가 꼭 필요한 경우에만 이미지를 요구하고, 텍스트로 충분하면 텍스트로 요청한다.
+
+> 🚧 **(v10.22) 반려 시 로그 기록 — 보고만 하고 끝내지 않는다.** 위 관문에 걸려 업로드를 중단했으면, 발행 반려 로그(https://www.notion.so/3adbfe4a2ae18167880ecbe3c73b90cc)의 표에 **행 1개를 반드시 추가한다.** 날짜 / 슬러그 / 사유 코드(`원본자료없음`·`가짜경험`·`이미지출처`·`들어오는링크`) / 무엇을 / 어디에 / 왜 / 어떻게 / 조달 주체(`루틴`·`사용자`) / 상태(`대기`)를 채운다.
+>
+> 이 기록이 없으면 writer는 다음 날에도 같은 방식으로 글을 쓰고, Draft는 같은 사유로 다시 반려한다. **두 단계는 서로 다른 실행이라 로그를 거치지 않으면 피드백이 전달되지 않는다.** 기록은 보고의 부속이 아니라 관문 처리의 일부다.
+
 - [ ] (Native Speech Check) 미국인 친구에게 자연스럽게 말할 수 있는가? → **Yes여야 함.**
 - [ ] (Authenticity Check) 한국인이 읽었을 때 로컬 문화를 왜곡하지 않았는가? → **Yes여야 함.**
 - [ ] (Curiosity Gap Check) 도입부 읽고 "왓? 진짜?" 호기심으로 스크롤을 내릴 수밖에 없는가? → **Yes여야 함.**
diff --git a/KoreaPlug-Writer.md b/KoreaPlug-Writer.md
index 820f8ab..44d7555 100644
--- a/KoreaPlug-Writer.md
+++ b/KoreaPlug-Writer.md
@@ -155,6 +155,11 @@
 - [ ] 기존 발행 목록과 중복되지 않는가?
 - [ ] 포괄 키워드가 아닌 구체적 질문형 롱테일인가? (예: `jjimjilbang guide` ❌ → `jjimjilbang tattoo rules` ⭕)
 - [ ] **(v10.4) 서브키워드 인텐트 일치**: 서브키워드의 검색 의도가 글의 목적과 일치하는가? how-to 글에 lookup형 키워드(number, search, check, status) 혼입 금지 (사례: korea business registration 글에 `registration number/search` 서브키워드 혼입 → 제거, 2026-07-09)
+- [ ] **(v10.22) 1급 자료 조달 계획 — 후보 확정 전 필수, 계획 없으면 후보 탈락**: 이 주제로 **1급 원본 자료를 어떻게 확보할 것인지** 한 줄로 적는다. 적을 수 없으면 그 주제는 채택하지 않는다. 다 쓰고 나서 Draft 관문에 걸리는 것은 낭비다 — 판정은 **작성 전 이 자리에서** 끝낸다.
+  - `루틴가능` — 로그인 없이 조작 가능한 공개 계산기·공식 페이지의 결과 화면, 실제로 돌려본 테스트 결과. **이 유형을 우선 채택한다.** (화면 캡처의 실행 주체는 브라우저가 있는 Draft/Cowork 단계 — Writer는 필요한 화면과 조작 경로를 Notion 기본 정보 표에 명시해 넘긴다)
+  - `사용자필요` — 현장 사진, 실제 견적서·영수증·고지서. 채택 시 Notion 기본 정보 표에 명시하고, **주 1~2건을 넘기지 않는다.**
+  - `불가` — 아무도 확보할 수 없음 → **후보 탈락.** (2-5: 1급 자료를 확보할 수 없는 주제는 이 블로그에 맞지 않는 주제다)
+- [ ] **(v10.22) 반려 로그 대조 — 같은 실패 반복 차단**: 발행 반려 로그(https://www.notion.so/3adbfe4a2ae18167880ecbe3c73b90cc)의 최근 7일 행을 읽었는가? 같은 사유 코드가 2건 이상 반복된 유형은 이번 회차 후보에서 제외했는가? `조달 주체=사용자`인 미해결 행이 3건 이상 쌓여 있으면, 이번 회차는 `루틴가능` 주제만 고른다.
 - [ ] **(v10.17) 경쟁 구도 관문 — 불통과 시 즉시 폐기, 예외 없음**: 해당 키워드 구글 1페이지에 **개인 블로그 또는 포럼 질문글(Reddit·Quora·Tripadvisor)이 1개 이상** 있는가? 1페이지가 전부 대형 매체·여행 대기업·공식기관이면 신생 도메인은 이기지 못한다. 검색량이 아무리 커도 **탈락**시킨다. (실측 2026-07-29 GSC: `gwangjang market` 72노출 0클릭·순위 52, `seoul apartment rent` 62노출 0클릭, `korean civil act age of majority` 99노출 0클릭 — 3개월 총 노출 9,140에 클릭 97, 평균순위 18.3위. 노출은 나오는데 전부 2페이지라 클릭이 0인 상태이며, 원인은 이 관문이 없었기 때문)
 - [ ] **(v10.17) 도시·명소·관광 헤드 금지**: `{도시명}` / `{시장·명소 이름}` / `{음식 카테고리}` 단독형 키워드는 채택 금지. 반드시 규칙·의미·조건 설명형 롱테일로 변환한다 (`gwangjang market` ❌ → `gwangjang market cash only` ⭕). 실제로 클릭이 발생한 쿼리는 전부 이 유형이었다 (`회식 meaning`, `sajangnim means`, `why do people wear masks in korea`, `pc bang food korea`)
 - [ ] **(v10.4) YMYL 주제 규칙**: 비자·연금·세금·보험·은행 주제는 정부·공식기관이 1페이지를 점령한 헤드 키워드를 피하고, 1인칭 문제해결형 각도(catch-22, 실패 경험, 실제 순서)로 차별화 + 타이틀에 타겟 청중(for Foreigners/Freelancers) 명시 + 공식 출처 외부링크 필수
