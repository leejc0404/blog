# 중복 대조 소스 이관 설계서 — Notion 현황표 → WordPress REST

작성일: 2026-08-25
범위: **중복 대조(기존 글 확인)만** 이관. 회차 카운트·현황표 갱신은 Notion 유지.
승인 전까지 어떤 파일도 수정하지 않음.

---

## 0. 왜 하는가 — 오늘 실증된 사례

2026-08-25 주간 점검에서 GSC 쿼리 `why do many elevators in south korea use the letter "f" instead of the number "4"?`(노출 91 · 순위 8.4 · 클릭 0)를 "전용 글이 없는데 8위"로 판단해 백로그 #22로 적재했다.

몇 시간 뒤 WP REST로 확인하니 **`no-4th-floor-korea` — "No 4th Floor in Korea: The Death Superstition Still Wired Into Every Elevator"** 가 이미 발행돼 있었다. 노출 91·8.4위는 공급 공백이 아니라 이 글이 이미 잡은 순위였다.

원인은 명확하다.

| 시도한 방법 | 결과 | 비용 |
|---|---|---|
| Notion 발행 목록 전문 대조 | **건너뜀** — 9.3만 자(약 2.3만 토큰)라 예산 부족 | 2.3만 토큰 |
| post-sitemap 슬러그 문자열 대조(대체 수단) | **놓침** — 슬러그에 `elevator`가 없음 | 낮음 |
| WP REST `?search=elevator` | **즉시 적발** — 제목·본문을 검색하므로 걸림 | **339토큰** |

비싸서 건너뛴 관문은 관문이 아니다. 이 이관의 핵심 근거다.

### 누적 오진단도 같은 뿌리다

| 기록 | 실제 |
|---|---|
| #90 Korea Bank Account "존재함" | WP에 없음 (3주째 미배포, 인바운드 10편이 404) |
| #125 Can You Vape "2026-08-04 발행됨" | 예약 상태였음 |
| 백로그 #16 "이미 발행됨"으로 탈락 | 발행 전이었음 → 판정 정정 |
| 일간 알람 "chuseok 글 없음" | 임시글 3301로 존재 |
| 백로그 #22 "전용 글 없음" | `no-4th-floor-korea` 존재 |

5건 전부 **Notion 사본과 WordPress 실제 상태의 불일치**다. 원천을 보면 이 오류 종류가 사라진다.

---

## 1. 무엇을 옮기고 무엇을 남기는가

현황표(발행 목록)는 성격이 다른 두 가지 일을 하고 있다. 이걸 섞어서 바꾸면 루틴이 깨진다.

### A. 옮긴다 — 중복 대조

"이 후보와 같은 각도의 글이 이미 있는가"를 묻는 일. 답은 워드프레스가 갖고 있다.

### B. 남긴다 — 회차 카운트 · 번호 · 상한 · 갱신

WordPress로 대체 **불가능**하다. 근거는 지침서 본문에 이미 적혀 있다.

> `KoreaPlug-Writer.md` L249
> ⚠️ **로그 블록·헤더 카운트·백로그 상태는 판정 근거로 쓰지 않습니다.** … **현황표 행 개수만** 셉니다.

> `0and1Life-Writer.md` L167
> **판정**: 현황표(`[0]`·`[1]` 양 계열) 어느 쪽에도 해당 날짜의 실행 로그가 없으면 누락이다. **초안 페이지 존재 여부로 판정하지 않는다 — 현황표 로그가 유일한 기준이다.**

구체적으로 WordPress에 존재하지 않는 것들:

| 항목 | WP에 있나 | 이유 |
|---|---|---|
| `TODAY_COUNT` (오늘 몇 회차 돌았나) | ❌ | STOP된 회차(발행 0건)도 세야 함 |
| `MAX_NUM` (Blog #번호 시퀀스) | ❌ | WP에 그 번호 체계가 없음 |
| Track 태그 `[L/에버그린]` · `[L/시한부]` | ❌ | Notion 전용 메타데이터 |
| 이벤트명 · 실행 수식 (연 3편·7일 간격 상한 판정용) | ❌ | Notion 전용 |
| 포커스 키워드 칼럼 | ❌ | 아래 §3 주의사항 참조 |
| 미달 회차 감지 | ❌ | 실행 로그 기반 |

---

## 2. 파일별 수정 위치 — 총 5개 파일 · 6곳

⚠️ `SKILL.md`는 **기록용 사본**이다. 실제 실행되는 것은 Cowork 예약 작업 `weekly-koreaplug-seo-check`에 저장된 본문이므로, 파일을 고치면 **예약 작업 본문도 같이 교체**해야 반영된다.

| # | 파일 | 위치 | 성격 |
|---|---|---|---|
| 1 | `Routine_KoreaPlug-Writer.md` | L158 STEP 3 ① 중복 대조 | 실행 절차 |
| 2 | `KoreaPlug-Writer.md` | L476 체크리스트 | 관문 |
| 3 | `KoreaPlug-Writer.md` | L484 체크리스트 | 관문 |
| 4 | `Routine_0and1Life-Writer.md` | L188 STEP 3 | 실행 절차 |
| 5 | `0and1Life-Writer.md` | L270 체크리스트 | 관문 |
| 6 | `SKILL.md` | L141 STEP 3 공통 규칙 | 주간 루틴 |

건드리지 않는 곳: `Routine_KoreaPlug-Writer.md` L36~48·L59·L63·L262 / `Routine_0and1Life-Writer.md` L35·L58~66·L303·L321~326 / `KoreaPlug-Writer.md` L180·L243·L249·L254·L268 / `0and1Life-Writer.md` L167

---

## 3. 공통 조회 규격 (각 파일에 삽입할 블록)

```
### 중복 대조 조회 규격 (WP REST)

GET https://{도메인}/wp-json/wp/v2/posts
    ?search={후보 핵심어}
    &status=any
    &per_page=20
    &_fields=id,slug,status,title

인증:
- writer 환경(Claude Code) — 앱 비밀번호 Basic 인증
- 주간 루틴(Chrome, wp-admin 로그인 상태) — 헤더 `X-WP-Nonce: wpApiSettings.nonce`

⚠️ `_fields`는 생략 금지. 실측(2026-08-25):
   `_fields` 지정      → 339토큰
   `_fields` 미지정    → 6,599토큰 (Astra 테마 meta 27개 필드가 따라옴)

⚠️ 인증 실패 시 `status=any`는 HTTP 400을 반환한다. 이때는 `status` 파라미터를
   빼고 발행분만 조회한 뒤 STEP 8에 「중복 대조 인증 실패 — 초안·예약 미대조」로
   기록한다. STOP 사유는 아니다.

⚠️ 후보 1건당 검색어 1~2개로 제한한다. 핵심어는 후보 키워드에서 고유명사·
   한국어 개념어를 고른다(the·in·korea 같은 범용어 제외 — 전체 목록이 돌아온다).
```

선례: `Routine_0and1Life-Writer.md` L273에 이미 같은 패턴이 있다 — `GET https://0and1life.com/wp-json/wp/v2/posts?search={제목 핵심어}&_fields=slug,title&per_page=3` (인증 불필요, 프록시 200 정상).

### 주의사항 — 포커스 키워드는 REST에 노출되지 않는다

2026-08-25 실측: `_fields=meta`로 조회한 결과 Rank Math의 `rank_math_focus_keyword`는 없고 Astra 테마 필드만 반환된다.

→ **"제목 + 포커스 키워드 대조"를 "제목 + 본문 검색"으로 치환한다.** `?search=`는 제목과 본문을 모두 훑으므로 실질 커버리지는 오히려 넓다(오늘 `no-4th-floor-korea`가 그 증거 — 슬러그·검색어 어디에도 `elevator`가 없지만 본문에서 걸렸다).

→ 포커스 키워드 정밀 확인이 꼭 필요한 후보에 한해, WP 검색에서 걸린 그 글의 Notion 현황표 행만 열어본다. 현황표 전문 읽기는 불필요.

### 주의사항 — 미배포 초안

writer가 Notion에 초안을 등록한 뒤 WP 배포 전이라면 WP 검색에 잡히지 않는다. 다만 `status=any`로 임시글·예약글까지 잡으므로 사각은 **같은 회차 내 시차**뿐이고, 그 구간은 이미 `SLOT >= 2` 같은 날 슬롯 충돌 필터가 담당한다.

---

## 4. 전문 수정안 (붙여넣기용)

### 수정 1 — `Routine_KoreaPlug-Writer.md` L158

**현재**

```
① **중복 대조** (각도 기반): 발행 목록과 비교해 핵심 키워드 2단어 이상 겹치는 후보를 1차 추린 뒤, 같은 대상·같은 질문·같은 목적(각도)인 경우만 최종 제거. 같은 이벤트·작품이라도 실행 수식·각도가 다르면 중복 아님 (단 이벤트별 발행 상한은 별개로 적용).
```

**수정 후**

```
① **중복 대조** (각도 기반): **WP REST 검색을 원천으로 삼는다** — `GET https://koreaplug.com/wp-json/wp/v2/posts?search={후보 핵심어}&status=any&per_page=20&_fields=id,slug,status,title` (인증: 앱 비밀번호 Basic). 반환된 제목과 비교해 핵심 키워드 2단어 이상 겹치는 후보를 1차 추린 뒤, 같은 대상·같은 질문·같은 목적(각도)인 경우만 최종 제거. 같은 이벤트·작품이라도 실행 수식·각도가 다르면 중복 아님 (단 이벤트별 발행 상한은 별개로 적용 — 상한 판정은 §STEP 0-② 현황표 소관이며 이 조회로 대체하지 않는다).
   - ⚠️ `_fields` 생략 금지(미지정 시 응답이 6,599토큰까지 부풀어 오른다 — 2026-08-25 실측). 인증 실패로 `status=any`가 400이면 `status`를 빼고 발행분만 조회한 뒤 STEP 8에 기록한다
   - ⚠️ **Notion 발행 목록은 중복 대조에 쓰지 않는다.** 2026-08-25 실측 사고 — 발행 목록이 2.3만 토큰이라 예산상 건너뛰고 슬러그 문자열만 대조한 결과, `no-4th-floor-korea`가 이미 있는데 백로그 #22를 중복 적재했다. WP 검색은 같은 판정을 339토큰에 해냈다
   - ⚠️ 포커스 키워드는 REST에 노출되지 않는다(Rank Math 메타 미등록, 2026-08-25 실측). `?search=`가 제목·본문을 모두 훑으므로 실질 대체되며, 정밀 확인이 필요한 후보만 해당 글의 현황표 행 하나를 연다
```

---

### 수정 2 — `KoreaPlug-Writer.md` L476

**현재**

```
- [ ] **검색 이전 중복 대조 + Foreigner Test — 최우선 관문**: 발행 목록 중복 대조와 Foreigner Test(`0-2`, 각도 전환 후 판정)를 모든 검증보다 먼저 끝냈는가? 탈락 시 새 주제를 즉흥 발명하지 않고 기존 리스트의 다음 순위로만 이동했는가?
```

**수정 후**

```
- [ ] **검색 이전 중복 대조 + Foreigner Test — 최우선 관문**: **WP REST 검색**(`?search={핵심어}&status=any&_fields=id,slug,status,title`)으로 중복 대조를, 그리고 Foreigner Test(`0-2`, 각도 전환 후 판정)를 모든 검증보다 먼저 끝냈는가? 탈락 시 새 주제를 즉흥 발명하지 않고 기존 리스트의 다음 순위로만 이동했는가?
  - 근거: 2026-08-25 주간 점검이 Notion 발행 목록(2.3만 토큰)을 예산 부족으로 건너뛰고 슬러그 문자열만 대조해 백로그 #22를 중복 적재했다. `no-4th-floor-korea`는 슬러그에 `elevator`가 없어 빠졌고, WP 검색은 339토큰에 잡아냈다. **비싸서 건너뛰는 관문은 관문이 아니다**
```

---

### 수정 3 — `KoreaPlug-Writer.md` L484

**현재**

```
- [ ] 기존 발행 목록과 중복되지 않는가? (같은 이벤트라도 실행 수식이 다르면 중복 아님 — 단 발행 상한은 별도로 적용)
```

**수정 후**

```
- [ ] **WP REST 검색 결과**와 중복되지 않는가? (같은 이벤트라도 실행 수식이 다르면 중복 아님 — 단 발행 상한 판정은 Notion 현황표 소관이며 이 조회로 대체하지 않는다)
```

---

### 수정 4 — `Routine_0and1Life-Writer.md` L188

**현재**

```
STEP 2 후보 전체를 현황표와 대조한다.
```

**수정 후**

```
STEP 2 후보 전체를 **WP REST 검색 결과**와 대조한다 — `GET https://0and1life.com/wp-json/wp/v2/posts?search={후보 핵심어}&status=any&per_page=20&_fields=id,slug,status,title` (인증: 앱 비밀번호 Basic). 한국어 검색어가 그대로 동작한다.
- ⚠️ `_fields` 생략 금지(미지정 시 응답이 6,599토큰까지 부풀어 오른다 — 2026-08-25 KoreaPlug 실측). 인증 실패로 400이면 `status`를 빼고 발행분만 조회한 뒤 STEP 8에 기록
- ⚠️ **현황표(`[0]`·`[1]` 계열)는 중복 대조에 쓰지 않는다.** 현황표는 회차 카운트(`TODAY_COUNT`)·번호 시퀀스·STEP 7 갱신 전용으로만 남는다 — STEP 0-⓪ ②의 fetch는 그대로 유지한다
- ⚠️ 포커스 키워드는 REST에 노출되지 않는다. `?search=`가 제목·본문을 모두 훑으므로 실질 대체되며, 정밀 확인이 필요한 후보만 해당 글의 현황표 행 하나를 연다
```

(바로 아래 "**중복 기준 (각도 기반)**" 문단은 그대로 둔다 — 판정 규칙이지 소스가 아니다.)

---

### 수정 5 — `0and1Life-Writer.md` L270

**현재**

```
- [ ] **검색 이전 중복 대조(최우선)**: 현황표(제목+포커스 키워드)와 대조를 모든 검증보다 먼저 끝냈는가. 단어가 아니라 **각도**로 판정한다 — 같은 대상·같은 질문·같은 목적이면 중복, 단어만 겹치고 각도가 다르면 중복 아님. 최근 회차가 같은 조문·같은 계산표를 소진했으면 재사용하지 말고 내부링크로 넘긴다.
```

**수정 후**

```
- [ ] **검색 이전 중복 대조(최우선)**: **WP REST 검색**(`?search={핵심어}&status=any&_fields=id,slug,status,title` — 제목+본문)과 대조를 모든 검증보다 먼저 끝냈는가. 단어가 아니라 **각도**로 판정한다 — 같은 대상·같은 질문·같은 목적이면 중복, 단어만 겹치고 각도가 다르면 중복 아님. 최근 회차가 같은 조문·같은 계산표를 소진했으면 재사용하지 말고 내부링크로 넘긴다.
```

그리고 같은 파일 L273도 함께 고친다.

**현재**

```
- [ ] 카테고리에 명확히 속하고 현황표 중복이 없는가.
```

**수정 후**

```
- [ ] 카테고리에 명확히 속하고 **WP REST 검색 결과에** 중복이 없는가.
```

---

### 수정 6 — `SKILL.md` L141 (주간 루틴 · 예약 작업 본문도 함께 교체)

**현재**

```
- 중복 제거 2중: ① 백로그 기존 행과 대조(**'탈락' 처리된 항목을 다시 넣지 말 것**) ② 발행 목록(KoreaPlug `33cbfe4a-2ae1-81b9-a743-cb7c194dea7f` / 0and1Life EFFICIENCY `37cbfe4a-2ae1-819f-8664-ff3d38fffe56` + LIFESTYLE `37cbfe4a-2ae1-81b9-a9ce-d0b937edd344`)의 제목·포커스 키워드와 대조 — 단어가 겹쳐도 각도가 다르면 중복 아님
```

**수정 후**

```
- 중복 제거 2중: ① 백로그 기존 행과 대조(**'탈락' 처리된 항목을 다시 넣지 말 것**) ② **WP REST 검색**과 대조 — `GET https://{도메인}/wp-json/wp/v2/posts?search={후보 핵심어}&status=any&per_page=20&_fields=id,slug,status,title` (Chrome에서 wp-admin 로그인 상태로 헤더 `X-WP-Nonce: wpApiSettings.nonce`). 단어가 겹쳐도 각도가 다르면 중복 아님
  - ⚠️ **Notion 발행 목록·슬러그 문자열 대조는 쓰지 않는다.** 2026-08-25 실측 사고 — 발행 목록이 2.3만 토큰이라 건너뛰고 post-sitemap 슬러그만 대조한 결과, `no-4th-floor-korea`가 이미 있는데 백로그 #22를 중복 적재했다. 슬러그에 `elevator`가 없어 빠졌고, WP 검색은 **339토큰**에 잡아냈다
  - ⚠️ `_fields` 생략 금지(미지정 시 6,599토큰). 포커스 키워드는 REST에 노출되지 않으므로 제목+본문 검색으로 대체하고, 정밀 확인이 필요한 후보만 해당 글의 현황표 행 하나를 연다
  - ⚠️ **STEP 4 월간 재검토의 "발행 목록·백로그를 읽어 추이 확인"은 그대로 Notion을 쓴다** — 편당 클릭·상위 편중도 계산에는 누적 발행 편수와 Track 태그가 필요하고 그건 WP에 없다
```

---

## 5. 검증 방법

수정 반영 후 다음 회차에서 확인할 것.

| # | 확인 항목 | 합격 기준 |
|---|---|---|
| V1 | writer STEP 3이 WP REST를 호출했는가 | STEP 8 로그에 조회한 검색어와 반환 건수가 기록됨 |
| V2 | 응답 크기가 통제되는가 | 후보당 응답 **1,000토큰 미만** (`_fields` 적용 확인) |
| V3 | 회차 카운트가 깨지지 않았는가 | `TODAY_COUNT`·`MAX_NUM`이 현황표 기준으로 정상 산출, 번호 충돌 0건 |
| V4 | 중복이 실제로 걸러지는가 | 다음 4주간 "적재 후 이미 발행됨으로 판정 정정" **0건** (직전 4주 3건) |
| V5 | 주간 점검 비용 | Notion 읽기 토큰이 직전 회차 대비 **감소** |

V4가 이 이관의 진짜 목표다. V5는 부수 효과다.

---

## 6. 반영 절차

1. GitHub의 지침 4개 파일에 위 수정 1~5를 적용하고 커밋 — **사용자 직접 수행**
2. `SKILL.md` 사본에 수정 6을 적용하고 커밋 — **사용자 직접 수행**
3. Cowork 예약 작업 `weekly-koreaplug-seo-check` 본문을 수정된 `SKILL.md` 전문으로 교체 — **사용자 직접 수행**
4. 다음 회차(8/30 주간 점검, 그리고 그 전 daily writer)에서 V1~V3 확인

3번을 빠뜨리면 파일만 바뀌고 실제 실행은 그대로다.
